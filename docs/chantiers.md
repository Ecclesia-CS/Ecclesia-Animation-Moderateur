# Liste des chantiers — Ecclesia (34 à 77)

> **À tenir à jour au fil des chantiers — sert de point de synchronisation entre contributeurs.** C'est le **seul** fichier de suivi de l'avancement du projet : `PROJECT_STATUS.md` a été supprimé au chantier 78 (2026-09-07) parce que deux fichiers de suivi en parallèle avaient produit exactement la dérive qu'on voulait éviter — il était périmé de quinze chantiers et se contredisait avec `git log`. Ne pas en recréer un second : toute mise à jour de statut vient ici.

> **Voir aussi** [`docs/registre-merges-en-attente.md`](./registre-merges-en-attente.md) — l'état détaillé des branches non mergées, des arbitrages en attente, du gel du parcours de vote (séance du 10/09) et de ce qui n'est pas commencé. Ce document-ci est l'index chronologique complet ; le registre est le tableau de bord opérationnel du moment.

> ⚠️ **Mergé ne veut pas dire vérifié.** Presque rien n'a été joué au navigateur sur ce projet — la quasi-totalité des chantiers listés ci-dessous, y compris ceux mergés sur `main` et déployés sur GitHub Pages, n'ont eu qu'une vérification `tsc`/tests/build en session headless, jamais un parcours réel à l'écran (contrainte structurelle : un seul jeton de serveur de dev partagé, une seule session à la fois a le droit de lancer `npm run dev`). Plusieurs commits le disent explicitement (« Aucune vérification navigateur », « pas de mot de passe superadmin en session headless ») ; l'absence de cette mention ne garantit pas non plus qu'un test réel ait eu lieu. `A_VERIFIER.md` reste la seule trace fiable de ce qui a été confirmé humainement.

**Méthode** : chaque ligne est établie à partir de `git log` sur `origin/main` (source de vérité pour ce qui est mergé), des branches distantes non mergées (`git branch -r`), de `docs/registre-merges-en-attente.md`, de `CLAUDE.md` et de `A_VERIFIER.md`/`A_VERIFIER.local-20260906.md`. Aucun numéro n'a été deviné : quand aucune trace fiable n'a été trouvée, c'est écrit tel quel plutôt qu'une supposition. `PROJECT_STATUS.md` n'a pas été utilisé — il s'arrête au chantier 54 (dernière mise à jour le 03/09/2026) et n'a servi à combler aucun trou ici.

**Numérotation lettrée antérieure** : avant le chantier 19 environ, les tâches étaient repérées par lettres (A1-H26, B3, C4, D16…), pas par numéro. Cette numérotation-là est hors du périmètre demandé (chantiers ≥ 34) et n'apparaît pas ci-dessous, y compris quand une branche porte encore un nom lettré (ex. `chantier-b3-reconnexion-prevoting`, `chantier-c4-vote-pass-neutre`) — ces branches sont antérieures au 34 et déjà mergées de longue date.

Légende de statut : ✅ mergé sur `main` et déployé · 🟡 livré mais pas mergé · 🟠 décidé mais pas commencé · ⚪ non commencé.

---

| # | Titre | Ce qui a été fait | Statut | Fichier / RPC principal |
|---|---|---|---|---|
| 34 | Camp sans vote sur les résultats | Un membre inscrit mais n'ayant jamais voté voyait « L'organisateur n'a pas encore nommé les groupes » au lieu de ne voir aucune carte de camp — la carte dépendait de l'allocation physique (`table_assignments`), pas de l'appartenance réelle à un camp d'opinion (`selfGroupId`, qui exige un vote pris en compte par l'analyse PCA). | ✅ Mergé (`5267448`, 2026-08-01) | `src/screens/ResultsMapScreen.tsx` |
| 35 | Synchronisation temps réel du statut modérateur | La section « Tables rattachées » du superadmin n'avait aucun rafraîchissement ; `session_members` n'avait jamais `REPLICA IDENTITY FULL`, empêchant Realtime d'appliquer la RLS sur ses `UPDATE`. `VoteScreen`/`TableContext` s'abonnent désormais à leur propre ligne. | ✅ Mergé (`e3a8aff`, 2026-08-03) | `src/context/TableContext.tsx`, `src/screens/VoteScreen.tsx` |
| 36 | Modérateur affiché deux fois + case modérateur sur "Débat en cours" | Dans l'onglet Tables du superadmin, un membre `is_moderator` apparaissait à la fois comme badge et comme puce ordinaire. `JoinTableForm` gagne la case « Je suis modérateur de cette table » (écrans « Débat en cours » et « Un ami t'invite »). | ✅ Mergé (`318d497`, 2026-08-03) | `src/screens/SuperadminScreen.tsx`, `src/components/JoinTableForm.tsx` |
| 37 | Retirer le clustering hérité + réassignation modérateur | Le bouton « Répartir en tables » court-circuitait l'allocation v2 (RPC `run_clustering_v1/v2`) sans passer par `AllocationPanel` — supprimé avec `ClusteringModal`. `set_member_moderator` pose désormais aussi le siège du membre sur une table sans modérateur, pas seulement le flag. | ✅ Mergé (`367d074`, 2026-08-03) | RPC `set_member_moderator` |
| 38 | Diagnostic puis correctif du reload superadmin | 1ère passe : hypothèse HMR (infirmée par Jules), correction en marge d'un `sessionPhase` figé dans `AnalysisPanel`. 2e passe : cause réelle trouvée — `SessionDetail` masquait tout son contenu derrière un spinner plein écran à chaque rafraîchissement de fond (polling 15s + Realtime), effondrant `scrollY`. Corrigé en ne montrant le spinner qu'au tout premier chargement. | ✅ Mergé (`6112c7e`, 2026-09-02) | `src/screens/SuperadminScreen.tsx` (`SessionDetail`), `AnalysisPanel.tsx` |
| 39 | Nomenclature des phases participant + suppression de la phase `questionnaire` | Nouveau `PhaseIndicator`/`phaseLabels.ts` (« Étape N · Libellé », 5 étapes) affiché sur 5 écrans participant. Phase `draft` renommée « Phase 0 » côté superadmin. Phase `questionnaire` retirée de l'énumération : `debating → closed` déclenche automatiquement `force_session_questionnaire`, gate désormais sur l'absence de réponse (`hasQuestionnaireResponse`) plutôt que sur la phase. | ✅ Mergé (`334c514`, 2026-09-02) | `src/lib/phaseLabels.ts`, `src/components/PhaseIndicator.tsx` |
| 40 | Ordre des modales d'entrée en débat + intitulés | Le panorama « Bienvenue dans le débat » s'affiche avant les règles (au lieu de l'inverse). Règles retitrées, bouton du panorama retitré pour annoncer la modale suivante. | ✅ Mergé (`44bb402`, 2026-09-01) | `src/screens/ParticipantView.tsx` |
| 41 | Modérateur nommé en débat invisible sans quitter/rejoindre | `isModerator` valait `physicalModerator && !moderatorRevoked` (un pur véto, jamais promoteur) : un participant nommé modérateur après avoir déjà rejoint sa table restait bloqué. Corrigé en `physicalModerator || sessionMemberIsModerator` (OR). | ✅ Mergé (`e851fa7`, 2026-09-02) | `src/context/TableContext.tsx` (calcul `isModerator`) |
| 42 | Debounce des notes participant non flush à la fermeture | Fermer la modale « Mes notes » déclenchait `onClose()` sans vider le debounce de 800ms de `saveNote` — rouvrir juste après une frappe pouvait donner l'impression que la note avait disparu. | ✅ Mergé (`f780954`, 2026-09-01) | `src/components/NotesModal.tsx` |
| 43 | Fusion de la barre d'outils modérateur dans "Outils Modo" | Regroupe Notes, Assertions votées et Questionnaire post-débat (boutons séparés du header) dans un seul menu à 3 sections (Camps & assertions, Table, Personnel). Retire le mode transcription live mort (`useTranscription.ts`). Développé et mergé sur la même branche que le chantier 44. | ✅ Mergé (`88182bf`, 2026-09-02) | `src/components/ModeratorToolsButton.tsx` |
| 44 | Bouton "Ajouter une personne sans téléphone" | Nouveau bouton dans la section Table de `ModeratorToolsButton` (à côté du QR code). RPC `add_offline_participant` : insère une ligne `participants` avec le même schéma de collision que `join_table`. Co-développé et mergé avec le chantier 43, sur la même branche (`chantier-43-outils-modo-transcription`) — **pas absorbé, les deux existent distinctement**, simplement livrés ensemble. | ✅ Mergé (`88182bf`, 2026-09-02) | RPC `add_offline_participant` |
| 45 | Questionnaire post-débat : retrait d'une question, note obligatoire en premier | Retrait de « à quel débat as-tu participé ? » (redondante avec `table_id`/`session_id`) ; la note globale 0-5 devient obligatoire et passe en première position. Vérifié en navigateur réel (worktree dédié). | ✅ Mergé (`8e9757e`, 2026-09-01) | `src/components/QuestionnaireModal.tsx`, `SessionQuestionnaireForm.tsx` |
| 46 | Résultats publics des séances closes (opt-in par séance) | `sessions.results_public` (défaut `false`). Bouton superadmin par séance close, modale « Anciennes séances », route `#results/<session_id>`. `get_public_results` durci (exige `closed` ET `results_public=true`) et enrichi (assertions + nuage PCA anonyme, sans `member_id`). | ✅ Mergé (`db939e7`, 2026-09-02) | RPC `get_public_results`, `src/screens/EntryScreen.tsx` |
| 47 | Entrée modérateur avant débat + créer une table depuis le vote | Ajoute le cas « à l'heure » (pré-vote/vote/allocation) de la déclaration modérateur, symétrique au chemin « en retard » déjà existant. Réutilise `claim_moderator_status`/`create_table` tels quels. **Remplacé depuis par le chantier 73** (bouton déplacé, création de table retirée côté participant). | ✅ Mergé (`da44d4e`, 2026-09-02) — superseded par 73 | `src/components/voting/ModeratorAccessPanel.tsx` *(supprimé par le chantier 73)* |
| 48 | "Je veux rejoindre une autre table" sur l'écran d'annonce | Nouveau lien discret sous « Accéder à la table » (`AllocatingScreen`), révèle un mini-formulaire de code à 6 caractères. Nouvelle RPC `switch_table` (pas de réutilisation de `join_table`, qui ne vérifie jamais l'appartenance à la séance ni ne nettoie les tables précédentes). | ✅ Mergé (`3c3edd9`, 2026-09-02) | RPC `switch_table`, `src/screens/AllocatingScreen.tsx` |
| 49 | Purge des codes de rappel des séances closes | `session_members.reclaim_code` (PIN en clair) n'a plus d'usage après clôture — combiné au pseudo, la donnée la plus sensible du schéma. Purge ponctuelle des séances déjà closes + `set_session_phase` purge désormais automatiquement à chaque clôture. Destructif et irréversible, autorisé par Jules sans sauvegarde. | ✅ Mergé (`a14e1ea`, 2026-09-03) — confirmé appliqué en base (vérifié chantier 76 : 0 code en clair sur 88 membres `closed`) | RPC `set_session_phase` |
| 50 | Fermer la lecture directe de `session_members`/`table_assignments` | Les deux tables avaient `SELECT USING (true)` : n'importe qui lisait noms réels + `reclaim_code` en clair de toutes les séances. Policies resserrées en self-only + nouvelle RPC `list_table_assignments_admin` pour le seul besoin croisé restant (vue Groupes superadmin). | ✅ Mergé (`769b1b4`, 2026-09-02) — confirmé appliqué en base (chantier 76) | RPC `list_table_assignments_admin`, policies `session_members_select_own`/`table_assignments_select_own` |
| 51 | Anonymat réel des auteurs d'assertions | `assertions_select_approved` laissait passer `member_id` (toutes colonnes) — croisé avec `session_members` (alors public), désanonymisait l'auteur de toute assertion approuvée. `GRANT SELECT` restreint aux colonnes sans `member_id` ; nouvelle RPC `get_my_assertion_ids` pour le seul site qui en avait besoin. | ✅ Mergé (`f99244f`, 2026-09-02) — confirmé appliqué en base (chantier 76). **Réserve non levée** : fuite possible de `member_id` par la charge utile Realtime `postgres_changes`, jamais testée en navigateur. | RPC `get_my_assertion_ids` |
| 52 | Valider les URL des sources collaboratives + fermer `collab_session_users` | `session_sources.url` n'était jamais validée (XSS `javascript:` possible, chaîne documentée vers le vol du mot de passe superadmin). Validation `http(s)` côté serveur (`add_collab_source`/`update_collab_source`) et côté client (`isSafeUrl`). `collab_session_users` restreinte au propriétaire de la ligne. | ✅ Mergé (`6a88f2b`, 2026-09-03) — confirmé appliqué en base, et complété par le chantier 72 (voir plus bas) | RPC `add_collab_source`, `src/lib/utils.ts` (`isSafeUrl`) |
| 53 | Plafonner le refetch déclenché par broadcast Realtime | Le canal `table:<id>` est ouvert en émission à quiconque a la clé anon publique — DoS confirmé exploitable. Debounce ~1s + plafond de fréquence (5 msg/s) sur la réception du broadcast `refresh`, sans toucher à l'émission ni aux 3 autres couches de rattrapage. | ✅ Mergé (`0a3b4e1`, 2026-09-02) — confirmé appliqué (chantier 76) | `src/context/TableContext.tsx` |
| 54 | Retirer au modérateur la possibilité de supprimer sa table | `TableContext.endTable()` (code mort côté écrans) et la policy `tables_delete_moderator` permettaient encore un `DELETE` direct via l'API REST. Jules a tranché : aucune capacité de suppression côté modérateur, par aucun chemin. `DROP POLICY` sans remplacement. | ✅ Mergé (`07c8c41`, 2026-09-03) — confirmé appliqué en base (chantier 76 : policy absente) | `DROP POLICY tables_delete_moderator` |
| 55 | Preuve de possession sur les reprises d'identité | Exiger le code de rappel (et non le seul pseudo) pour reprendre une identité (`confirm_attendance`, `reclaim_prevoting_member`, `register_collab_pseudo`…). | ⚪ Non commencé | Aucune branche. Bloqué sur un arbitrage produit de Jules (code obligatoire vs filet de rattrapage superadmin) — voir `docs/registre-merges-en-attente.md` §7. Cible : RPC `confirm_attendance` |
| 56 | Durcissement SQL : `search_path` + `app_config` | `search_path = public, extensions` figé sur les fonctions à `crypt()` (`check_superadmin_password`, `create_table`, `reclaim_moderator` ×2, `claim_moderator_status`, `set_session_results_public` — `get_public_results` l'avait déjà). `REVOKE ALL ON app_config, assertion_merges FROM anon, authenticated` (seconde barrière indépendante de la RLS zéro-policy déjà en place). | ✅ Mergé (`208cc0f`, 2026-09-16) — migration appliquée en base par la session de chantier, avec Jules disponible comme l'exigeait le registre. Signatures/`proconfig`/grants vérifiés en base avant et après ; `crypt()` confirmé fonctionnel (test indirect). **Recette navigateur complète (Code Ecclesia + `reclaim_moderator` réels) encore à jouer par Jules** — voir `A_VERIFIER.md`. | `supabase/migrations/20260916_chantier56_durcissement_sql.sql` |
| 57 | Quota et plafond de charge utile sur `gemini-proxy` | L'Edge Function ne vérifiait qu'un JWT valide — n'importe quel porteur de la clé anon pouvait l'appeler sans limite. Ajoute un quota de 20 appels/60s par `user_id` (429) et un plafond de 300 Ko de charge utile (413). | ✅ Mergé (`c905d20`, 2026-09-02) — confirmé dans le code (chantier 76). **Edge Function redéployée le 2026-09-20** (chantier 83). **Testé au navigateur le 2026-09-20 (122 appels réels)** : le plafond de taille (413) fonctionne. **Le quota de débit (429) ne se déclenchait jamais en pratique** — compteur en mémoire par instance Edge Function, sans garantie de réutiliser la même instance d'un appel à l'autre. **Corrigé au chantier 83bis** (même jour) : compteur remplacé par une table + RPC Postgres partagées (`check_gemini_rate_limit`). Migration appliquée en base, Edge Function redéployée via le dashboard, **quota re-testé au navigateur et confirmé fonctionnel** : 30 requêtes séquentielles → blocage exact à la 21ᵉ ; 40 requêtes en parallèle → exactement 20/20, aucun dépassement même sous charge concurrente. Détail dans `A_VERIFIER.md` § Chantier 83bis. | `supabase/functions/gemini-proxy/index.ts`, `supabase/migrations/20260920_chantier83bis_gemini_rate_limit_partage.sql` |
| 58 | Restreindre les colonnes de `sessions` lisibles publiquement | `description`, `doc_*_url`, `moderation_policy`, `group_names`, `results_public` etc. ne sont plus lisibles par `select()` direct. 4 RPC (`get_session_by_id`, `get_session_by_join_code`, `list_sessions_admin`, `list_public_closed_sessions`) pour les 8 `select('*')` et 3 lectures de colonnes retirées. | ✅ Mergé sur `main` et `REVOKE`/`GRANT` appliqué le 2026-09-07 (dérogation explicite de Jules au gel du 10/09, comme le chantier 59). `onboarding_enabled` inclus dans les colonnes accordées. Recette de vérification déroulée via MCP/`curl` — détail dans `A_VERIFIER.md`. | RPC `get_session_by_id`, `list_sessions_admin` |
| 59 | Canaux Realtime privés | Ferme le DoS de fond du chantier 53 (F6) : `privateChannel()` (nouveau helper, journalise les `CHANNEL_ERROR`), policies sur `realtime.messages` (lecture par topic, émission restreinte). 2 canaux superadmin déjà morts supprimés au passage. | 🟡 Livré, pas mergé — migration écrite, **non appliquée**. **Ordre non négociable** : migration → code → recette → **puis seulement** désactiver « Allow public access » dans le dashboard Supabase (réglage global, hors SQL — l'inverser casse toute la prod). Rien avant la séance du 10/09. Branche `chantier-59-realtime-prive`. | `src/lib/realtime.ts` (`privateChannel`) |
| 60 | Découpler l'autorité du modérateur de la propriété de la table | Un membre marqué `session_members.is_moderator` (allocation v2) voyait `ModeratorView` (chantier 41) mais aucune action n'aboutissait : les gardes testaient `created_by`, or `apply_allocation` y pose l'uid du superadmin. Nouveau helper `is_table_moderator`, repris sur 9 fonctions + 7 policies RLS. | ✅ Mergé (`3946bfc`, 2026-09-02) — confirmé appliqué en base (chantier 74 s'appuie dessus) | RPC/helper `is_table_moderator` |
| 61 | Inscription et vote pendant la phase `allocating` | `register_session_member` refusait toute phase hors `draft/pre_voting/voting` — un retardataire ne pouvait pas rejoindre pendant le calcul de répartition. `cast_vote`/`submit_assertion` n'avaient déjà aucune garde de phase. `allocating` ajouté aux phases d'inscription. | ✅ Mergé (`998ecd2`, 2026-09-02) | RPC `register_session_member` |
| 62 | Sortie de secours pour un participant sans affectation de table | Un membre inscrit après `apply_allocation` (via le chantier 61) n'avait pas de `table_assignments` et restait bloqué sur « Formation des groupes en cours… » indéfiniment. `TableAssignmentCard` distingue désormais loading / pas d'affectation en `debating` (formulaire de code, réutilise `switch_table`) / pas d'affectation en `closed`. | ✅ Mergé (`2a5da88`, 2026-09-02) | `src/components/voting/TableAssignmentCard.tsx` |
| 63 | Questionnaire de clôture masqué par l'overlay "Séance terminée" | Les deux surcouches plein écran étaient à z-index égal ; l'overlay (rendu après) l'emportait toujours. Corrigé en masquant l'overlay tant que le questionnaire forcé est ouvert. `onDone` du questionnaire menait à un cul-de-sac sur 2 des 3 écrans d'entrée — corrigé pour converger vers les résultats. | ✅ Mergé (`c6d89f4`, 2026-09-02) | `src/screens/ParticipantView.tsx` |
| 64 | Une table `leaderless` devient modérée quand un membre devient modérateur | `set_member_moderator`, `claim_moderator_status`, `assign_moderator_to_table` posent `leaderless = false` quand ils affectent un modérateur déjà assis sur une table sans animateur. Deux compléments le même jour : `leaderless_by_design` (distingue une table conçue modérée d'une table convertie) + bascule arrière au départ du modérateur (`switch_table`, puis `move_member_to_group` par symétrie). | ✅ Mergé (`1369101`, 2026-09-02) | `tables.leaderless_by_design`, RPC `set_member_moderator` |
| 65 | Verrouiller l'inscription aux séances en brouillon | `register_session_member` acceptait encore la phase `draft` (oubli du chantier 61). Retiré côté RPC et frontend ; même trou fermé dans `confirm_attendance`, qui ne testait aucune phase. | ✅ Mergé (`ed36859`, 2026-09-02) | RPC `register_session_member`, `confirm_attendance` |
| 66 | `join_table` nettoie les appartenances précédentes de la séance | `join_table` faisait un simple upsert : rejoindre une nouvelle table dans la même séance laissait l'ancienne ligne `participants` en place (identités fantômes constatées en base). Nouvelle fonction `leave_other_session_tables`, reprise par `join_table` et `switch_table`. | ✅ Mergé (`0d40109`, 2026-09-04) | RPC `leave_other_session_tables` |
| 67 | Quatre correctifs issus de la revue des parcours | (1) Pré-votant plus éjecté au passage en phase `voting`, option « continuer à distance » sans confirmer sa présence. (2) `claim_moderator_status` pose désormais `reclaim_code` en pré-vote, comme `register_session_member`. (3) `sync_table_assignment` ne masque plus ses erreurs en silence. (4) `AppIntroModal` aligné sur les 5 étapes du chantier 39. Correctif ultérieur le 09-03 : la migration ne devait pas écraser le bloc du chantier 64 dans `claim_moderator_status`. | ✅ Mergé (`72ec2fe`, 2026-09-03) | RPC `claim_moderator_status`, `sync_table_assignment` |
| 68 | `claim_table_as_moderator` : choisir sa table par code sans déposséder le modérateur en place | `JoinTableForm`/`EntryScreen` appelaient `reclaim_moderator`, qui écrase `created_by` sans aucune vérification. Nouvelle RPC dédiée + helper `table_has_moderator` : refuse Code Ecclesia invalide, code d'une autre séance, ou table déjà modérée. `reclaim_moderator` reste pour son usage légitime (le modérateur déjà en place). **Point laissé ouvert** : plus aucun écran n'expose de chemin pour la « vraie » reprise de main. | ✅ Mergé (`f00458a`, 2026-09-04) | RPC `claim_table_as_moderator`, `table_has_moderator` |
| 69 | Écran postvote (revoter après le débat) | Nouveau `PostVoteScreen`, accessible depuis `ResultsMapScreen` (« ↻ Revoter ») : revoter sur ses assertions, en proposer une nouvelle, voter sur celles jamais vues. Signale au passage que `cast_vote` écrase le vote initial sans historique — pas de comparaison avant/après débat possible avec le schéma d'alors (repris par le chantier 70). | ✅ Mergé (`8bdf3c5`, 2026-09-04) | `src/screens/PostVoteScreen.tsx` |
| 70 | Historiser les votes pour mesurer le déplacement des opinions | Nouvelle table `assertion_vote_history` alimentée par `cast_vote` avant chaque écrasement. `session_analysis.vote_scope` (`current`/`pre_closure`) permet de reconstituer les votes juste avant clôture. Deux nouvelles RPC de lecture (`list_session_analyses`, `get_analysis_by_id`) pour un futur écran de comparaison — **non construit ici, volontairement laissé à Jules**. | ✅ Mergé (`6123ef5`, 2026-09-04) | Table `assertion_vote_history`, RPC `cast_vote` |
| 71 | Désactiver l'onboarding par séance | Retour de Jules : « le vote ne doit pas servir de référence [...] je dois juste pouvoir créer une session sans onboarding. » Nouvelle colonne `sessions.onboarding_enabled` (défaut `true`), interrupteur superadmin, `VoteScreen` saute l'onboarding quand le flag est faux. Bloquant pour la séance de production du 10/09. | ✅ Mergé (`3e1e1d2`, 2026-09-04) | `sessions.onboarding_enabled`, RPC `set_session_onboarding_enabled` |
| 72 | Modération superadmin : reprise de table, ajout de modérateur, defaults | Corrige deux bugs indépendants derrière « Cette table a déjà un modérateur » (retrait incomplet de `created_by`, et refus du modérateur légitime revenant d'un autre appareil). Nouvelle RPC `release_table_moderation`, helper `table_moderator_is`. Ajoute l'accès manquant pour « ajouter un modérateur » à une table sans animateur, un défaut « actif » pour un membre sans `entry_responses`, `DROP` de la surcharge non validée d'`add_collab_source` (4 arguments, contournait le chantier 52), édition du titre/description d'une séance, réorganisation de l'onglet Tables. **Arbitrage en attente de validation par Jules** (voir registre §5). | ✅ Mergé (`00fd5ae`, 2026-09-06) — confirmé appliqué en base | RPC `release_table_moderation`, `table_moderator_is`, `update_session_meta` |
| 73 | Déclaration modérateur dès l'inscription | Case « Je suis modérateur » sur les 3 formulaires d'inscription (`PseudoForm`, `VotingEntryForm`, `AttendanceConfirmScreen`), dans les 4 phases pertinentes — inscription toujours réussie même si le mot de passe est faux. Bouton « Je suis modérateur »/« Créer une table » retiré du header (`ModeratorAccessPanel` supprimé), remplacé par « Me déclarer modérateur » dans le panneau Outils. Bouton des anciennes séances aligné visuellement ; ordre des modales d'accueil pré-vote inversé. **Les 3 onglets d'`EntryScreen` n'ont pas été retirés** (inventaire fait, conclusion inversée depuis par le chantier 75). | ✅ Mergé (`1c62418`, 2026-09-06) | `src/screens/VoteScreen.tsx`, `ModeratorDeclareField.tsx`, `ModeratorClaimModal.tsx` |
| 74 | Modale des phases + participant hors ligne | (1) La pill `PhaseIndicator` devient un bouton ouvrant une modale listant les 5 phases, l'étape en cours marquée. (2) Diagnostic du signalement « les participants sans téléphone n'existent pas en base » : la garde d'autorisation était déjà corrigée par le chantier 60 (fausse piste) — le vrai gap est que `add_offline_participant` n'écrivait jamais dans `session_members`, seulement `participants`. (3) Champ de saisie invisible corrigé (`text-gray-900 bg-white` — le texte héritait du `text-white` de la racine sombre de `ModeratorView`). | 🟡 Livré, pas mergé — **et pas même poussé sur `origin` sous ce nom** : les 2 commits existent uniquement dans le worktree local `Ecclesia-chantier-71` (branche locale `chantier-74-phases-et-hors-ligne`), le push a échoué (« refusing to update checked out branch ») et n'a jamais été refait. La **migration SQL, elle, a été appliquée directement en base par une autre session** le 06/09 (confirmé : `add_offline_participant` écrit désormais dans `session_members` aussi). | `src/components/PhaseIndicator.tsx`, RPC `add_offline_participant` |
| 75 | Retrait des 3 onglets de `EntryScreen` | Le chantier 73 avait conclu qu'il fallait garder « Rejoindre » et « Créer » faute d'équivalent. **Jules a tranché l'inverse le 06/09** : retirer les trois onglets (« Modérateur / Rejoindre / Créer »), la création de table étant du ressort du superadmin et personne ne rejoignant une séance par le seul numéro de table. À vérifier avant tout retrait : que le chemin « rejoindre une table par son seul code en pleine phase débat » existe réellement et est atteignable (`switch_table` depuis `AllocatingScreen`, `JoinTableForm` à l'étape `ended` de `VoteScreen` — qui ne reçoit pas de `sessionId`, contrairement à `SessionRouterScreen`) — le construire d'abord si besoin, retirer ensuite. | 🟠 Décidé, jamais lancé | Aucune branche. Cible : `src/screens/EntryScreen.tsx` |
| 76 | Plan de sécurité consolidé | Les 4 documents d'audit/revue/plan de sécurité (03/08 et 02/09) relus un par un contre le code et la base de production **d'aujourd'hui** (MCP Supabase, lecture seule), pas contre les fichiers de migration. Chaque constat classé corrigé / ouvert / sans objet, avec preuve et ordre de traitement. Fait nouveau trouvé en vérifiant : `results_public` déjà actif sur 2 séances réelles. | 🟡 Livré, pas mergé — purement documentaire, aucun risque à merger. Branche `chantier-76-plan-securite`. | `docs/2026-09-06-plan-securite-consolide.md` |
| 77 | Liste des chantiers (ce document) | Index chronologique et vérifié des chantiers 34 à 77, pour que chaque conversation puisse voir d'un coup d'œil ce qui a été fait et livré sans reconstituer l'historique à chaque fois. | 🔵 En cours (ce chantier) | Branche `chantier-77-liste-chantiers`. `docs/chantiers.md` |
| 79 | Écran de comparaison avant/après débat | Nouvel accordéon « Comparaison avant / après débat » dans l'onglet 📊 Analyse : apparie les camps de deux analyses (chantier 70 : `vote_scope` `pre_closure`/`current`) par **composition réelle de membres** (`pairGroups`, glouton par chevauchement), jamais par `group_id` — deux calculs k-means successifs ne numérotent pas les camps pareil. Affiche mouvement des membres entre camps, appariement avec effectifs, assertions dont le consensus a le plus bougé (comparaison directe, indépendante du numéro de groupe). Vérifié au navigateur par Jules (09/07) sur une séance de test générée en base (16 membres fictifs, votes avant/après débat, analyse `pre_closure` calculée avec le vrai pipeline PCA/k-means) — donnée de test supprimée après validation. **Point laissé ouvert** : aucun bouton superadmin ne déclenche encore le calcul d'une analyse `pre_closure` en conditions réelles — sans ça l'écran n'a un second terme à comparer que si quelqu'un revote via le postvote (chantier 69) sur une séance déjà close, voir `A_VERIFIER.md`. | ✅ Mergé (`f41a220`, 2026-09-07) — vérifié au navigateur par Jules | `src/components/AnalysisPanel.tsx` (`AnalysisComparisonPanel`), `src/lib/analysis.ts` (`pairGroups`) |
| 89 | Phase `post_voting` — le revote post-débat devient une fenêtre, pas un accès permanent | Jules : le passage `debating → closed` ouvrait le revote (chantier 69) sans jamais le refermer. Nouvelle phase `post_voting` insérée entre `debating` et `closed` : le questionnaire forcé et le revote (`PostVoteScreen`) se déclenchent à l'entrée en `post_voting` ; le passage en `closed` coupe le bouton « ↻ Revoter » sur `ResultsMapScreen`. Tous les points qui routaient sur `phase === 'closed'` pour détecter "débat fini" (`VoteScreen`, `AllocatingScreen`, `TableAssignmentCard`, `ParticipantView`, `ModeratorView`, `SessionRouterScreen`) traitent désormais `post_voting` de la même façon ; seuls la purge de `reclaim_code` et le résumé public (`get_public_results`) restent gated sur `closed` strictement. Nomenclature participant à 6 étapes (`PARTICIPANT_PHASE_STEPS`) au lieu de 5. | ✅ Mergé (2026-09-07), migration appliquée en base. **Vérifié au navigateur** le 07/09 (séance QA jetable, purgée après test) : `debating → post_voting → closed` en Realtime, questionnaire forcé, revote fonctionnel, bouton « ↻ Revoter » disparu en `closed`, zéro erreur console. **⚠️ Mergé pendant le gel du parcours de vote** (touche `VoteScreen.tsx`, `docs/registre-merges-en-attente.md` §1) — autorisé explicitement par Jules le 07/09, avant la fin de la séance du 10/09. Non couvert : le bouton `PhaseBar` superadmin lui-même (mot de passe non transmis — transitions faites par `UPDATE sessions.phase` direct) et le visiteur non inscrit en `post_voting`. Détail dans `A_VERIFIER.md` § Chantier 89. | `src/lib/types.ts`, `src/lib/phaseLabels.ts`, `src/screens/SuperadminScreen.tsx`, `src/screens/ResultsMapScreen.tsx`, `supabase/migrations/20260907_chantier89_post_voting_phase.sql` |
| 80 | Réserves sécurité sur `get_public_results` | Jules, 07/09 : ne pas pouvoir remonter aux participants depuis la page publique, ou le rendre dur. Vérification en base (`pg_get_functiondef`) : aucun identifiant/pseudo ne sortait déjà de la fonction. Deux trous corrigés : points du nuage renvoyés dans l'ordre d'inscription faute d'`ORDER BY` (ajout `ORDER BY random()`), `SET search_path` manquant sur cette fonction `SECURITY DEFINER`. Explicitement hors périmètre (refusé par Jules) : seuil de k-anonymat, compteurs par assertion, coupure d'une séance déjà publique. | ✅ Migration appliquée en base et vérifiée au navigateur le 2026-09-07 (deux séances publiques réelles) | `supabase/migrations/20260907_chantier80_reserves_public_results.sql`, RPC `get_public_results` |
| 88a/88b/88c | Dette héritée (annexe B) — toasts, page 404, QR code superadmin, vérification navigateur — **mergé** | Sur les 11 items de l'annexe B, triage préalable : D1/A2/A3/A4+D17 déjà résolus par des chantiers antérieurs (3, 8, 39/89 — rien à refaire), C7 gelé (touche `VoteScreen.tsx`, gel jusqu'au 10/09, laissé de côté même après feu vert de Jules pour dépasser le gel — faute de repro connue du bug, aucun repro trouvé nulle part dans le dépôt) et C2 bloqué sur une charte graphique de Jules. Code écrit : infra toasts (`ToastContext`, montée à la racine), branchée sur trois cas jusqu'ici silencieux (exclusion d'un participant, table supprimée pendant qu'on y est, table restaurée depuis `localStorage` introuvable au démarrage) ; page 404 (`NotFoundScreen`) pour tout hash inconnu ; QR code superadmin (bouton dans `ShareLinkBanner`, réutilise `QrCodeModal` déjà existant côté modérateur/participant) remplaçant le recours à un site externe. La persistance de la pause après rechargement (item annexe B) s'est révélée déjà implémentée (chantier antérieur, `ModeratorView.tsx`) — pas de code à écrire. **88c** : jeton de serveur de dev pris, table de test jetable créée/purgée par SQL direct pour ne pas toucher la séance réelle en cours sur l'accueil — D1, A2 (DnD) et A3 (notes) confirmés au navigateur, 404 confirmée. Fait au passage : `ModeratorView` déborde horizontalement en viewport mobile (375px) — probablement volontaire (vue « projetable »), jamais tranché explicitement, signalé dans `A_VERIFIER.md` plutôt que corrigé sans confirmation. **Suite le 2026-09-15** : le gel du 10/09 étant passé, C7 (bug prévote) reste néanmoins abandonné faute de repro (Jules a choisi de laisser tomber plutôt que d'en chercher une). Branche rebasée sur `origin/main` (chantiers 58/59 mergés entre-temps — colonnes `sessions` restreintes, canaux Realtime privés) : un seul conflit dans `TableContext.tsx` (fusion d'imports), résolu proprement, `tsc`/build/tests repassés verts. Les deux derniers scénarios de toast (table supprimée en Realtime pendant qu'un onglet y est, table restaurée introuvable au démarrage) confirmés au navigateur avec le nouveau `privateChannel()`. | ✅ Mergé sur `main` le 2026-09-15 (`4e0394f`). `tsc`/build/tests propres, **D1/A2/A3/404, les 3 scénarios de toast et le QR code superadmin tous vérifiés** — ce dernier confirmé visuellement par Jules lui-même via un serveur de dev local pointant sur la vraie base de production. C7 et C2 restent ouverts en annexe B. | `src/context/ToastContext.tsx`, `src/screens/NotFoundScreen.tsx`, `src/App.tsx`, `src/context/TableContext.tsx`, `src/components/ParticipantsTable.tsx`, `src/screens/SuperadminScreen.tsx` |
| 90 | Sortie de débat : le passage en postvote devient optionnel | Jules, 16/09 : le bouton superadmin "phase suivante" doit demander, en quittant `debating`, si on va en `post_voting` ou directement en `closed`. Nouveau composant `DebateExitModal` (deux boutons d'action + annuler), intercepté dans le câblage `onNext` de `PhaseBar` seulement quand `currentSession.phase === 'debating'` (les autres transitions gardent l'ancien `ConfirmModal`). `handlePhaseChange` déclenche `force_session_questionnaire` dans les deux issues de sortie de `debating` (`post_voting` **ou** `closed`), pas seulement vers `post_voting` — décision de Jules ("1 oui"). Purge de `reclaim_code` non touchée : déjà gated côté RPC sur l'entrée en `closed` (chantier 49), se déclenche simplement plus tôt en cas de saut direct — décision de Jules ("2 oui"). | ✅ Mergé (2026-09-16). `tsc --noEmit` propre, **vérifié au navigateur** le jour même sur la séance de test partagée `FADE01` (mot de passe superadmin fourni par Jules) : les deux issues (`post_voting` et `closed` direct) confirmées en base, questionnaire forcé dans les deux cas, "Annuler" neutre. Détail dans `A_VERIFIER.md` § Chantier 90. | `src/screens/SuperadminScreen.tsx` |
| 91 | Allocation : les passifs deviennent du public — **mergé** | Consigne de Jules du 16/09, affinée en conversation (une v1 à ratio 3/5 a été abandonnée le même jour). Les tables se forment sur les seuls actifs (5 à 14 par table animée, 5 à 7 sans modérateur), la règle « assez d'actifs » est supprimée, les passifs sont placés en public uniformément sur les tables animées, limite de 30 personnes par table. Quatre corrections trouvées en mesurant : maximin d'hétérogénéité plafonné au seuil de viabilité (120 part. : 6 tables animées au lieu de 11 dont 5 sans modérateur), tailles équilibrées entre tables animées, boucle du surplus de modérateurs (chantier 25b) qui manquait le bon nombre d'animateurs, tables enregistrables protégées des non-consentants du public. Aucune migration. | ✅ Mergé le 2026-09-16 après validation de Jules du tableau avant/après — recette navigateur non jouée | `src/lib/allocation.ts`, `src/components/voting/TableDiagnosticsList.tsx` |
| 92 | Allocation : appairage entre participants — **branche non mergée** | Consigne de Jules du 16/09. Un participant cite 1 ou 2 personnes (4e question d'onboarding, facultative ; « Mes binômes » dans les Outils du vote, de l'écran d'annonce et de la table). Seuls les liens **réciproques** comptent ; grappes plafonnées à 3, découpage déterministe (liens les plus anciens gardés). Règle 1 de l'allocation : grappes regroupées par échanges avec des personnes seules, recherche locale par blocs, passifs appairés suivent leur grappe — dégradation sans exception si impossible. Glisser-déposer superadmin : la grappe se déplace entière, badge 🔗, compteur de grappes réunies. Retardataire en allocation/débat rattaché à la table de la personne citée réciproquement. Banc : 0 grappe cassée, aucune table rendue non viable sur les 9 scénarios du 91 ; degré d'hétérogénéité minimal en baisse modérée. Migration appliquée ; recette navigateur non jouée ; Jules a validé les choix et le tableau, et demandé que la réciprocité soit explicite à la proposition et après le clic (encadré + écran « Tes binômes »). Parcours participant vérifié au navigateur ; étapes superadmin non jouées (mot de passe). | ✅ Mergé sur `main` (2026-09-16) | Migration `20260916_chantier92_appairages.sql`, `src/lib/allocation.ts` (+ tests, `bench/chantier-92-compare.test.ts`), `src/lib/voting.ts`, `PairingModal.tsx` (nouveau), `OnboardingForm.tsx`, `ParticipantToolsButton.tsx`, `VoteScreen.tsx`, `AllocatingScreen.tsx`, `AllocationPanel.tsx`, `SuperadminScreen.tsx` |
| 94 | Vue modérateur : temps de parole par camp idéologique | Consigne de Jules du 16/09 : afficher au modérateur le temps de parole cumulé par camp (clustering pol.is), avec floutage 5 min pour éviter la désanonymisation, plus deux garde-fous validés par Jules (« bonne initiatives de ta part ») : rien tant qu'un camp n'a pas ≥2 personnes à la table, rien avant un seuil de temps cumulé — **300s (5 min), confirmé par Jules le 16/09** (une première version avait mis 180s par erreur d'interprétation du floutage, corrigée le même jour). Nouvelle RPC `get_table_camp_speaking_times` (`SECURITY DEFINER`, auth `is_table_moderator`, agrégation en base — aucune composition individuelle exposée). Floutage 5 min assuré côté client par un `setInterval` de polling dans un composant isolé (`CampSpeakingTimes.tsx`, pattern `SessionTimerDisplay`), jamais par la RPC elle-même — les deux mécanismes (floutage et seuil minimum) sont indépendants dans le code même s'ils partagent la même valeur. Séance de test créée en base (join_code table `C94A01`) pour que Jules puisse valider au navigateur en se connectant comme modérateur. | ✅ Vérifié au navigateur par Jules le 2026-09-18 (servi en local depuis ce worktree, séance de test créée en base) : seul Camp Nord (≥2 pers., ≥5 min) s'affiche, Camp Sud/Camp Est restent cachés comme attendu. Bug trouvé et corrigé pendant la recette : le composant avalait silencieusement les erreurs RPC (`.catch(() => {})`), masquant la vraie cause d'un premier échec (changement d'`auth.uid()` entre deux sessions anonymes sur deux ports de dev différents) — corrigé pour logger en console. Détail complet dans `A_VERIFIER.md` § Chantier 94 ; séance de test encore en base, purge SQL prête mais pas relancée | `supabase/migrations/20260916_chantier94_camp_speaking_times.sql`, `src/components/CampSpeakingTimes.tsx`, `src/screens/ModeratorView.tsx` |
| 95 | Ménage des portes d'entrée + tables créées à la main par le superadmin | Remplace le 75. Les deux accordéons de l'onglet Tables (« Tables rattachées », « Tables disponibles à rattacher ») et les trois onglets de l'accueil (« Modérateur », « Rejoindre », « Créer ») sont supprimés : l'onglet « Créer » était la seule source de tables hors séance, donc du second accordéon, et les deux autres doublonnaient des chemins déjà couverts (formulaire de rattrapage en phase débat, `ModeratorDeclareField`/`ModeratorClaimModal`). En remplacement, la vue Groupes gagne « + Table animée » / « + Table sans modérateur », disponibles en `allocating` **et** `debating`. Verrou technique levé : le numéro de table n'existait que dans `table_assignments`, donc une table vide était invisible et impossible à viser en glisser-déposer — nouvelle colonne `tables.table_number`, RPC `admin_create_session_table`, `move_member_to_group` et `sync_table_assignment` adaptées. Côté participant, nouveau `TableChangeModal` : un déplacement décidé pendant le débat ne réécrivait que l'affectation, et la personne ne voyait strictement rien (ni polling, ni au rechargement). Déclaration modérateur laissée fermée en phase `allocating` (décision de Jules), avec un message explicite à l'écran. | 🚧 SQL appliqué en base, recette navigateur superadmin à jouer par Jules (`A_VERIFIER.md` § Chantier 95) | `supabase/migrations/20260918_chantier95_numero_table_sur_tables.sql`, `src/screens/SuperadminScreen.tsx`, `src/screens/EntryScreen.tsx`, `src/screens/ParticipantView.tsx`, `src/screens/AllocatingScreen.tsx`, `src/components/TableChangeModal.tsx`, `src/lib/sessions.ts`, `src/App.tsx` |
| 100 | Diagnostic cybersécurité : interruption de séance & exfiltration | Audit demandé par Jules le 19/09, sans code. Base de production relue en lecture seule (policies, grants colonne par colonne, corps de fonctions, advisors) contre deux questions : peut-on interrompre une séance en cours, peut-on voler des données. **Vol de données : non à grande échelle** — les fuites de masse du 03/08 restent fermées, le PIN de rappel n'existe plus en clair (93), `search_path` et `app_config` durcis (56), canaux Realtime privés (59). **Interruption : oui** — cinq helpers `SECURITY DEFINER` (`leave_other_session_tables`, `sync_table_assignment`, `clear_reclaim_attempts`, `record_reclaim_failure`, `gen_member_reclaim_code`) ont `EXECUTE` pour `anon`, ne vérifient aucune autorité, prennent pour deux d'entre eux le `user_id` **en paramètre**, et ne sont appelés nulle part dans `src/`. Trouvé au passage : la restriction de colonne du chantier 51 sur `assertions` n'est **plus en vigueur en base** (`member_id` de nouveau accordé à `anon`), sans qu'aucune migration du dépôt ne l'explique. Correctif principal recommandé : un `REVOKE EXECUTE`, sans changement de frontend. | ✅ Audit livré (document seul, aucune migration, aucun code) | `docs/2026-09-19-audit-chantier100-interruption-exfiltration.md` |
| 97 | Vue modérateur : badge actif/passif + présence réelle à la table | Consigne de Jules du 19/09, deux demandes fusionnées (même écran) : un indicateur actif/passif à côté de chaque nom (`entry_responses.participation_style`, chantier 91 — la donnée existait déjà, ce chantier ne fait que l'afficher), et la visibilité des membres affectés à la table (`table_assignments`) même avant qu'ils se connectent physiquement, en grisé/italique. `table_assignments`/`session_members` étant self-only depuis le chantier 50, une lecture directe ne verrait que la ligne de l'appelant — nouvelle RPC `list_table_members_for_moderator` (`SECURITY DEFINER`, garde `is_table_moderator`, même schéma d'autorisation que le chantier 94) qui renvoie le roster complet de la table avec un flag `connected` calculé côté serveur (existence d'une ligne `participants` pour le même `user_id`). Aucune colonne liée au clustering d'opinion exposée. | ✅ Migration appliquée en base et **vérifiée au navigateur** dans ce worktree : séance + table + 4 membres de test créés en base (1 modérateur actif connecté, 1 participante passive connectée, 2 membres affectés non connectés — un actif, un passif), roster affiché correctement (badges Actif/Passif sur les 4, les 2 non connectés grisés/italiques), `tsc`/`build` propres. Données de test purgées après vérification. Comportement inchangé pour `ParticipantView` (table `leaderless`) : `tableId`/`isModerator` non transmis à `ParticipantsSidebar`, pas de fetch, pas de badge. | `supabase/migrations/20260919_chantier97_moderator_table_roster.sql`, `src/lib/voting.ts` (`loadTableMembersForModerator`), `src/components/ParticipantsSidebar.tsx`, `src/screens/ModeratorView.tsx` |
| 98 | Allocation : option « interdire les tables sans modérateur » | Consigne de Jules du 19/09 : « mettre la possibilité d'interdire la création de table sans modérateur. Dans ce cas, on laisse d'autres critères être brisés, bien sûr. » Nouveau champ `AllocationInput.forbidUnmoderatedTables` (désactivé par défaut, comportement inchangé) : quand actif, `enumerateShapes` ne retient que les formes où toutes les tables sont animées — rang au-dessus des 4 règles habituelles, pas une 5ᵉ règle lexicographique. En cas de capacité de modération insuffisante, repli sur le filet de sécurité déjà existant (table unique), qui dégrade tout le reste sans jamais lever d'exception. Interrupteur ajouté dans `AllocationPanel.tsx`, persisté en `sessionStorage` avec le reste de l'état de travail (comme les chantiers 25/H14). | ✅ Mergé (2026-09-19). `tsc`/tests/build propres (5 nouveaux tests dédiés), **vérifié au navigateur et confirmé par Jules** sur une séance de test créée puis supprimée en base (`C98TEST`) — détail dans `A_VERIFIER.md` § Chantier 98 | `src/lib/allocation.ts`, `src/components/voting/AllocationPanel.tsx` |
| 102 | Sécurité : refermer les six helpers SQL exposés à `anon` | Lot 1 du [plan anti-interruption](./2026-09-19-plan-anti-interruption-seance.md), issu du diagnostic du chantier 100. `REVOKE EXECUTE` sur `leave_other_session_tables`, `sync_table_assignment`, `clear_reclaim_attempts`, `record_reclaim_failure`, `gen_member_reclaim_code` et `generate_session_join_code` — aucune n'est appelée depuis `src/`, leurs seuls appelants sont d'autres fonctions `SECURITY DEFINER` `OWNER = postgres` (non affectées, le contrôle d'exécution porte sur le propriétaire). **Piège rencontré** : `REVOKE ... FROM anon, authenticated` seul ne suffit pas — PostgreSQL accorde `EXECUTE` à `PUBLIC` par défaut et ces deux rôles en héritent ; il faut `REVOKE ... FROM PUBLIC`. Aucun changement de frontend. | ✅ Mergé (2026-09-19). Vérifié en base (`has_function_privilege` false pour `anon`/`authenticated`, toujours true pour `postgres`/`service_role`), par appel REST direct (`leave_other_session_tables` renvoie `42501 permission denied`), et au navigateur (`join_table` rejoué sur la séance de test « Test chantier 94 », participant bien ajouté) — détail dans `A_VERIFIER.md` § Chantier 102. `switch_table`/reclaim/création de table modérateur non rejoués (même mécanisme, non affecté) | `supabase/migrations/20260919_chantier102_revoke_exec_internal_helpers.sql` |
| 103 | Sécurité : ne plus faire confiance au `user_id` reçu en paramètre | Lot 2 du [plan anti-interruption](./2026-09-19-plan-anti-interruption-seance.md), rend permanent le 102. `leave_other_session_tables` et `sync_table_assignment` lisaient un `p_user_id` fourni par l'appelant au lieu de `auth.uid()` — un `GRANT EXECUTE` qui se rétablirait un jour (déjà arrivé sans trace, voir 105) rouvrirait l'éjection/déplacement à distance d'un participant arbitraire. `DROP FUNCTION` + `CREATE` (changement de signature, paramètre retiré), corps repris de `pg_get_functiondef` en base ; les quatre appelants (`join_table`, `switch_table`, `create_table`, `claim_table_as_moderator`) recréés dans la même migration pour appeler la nouvelle signature — leur propre signature ne change pas, et ils passaient déjà `auth.uid()`, donc changement fonctionnellement neutre. **Piège rencontré, nouveau par rapport au 102** : ce projet a des privilèges par défaut (`ALTER DEFAULT PRIVILEGES IN SCHEMA public`) qui accordent `EXECUTE` **directement** à `anon`/`authenticated` sur toute fonction nouvellement créée — pas seulement via `PUBLIC`. Le premier `REVOKE ... FROM public` de la migration ne suffisait donc pas après le `DROP`+`CREATE` ; il a fallu un second `REVOKE ... FROM anon, authenticated` explicite. Les deux revokes sont dans le fichier de migration final. | ✅ Mergé (2026-09-19). Vérifié en base (`has_function_privilege` false pour `anon`/`authenticated` sur les deux fonctions internes, aucune surcharge en double), par appel REST direct (permission denied confirmé), et par les trois chemins participant rejoués en direct sur une séance de test jetable créée puis purgée par SQL (`create_table` leaderless, `join_table`, `switch_table` — ce dernier confirmé retirer la ligne `participants` de l'ancienne table). `claim_table_as_moderator` **non rejoué** : nécessite le Code Ecclesia réel, que cette session n'a pas et ne devait pas chercher à obtenir ; son seul changement est le même motif (`leave_other_session_tables` à 2 arguments) déjà vérifié via `switch_table` — voir `A_VERIFIER.md` § Chantier 103 pour la recette restante | `supabase/migrations/20260919_chantier103_auth_uid_instead_of_param.sql` |
| 104 | Sécurité : restreindre ce qu'un modérateur peut réécrire sur `tables` (C7) | Lot 3 du [plan anti-interruption](./2026-09-19-plan-anti-interruption-seance.md), indépendant du 102/103. La policy RLS `tables_update_moderator` (chantier 60) autorise l'`UPDATE` dès `is_table_moderator(id)` sans restreindre aucune colonne, et le `GRANT UPDATE` à `anon`/`authenticated` portait sur les 11 colonnes de `tables` : un modérateur pouvait réécrire `join_code` (plus personne ne peut rejoindre la table) ou `session_id` (rattachement à une autre séance), sans passer par aucune RPC. `REVOKE UPDATE ON tables FROM anon, authenticated` puis `GRANT UPDATE (questionnaire_forced_at)` — seul `UPDATE` direct du frontend (`TableContext.tsx:538`/`:547`, forçage/annulation du questionnaire) ; parole en cours, `leaderless`, numéro de table et rattachement passent tous par des RPC `SECURITY DEFINER`, non affectées par un `GRANT` de colonne. La policy RLS ne change pas. Aucun changement de frontend — pas de nouveau conflit de fichiers avec une autre branche en cours. | ✅ Mergé sur `main` le 2026-09-19 (fast-forward `35fbe3e..163414c`). Vérifié en base par simulation fidèle de PostgREST (transaction `SET LOCAL ROLE authenticated` + `SET LOCAL request.jwt.claims` avec le `sub` d'un vrai modérateur de test, `ROLLBACK` systématique) : forçage/annulation du questionnaire réussissent, `UPDATE` direct sur `join_code`/`session_id` refusé (`42501 permission denied for table tables`), tour de parole complet (`grant_floor`/`end_turn`) inchangé. Non rejoué au clic (changement de grant pur, sans code frontend touché) — détail dans `A_VERIFIER.md` § Chantier 104 | `supabase/migrations/20260919_chantier104_restrict_tables_update_columns.sql` |
| 85 | Sauvegardes chiffrées quotidiennes de la base Supabase | Le projet Supabase tourne sur le plan gratuit, sans sauvegarde managée restaurable (constat F3 de l'audit du 02/09, le plus grave des 19). `.github/workflows/db-backup.yml` : dump quotidien du schéma `public` (`supabase db dump`, cron 05:00 UTC + déclenchement manuel), chiffré AES-256 (gpg) avant d'être stocké comme artefact GitHub Actions (rétention 30 jours) — chiffrement obligatoire car ce dépôt est public. Un job `verify-restore` s'exécute après chaque sauvegarde : télécharge l'archive du jour, la déchiffre, restaure le dump dans un Postgres jetable et vérifie la présence des tables attendues — la preuve que le dump est réellement restaurable, pas seulement produit. Procédure de restauration réelle documentée en tête du fichier. Trois correctifs après le premier run réel (2026-09-20) : `SUPABASE_DB_URL` doit utiliser le Session pooler (la connexion directe n'est joignable qu'en IPv6, absente des runners GitHub Actions), le Postgres jetable a besoin d'un schéma `auth` factice (`auth.uid()` référencé par plusieurs `DEFAULT` de colonnes), et doit être en version 17 pour matcher la version source du dump (le privilège `MAINTAIN`, ajouté en Postgres 17, fait échouer une restauration vers un 15). | ✅ Mergé sur `main` le 2026-09-20 (branche `chantier-secu-sauvegardes`, commit `b0a19ef`, corrigé par `25f2000`/`16818ae`) — **run réel vérifié de bout en bout** : `backup` et `verify-restore` tous deux verts. Reste à confirmer sur la durée : le déclenchement automatique du cron quotidien (05:00 UTC) sans intervention — voir `A_VERIFIER.md`. | `.github/workflows/db-backup.yml` |
| 99 | Boutons de guidage dans le menu Documentation (biais cognitifs, arguments fallacieux) | Consigne de Jules du 20/09 : pour chaque séance, le menu « Documentation » doit toujours proposer deux liens statiques vers le site de ressources externe (`ecclesia-centralesupelec.vercel.app/ressources#biais-cognitifs` et `#arguments-fallacieux`), en plus des liens propres à la séance (fiche info, résumé, sources collaboratives) qui restent conditionnés à leur présence en base. Trois endroits distincts affichent une section « Documentation » et ont chacun reçu les deux liens : `DocumentationButton.tsx` (menu modérateur, `ModeratorView`), la section Documentation de `ParticipantToolsButton.tsx` (panneau Outils en débat) et celle de `VoteToolsPanel` dans `VoteScreen.tsx` (panneau Outils en attente/vote). Les trois composants n'affichaient plus rien (ou un message « aucune documentation ») quand la séance n'avait aucun `doc_*_url` — retiré, la section est désormais toujours visible. `DocNudge` (encart non cliquable de suggestion pendant le vote) volontairement laissé de côté : ce n'est pas un menu « Documentation » cliquable, hors périmètre de la consigne. | ✅ Mergé le 2026-09-20. `tsc --noEmit` propre. **Vérifié au navigateur et confirmé par Jules** sur une séance de test jetable créée puis supprimée en base — seul le parcours `VoteToolsPanel` (`VoteScreen.tsx`) a été montré à l'écran, les deux autres emplacements non rejoués individuellement (même composant, détail dans `A_VERIFIER.md` § Chantier 99). | `src/components/DocumentationButton.tsx`, `src/components/ParticipantToolsButton.tsx`, `src/screens/VoteScreen.tsx` |
| 105bis | Sécurité : ferme l'auto-désignation libre de modérateur (A4) | Lot 4 (produit) du [plan anti-interruption](./2026-09-19-plan-anti-interruption-seance.md). `designate_moderator` ne demandait aucun secret : un participant assis sur une table `leaderless` se nommait modérateur, ouvrant `kick_participant`/`grant_floor`/`correct_turn`/`add_offline_participant`. **Arbitrage de Jules (20/09)** : le bouton participant reste visible mais renvoie désormais vers le superadmin (modale informative, plus d'appel RPC) ; seul le superadmin peut accorder le rôle, et seulement s'il est disponible — pas de filet automatique. `EXECUTE` sur `designate_moderator` révoqué pour `anon`/`authenticated` en base (défense en profondeur, pas seulement un changement côté client). Côté admin, aucune nouvelle RPC : `AddModeratorControl` (onglet Tables du superadmin, chantier 72) couvrait déjà ce besoin via `assign_moderator_to_table`/`set_member_moderator`. | ✅ Fait et **vérifié au navigateur** le 2026-09-20. `EXECUTE` vérifié en base avant/après (`information_schema.role_routine_grants` : anon/authenticated absents après migration, postgres/service_role conservés). `tsc --noEmit` propre. Testé en direct sur une table `leaderless` jetable : le bouton ouvre la modale de renvoi vers le superadmin sans appel réseau, `tables.leaderless` inchangé après clic, et un appel REST direct à la RPC échoue en `403 permission denied` (défense en profondeur confirmée, pas seulement un blocage côté client) — détail dans `A_VERIFIER.md` § Chantier 105bis. Chemin de recours superadmin (`AddModeratorControl`) non rejoué, déjà vérifié à d'autres chantiers et non modifié ici. | `supabase/migrations/20260920_chantier105bis_close_designate_moderator.sql`, `src/screens/ParticipantView.tsx`, `src/context/TableContext.tsx` |
| 105 | Sécurité : rétablir la restriction de colonne du chantier 51 sur `assertions` | Lot 4 du [plan anti-interruption](./2026-09-19-plan-anti-interruption-seance.md), suite du chantier 100. **Constat à l'ouverture, différent de ce que disait l'audit** : la restriction du chantier 51 (`REVOKE SELECT` + `GRANT SELECT` sur 5 colonnes, `member_id` exclu) **était déjà en vigueur en base** — vérifié via `information_schema.column_privileges`. La cause du soupçon initial (« grant rétabli hors migration ») n'a pas pu être confirmée : ni le dashboard Table Editor (Jules confirme n'y avoir pas touché), ni les chantiers 101-104 (vérifié : aucune de leurs migrations n'écrit sur `assertions`) ne l'expliquent. Piste retenue, non confirmée : les privilèges par défaut du schéma (`pg_default_acl`, posés à la création du projet) accordent `SELECT` sur toutes les colonnes de toute **nouvelle** table à `anon`/`authenticated` — si `assertions` était un jour recréée, ils écraseraient silencieusement la restriction, sans migration ni log dans le dépôt. Geste fait malgré tout : la restriction, qui ne reposait que sur un `GRANT` posé en base, est recodifiée dans une migration idempotente du dépôt. | ✅ Fait le 2026-09-20. Revérifié en base après application (`information_schema.column_privileges` + test réel en rôle `anon` : `select member_id from assertions` renvoie `42501 permission denied`). Cause non tranchée mais sans conséquence pratique — détail dans `A_VERIFIER.md` § Chantier 105. | `supabase/migrations/20260920_chantier105_restore_assertions_column_grant.sql` |
| 87+101 | Audit du parcours participant & modérateur (les deux chantiers fusionnés) | Le 87 (« revue complète des parcours utilisateurs ») attendait un texte de Jules ; ce texte est arrivé le 20/09 et couvre exactement le terrain du 101 (« toutes les situations d'arrivée d'un participant ou d'un modérateur sont-elles couvertes ? »). Les deux sont donc traités en **un seul audit**, ce qui lève le chevauchement que `chantiers-a-faire.md` demandait de clarifier avant de lancer l'un ou l'autre. Relecture phase par phase (`draft` → `closed`) du code contre la description de référence. **Verdict : globalement couvert**, les manques sont des incohérences de couverture plutôt que des trous. Trois problèmes principaux : (A) rien ne garantit **un seul écran modérateur par table** — deux membres `is_moderator` assis à la même table obtiennent tous deux `ModeratorView` **et** l'autorité SQL, alors que l'allocation assoit sciemment les modérateurs en surplus sans retirer leur flag (chantier 25b) ; (B) `claim_moderator_status` **réécrit l'affectation** de quelqu'un déjà assis sur une table animée, ce qui donne un écran modérateur **sans autorité** sur la table où il est physiquement ; (C) la déclaration modérateur est incohérente d'un point d'entrée à l'autre (ouverte à l'inscription en `allocating`, fermée aux déjà-inscrits sur le même écran par le chantier 95 ; absente du formulaire de secours en `debating` ; silencieusement perdue dans la reconquête pré-vote). Plus deux pièges transverses : perte de l'`auth.uid()` anonyme = perte du statut modérateur sans recours à l'écran, et absence de toute notion de « modérateur en exercice » dans le modèle de données. Six correctifs candidats (P1-P6) et trois arbitrages à rendre par Jules sont listés dans le document. | ✅ Audit livré (document seul, aucun code, aucune migration) | `docs/2026-09-20-audit-chantier87-101-parcours-participant.md` |
| 106 | Un seul écran modérateur par table (« déclaré » ≠ « en exercice », problème A de l'audit 87/101) | Fondation des chantiers 106-112. `is_table_moderator` accordait l'autorité SQL à *tout* membre `session_members.is_moderator` assis à la table — l'allocation assoit sciemment des modérateurs en surplus (chantier 25b) sans retirer leur drapeau, donnant deux `ModeratorView` sur la même file/le même chrono. Nouvelle colonne `tables.active_moderator_member_id` (nullable, FK `session_members`), exigée en plus des conditions actuelles par la branche (b) d'`is_table_moderator` (la branche (a), créateur physique, inchangée). Posée par le **premier** chemin qui attribue l'animation (arbitrage de Jules du 20/09 : « le modérateur est le premier arrivé ») — `COALESCE` dans les 5 chemins concernés (`apply_allocation`, `claim_moderator_status`, `set_member_moderator`, `assign_moderator_to_table`, `claim_table_as_moderator`), jamais d'écrasement. Vidée par `set_member_moderator` au retrait explicite du drapeau par le superadmin, si c'était bien ce membre qui était en exercice. `is_moderator` ne change pas de sémantique et n'est jamais retiré (demande explicite de Jules : le superadmin doit continuer à voir qui est modérateur « en plus »). `list_table_assignments_admin` étendue pour exposer le champ ; onglet Groupes du superadmin : nouveau badge ambre « Modérateur en surplus » distinct du badge indigo « Modérateur » (en exercice), et `AddModeratorControl` réapparaît dès qu'aucun modérateur n'est *actif* sur la table (même si un surplus y est assis). | ✅ Mergé le 2026-09-21. `tsc --noEmit` et `npx vitest run` (119 tests) propres. **Vérifié en base** (bypass RLS via rôle service, `auth.uid()` simulé) : `is_table_moderator` répond `true` pour le membre en exercice, `false` pour le surplus assis à la même table ; répond `false` pour les deux après retrait (`active_moderator_member_id = NULL`). **Vérifié au navigateur** (séance QA jetable, modérateur posé via `localStorage` + une ligne `participants`, sans mot de passe superadmin) : `ModeratorView` s'affiche quand `active_moderator_member_id` pointe vers le membre du navigateur de test, **`ParticipantView`** s'affiche pour le même utilisateur (toujours `is_moderator=true`) dès que `active_moderator_member_id` pointe vers un autre membre. **Non vérifié à l'écran** (pas de mot de passe superadmin pour cette session) : badges de l'onglet Groupes, modale « Table physique », `AddModeratorControl`, et une allocation réelle via `apply_allocation` (le geste testé a posé `active_moderator_member_id` directement en SQL) — détail complet dans `A_VERIFIER.md` § Chantier 106. | `supabase/migrations/20260921_chantier106_active_moderator_member_id.sql`, `src/context/TableContext.tsx`, `src/screens/SuperadminScreen.tsx` |
| 107 | `claim_moderator_status` ne doit jamais déplacer quelqu'un déjà assis (problème B de l'audit 87/101) | Si l'appelant était déjà assis sur une table **non** `leaderless`, la branche `ELSE` réécrivait sa `table_assignments` vers la première table animée sans modérateur — sa place physique (`participants`) ne bougeait pas, il obtenait l'écran modérateur d'une table où il n'était pas, et toutes ses actions d'animation échouaient en silence RLS sur la table où il siégeait réellement. **Décision de Jules (20/09, arbitrage 1)** : la déclaration doit toujours être possible mais ne doit jamais faire bouger la répartition que le superadmin regarde. Le placement d'office (recherche + réécriture de `table_assignments`) ne subsiste désormais que pour un appelant **sans aucune table** et **hors phase `allocating`** ; dans tous les autres cas (déjà assis ailleurs, ou phase `allocating`) la déclaration pose uniquement `session_members.is_moderator = true`, rien d'autre. La conversion en place d'une table `leaderless` où l'appelant est déjà assis (chantier 64) est conservée à l'identique — elle ne déplace personne. Neutralité au recalcul (précisée par Jules) : un modérateur déclaré pendant `allocating` **est bien recompté** si le superadmin relance le calcul — rien n'a été changé à `get_allocation_inputs`, qui relit les entrées fraîches ; la neutralité s'obtient uniquement en empêchant la déclaration de poser un siège. `set_member_moderator` (chantier 37, geste superadmin volontaire) n'est pas concerné, conformément au périmètre du chantier. | ✅ Mergé le 2026-09-21. Migration appliquée en base et vérifiée (`pg_get_functiondef` comparé avant/après). **Vérifié au navigateur réel** le 2026-09-21 (vrai Code Ecclesia fourni par Jules pour ce test, jamais stocké dans le dépôt) : le cas central de l'audit — un testeur déjà assis sur une table non `leaderless`, se déclarant via le vrai formulaire de reconquête `VotingEntryForm` — reste bien sur sa table (aucune bascule vers la première table libre sans modérateur, qui aurait été choisie par l'ancien code) ; et un nouveau membre se déclarant en phase `allocating` obtient le drapeau sans aucun placement. Détail complet et limites (recalcul d'allocation non rejoué, actions d'animation post-fix non rejouées une à une) dans `A_VERIFIER.md` § Chantier 107. | `supabase/migrations/20260921_chantier107_claim_moderator_no_displacement.sql` |
| 108 | Harmoniser la déclaration modérateur sur tous les points d'entrée (C1, C2, C3 de l'audit 87/101) | Trois écarts fermés **après** le 107 (la déclaration ne pose plus qu'un drapeau pendant `allocating`, l'ouvrir davantage ne risque donc plus de déplacer personne). **C1** : la reconquête pré-vote (`PseudoForm.tsx`, nom déjà pris → nom + code) ignorait silencieusement la case « Je suis modérateur » cochée juste au-dessus — `tryClaimModeratorStatus` est désormais rejouée après un reclaim réussi, comme le fait déjà `VotingEntryForm`, avec un écran de résultat qui distingue le libellé reclaim/inscription. **C2** : le message du chantier 95 sur `AllocatingScreen.tsx` (« les groupes ne peuvent plus être modifiés depuis cet écran ») est remplacé par un vrai formulaire de déclaration (case + Code Ecclesia, `tryClaimModeratorStatus`) quand le membre n'est pas encore modérateur, et par une note informative sinon. *Constat en cours de route, consigné dans `A_VERIFIER.md`* : `AllocatingScreen` ne se monte en pratique que si la phase a déjà atteint `debating` — le seul cas réel où `currentSession.phase === 'allocating'` s'y réaffiche est un retour en arrière de phase pendant qu'un participant a déjà cet écran ouvert (Realtime) ; le chemin « Outils → Me déclarer modérateur » (chantier 73) n'a lui jamais été fermé pendant `allocating`. Le fix suit le périmètre exact demandé. **C3** : le formulaire de secours de `TableAssignmentCard.tsx` (membre inscrit, aucune affectation, phase `debating`) ne proposait que `switch_table` — case modérateur ajoutée, câblée sur `claim_table_as_moderator` (chemin déjà présent quelques lignes plus haut dans le même composant). | ✅ Mergé le 2026-09-21. `tsc --noEmit` propre, `npm run build` réussi, `npx vitest run` : 119 tests passés / 4 skip pré-existants. **Vérifié au navigateur** sur une séance jetable créée et intégralement supprimée en base pour ce test (`C108TST`, 0 ligne restante après coup) : les trois formulaires (C1, C2, C3) déclenchent bien le bon appel RPC (`tryClaimModeratorStatus`/`claim_table_as_moderator`) et affichent correctement l'erreur serveur — testé avec un mot de passe **volontairement invalide**, faute du vrai Code Ecclesia pour cette session. Le succès RPC lui-même (passage effectif de `is_moderator` à `true`) reste à rejouer avec le vrai mot de passe — détail complet dans `A_VERIFIER.md` § Chantier 108. | `src/components/voting/PseudoForm.tsx`, `src/screens/AllocatingScreen.tsx`, `src/components/voting/TableAssignmentCard.tsx` |
| 109 | Placement des modérateurs en attente au passage en `debating` | Contrepartie du 107 : depuis ce chantier, `claim_moderator_status` ne pose plus qu'un drapeau pendant `allocating`, sans plus jamais placer personne — il fallait donc un geste explicite pour placer, au moment où le superadmin en a fini avec les tables, chaque modérateur resté « en attente » (`is_moderator=true`, pas en exercice — `active_moderator_member_id`, chantier 106). **Décision de Jules (20/09, arbitrage 1)** : « Lorsqu'il a fini de travailler sur les tables, et passent en débat, les modérateurs qui étaient en attente sont associés à leurs tables. » Nouvelle RPC `assign_pending_moderators(password, session_id, apply)` : place chaque modérateur en attente sur la première table animée sans modérateur en exercice, par `table_number` croissant, les modérateurs pris par ordre d'inscription. `apply=false` (dry-run, aucune écriture) sert de récapitulatif de confirmation exigé par Jules (« 3 modérateurs en attente vont être placés aux tables 2, 5 et 7 — confirmer ? », avec ce qui ne sera pas fait : modérateurs sans table libre, tables restées sans modérateur) ; `apply=true` rejoue le même calcul et écrit. Câblée dans `SuperadminScreen.tsx` sur le bouton « Ouvrir le débat » : la modale de confirmation calcule le dry-run à l'ouverture et n'applique qu'à la confirmation, juste avant le passage effectif en `debating`. Le surplus de modérateurs en attente (plus que de tables libres) reste participant avec son drapeau, sans traitement particulier (arbitrage 2, chantier 106) ; une table restée sans modérateur (pas assez de modérateurs en attente) est seulement signalée, pas corrigée (l'option « interdire les tables sans modérateur » du chantier 98 existe déjà en amont). | ✅ Mergé sur `main` le 2026-09-21 (fast-forward depuis `claude/chantier-109-browser-tests-b1f6d8`). Migration appliquée en base. `tsc --noEmit` propre, `npx vitest run` : 119 tests passés. **Vérifié en base** (transaction jetable `BEGIN…ROLLBACK`, garde `check_superadmin_password` temporairement retirée à l'intérieur de la même transaction) : dry-run et apply produisent le même calcul, apply écrit bien `table_assignments`/`active_moderator_member_id`, un second dry-run après apply est vide (idempotent), le surplus sans table libre est bien signalé sans être placé. **Non vérifié à l'écran** (pas de mot de passe superadmin pour cette session) : le récapitulatif affiché dans la modale « Ouvrir le débat » avec de vraies données, et que `ModeratorView` s'affiche bien chez le modérateur nouvellement placé après un vrai clic — détail complet dans `A_VERIFIER.md` § Chantier 109. | `supabase/migrations/20260921_chantier109_assign_pending_moderators.sql`, `src/lib/voting.ts`, `src/screens/SuperadminScreen.tsx`, `src/components/ConfirmModal.tsx` |
| 115 | « Mes binômes » : retiré du débat, remplacé par « Changer de table » | Dicté par Jules en chat (pas dans la file d'attente), en observant le menu Outils en débat : le rattachement automatique par binôme (chantier 92) n'a jamais fonctionné qu'un cas — un retardataire sans table rattaché à celle d'un binôme réciproque déjà assis, via `NOT EXISTS (... table_assignments ...)` — et ne servait strictement à rien pour qui était déjà assis (préférence enregistrée, jamais utilisée). **Décision de Jules** : plus simple de demander directement le code de table à la personne visée. `set_my_pairings` (SQL) resserré à la phase `voting` uniquement (avant, acceptait aussi `allocating`/`debating`) ; le bloc de rattachement automatique est supprimé, `placed_table_number` retourne désormais toujours `NULL`. Nouveau composant `ChangeTableModal.tsx` (calqué sur le bloc « code manuel » déjà écrit dans `TableChangeModal.tsx`, chantier 95) : un simple champ code + bouton « Rejoindre », visible dans `ParticipantToolsButton` (Outils, débat) uniquement — pas en `allocating`, où aucune table n'existe encore. « Mes binômes » retiré de `ParticipantToolsButton` et d'`AllocatingScreen` (l'unique porteur du mécanisme de rattachement retardataire) ; conservé et renommé **« Être avec un ami (facultatif) »** dans `VoteToolsPanel` (`VoteScreen.tsx`, resserré à la phase `voting` comme la RPC) et dans `OnboardingForm.tsx`/`PairingModal.tsx`, seuls endroits où il reste un usage réel (préférence pour le calcul d'allocation, avant que les tables n'existent). | ✅ Mergé le 2026-09-21. `tsc --noEmit` et `npm run build` propres. Migration appliquée en base (comparée à `pg_get_functiondef` avant application, corps identique au fichier de migration précédent). **Vérifié au navigateur** (séance QA jetable créée/purgée en base, `.env` copié depuis la racine, deux tables `debating`) : le bouton « Mes binômes » a bien disparu d'`AllocatingScreen`/`ParticipantToolsButton` ; « Changer de table » apparaît dans Outils en débat, saisie du code de la seconde table → `join_table` appelé, rechargement, le participant se retrouve bien sur la seconde table (`participants`/`table_assignments` confirmés en base, plus aucune trace sur la première). **Vérifié en base** : un appel direct à `set_my_pairings` sur la séance `debating` de test échoue avec le message d'erreur attendu (phase refusée). **Non vérifié à l'écran** : le bouton « Être avec un ami » conservé côté `VoteScreen`/`OnboardingForm` (texte inchangé de fond, seul le libellé change — pas rejoué, risque de régression jugé négligeable). | `supabase/migrations/20260921_chantier115_pairings_voting_only.sql`, `src/components/voting/ChangeTableModal.tsx`, `src/components/ParticipantToolsButton.tsx`, `src/screens/AllocatingScreen.tsx`, `src/screens/VoteScreen.tsx`, `src/components/voting/PairingModal.tsx`, `src/components/voting/OnboardingForm.tsx` |
| 112 | Le modérateur ne voit pas le questionnaire de fin de séance (C4 de l'audit 87/101) | `ParticipantView` a reçu au chantier 63 une garde `!forcedQOpen` sur son overlay « La séance est terminée », pour qu'il ne recouvre pas le questionnaire forcé à l'instant `debating → post_voting`. `ModeratorView` n'avait jamais reçu l'équivalent : l'overlay plein écran s'affichait par-dessus tout, y compris « Outils Modo », empêchant le modérateur d'ouvrir le questionnaire post-débat depuis sa propre table (il le retrouvait ensuite via « Voir les résultats », donc rien de définitivement perdu, mais l'ordre questionnaire → résultats → revote n'était pas respecté pour lui). `ModeratorView` n'ouvrant pas de modal automatique (contrairement à `ParticipantView`, où le modérateur force le questionnaire mais l'ouvre lui-même via le panneau), la garde ajoutée masque l'overlay pendant toute la fenêtre active de `table.questionnaire_forced_at` (1h) plutôt que pendant l'ouverture effective d'un modal — seul signal disponible côté `ModeratorView`. | ✅ Mergé le 2026-09-21. `tsc --noEmit` propre. **Vérifié au navigateur** sur une séance QA jetable créée directement en base (modérateur posé via `localStorage` + une ligne `participants`, sans mot de passe superadmin) : overlay masqué en `post_voting`/`closed` tant que le forçage est actif, "Outils Modo" → "Questionnaire post-débat" fonctionnel ; contre-épreuve (forçage retiré → overlay réapparaît, prouvant que `session.phase` était bien à jour côté client) ; fenêtre expirée (>1h) → overlay réapparaît. Détail complet dans `A_VERIFIER.md` § Chantier 112. | `src/screens/ModeratorView.tsx` |
| 110 | Bouton « Je suis le modérateur de cette table » dans les Outils + filet d'identité (piège D1 de l'audit 87/101) | Si le jeton anonyme est renouvelé en séance (veille longue, navigateur in-app Messenger, purge Safari ITP), `App.tsx` restaurait la table depuis `localStorage` puis reposait `isModerator = r.created_by === userId` — toujours faux (anti-pattern « Ne jamais faire » de CLAUDE.md : `created_by` d'une table issue de l'allocation est l'uid du superadmin, jamais celui du modérateur assis, et même hors allocation `r` vient de LA TABLE AVANT ce `join_table`). Aucun chemin ne rendait alors l'écran modérateur. **Décision de Jules (20/09, arbitrage 5)** : « il faut un bouton "je suis le modérateur de cette table" dans outils, pour reprendre la main si on a perdu le compte, c'est une très bonne idée, option A. » Nouvelle RPC `reclaim_table_as_moderator(table_id, creation_code)` : exige le Code Ecclesia, exige que l'appelant soit déjà assis à cette table précise (`is_table_participant`), puis transfère l'autorité **sans COALESCE** — seul chemin du projet qui déloge volontairement un titulaire déjà en exercice (`tables.created_by` et `active_moderator_member_id` écrasés d'un coup, jamais l'un sans l'autre, sinon la branche oubliée d'`is_table_moderator` laisserait l'ancien titulaire actif). **Tranché explicitement par Jules, cœur du chantier** : la reprise doit réussir même si la table a déjà un modérateur (potentiellement l'appelant lui-même sous une autre identité, ou passation de main à un tiers), sans se restreindre au seul cas « identité perdue » (indétectable côté serveur). Distincte de `claim_table_as_moderator` (chantier 68/72, pensée pour l'EXTÉRIEUR de la table par join_code+pseudo, refuse un tiers) : ce bouton n'est accessible que depuis l'intérieur d'une table déjà rejointe (`ParticipantToolsButton`, masqué si déjà modérateur), pas de join_code/pseudo à ressaisir. `App.tsx` : `isMod` figé à `false` après un `join_table` de reconquête (plus aucune comparaison `created_by === userId`) — le seul filet de rattrapage devient ce bouton. `TableContext.tsx` : toast informatif pour l'ancien titulaire quand il est délogé en direct (son `isModerator` retombe déjà tout seul via le canal Realtime `table:<id>` déjà ouvert). | ✅ Mergé le 2026-09-21. `tsc --noEmit` et `npm run build` propres. Migration appliquée en base. **Vérifié en base** (transaction jetable `BEGIN…ROLLBACK`, hash de Code Ecclesia de test posé et restauré à l'intérieur de la transaction) : un tiers non assis à la table est refusé ; un second participant déjà assis reprend l'animation malgré un premier modérateur déjà en exercice (`is_table_moderator` bascule de `true` à `false` pour l'ancien titulaire, de `false` à `true` pour le nouveau, `created_by` transféré). **Vérifié au navigateur réel** (séance QA jetable créée/purgée en base pour ce test, dev server lancé sur ce worktree, `.env` copié depuis la racine) : le bouton apparaît dans Outils pour un participant non-modérateur, la modale « Reprendre l'animation de cette table » s'ouvre avec le bon texte, la soumission avec un Code Ecclesia **volontairement invalide** déclenche bien l'appel réseau à `reclaim_table_as_moderator` et affiche « Code Ecclesia incorrect » — succès RPC avec le vrai Code Ecclesia non rejoué à l'écran (pas de mot de passe superadmin pour cette session), couvert par la vérification en base ci-dessus. Détail complet dans `A_VERIFIER.md` § Chantier 110. | `supabase/migrations/20260921_chantier110_reclaim_table_as_moderator.sql`, `src/components/ParticipantToolsButton.tsx`, `src/components/voting/ReclaimTableModeratorModal.tsx`, `src/lib/voting.ts`, `src/App.tsx`, `src/context/TableContext.tsx` |
| 111 | Retardataire par code de table : bouton « Assignez-moi une table » (écart C5 de l'audit 87/101) | **Constat fait EN BASE avant d'écrire une seule ligne de code** (transaction jetable `BEGIN`, `join_table` appelé pour un faux `auth.uid()` sur une table de séance, `ROLLBACK`) : l'écart C5 décrit par l'audit (« un retardataire qui rejoint uniquement une table n'a pas de ligne `session_members` ») est **déjà refermé depuis les chantiers 66/67** (2026-09-02/03, antérieurs à l'audit du 20/09) — `join_table` → `sync_table_assignment` inscrit déjà automatiquement en `session_members`/`table_assignments` quiconque rejoint une table par son code, même jamais passé par le vote. Ce qui manquait réellement, conformément à l'arbitrage 3 de Jules (« on peut aussi tout à fait lui donner un bouton : Assignez-moi une table ») : uniquement le bouton qui évite de devoir mendier un code à un voisin. Nouvelle RPC `assign_least_filled_table(session_id, pseudo)` (signature neuve, aucune collision) : inscrit l'appelant en `session_members` s'il ne l'est pas déjà — avec un vrai code de rappel (`gen_member_reclaim_code` + `reclaim_code_hash`, dupliquant volontairement le geste de `register_session_member` qui refuse pourtant la phase `debating`) — puis choisit la table **animée** (`leaderless = false`) la moins remplie en participants physiquement assis, égalité → plus petit `table_number`, déterministe (§6). Réutilise `leave_other_session_tables`/`sync_table_assignment` exactement comme `join_table`/`switch_table`. **Signatures de base vérifiées par `pg_get_functiondef` avant d'écrire la migration** : `sync_table_assignment`/`leave_other_session_tables` ont évolué en base au-delà des fichiers de migration existants (3/2 arguments avec `auth.uid()` implicite, plus les 4/3 arguments documentés) — la migration de ce chantier les appelle avec leur signature réelle. Bouton câblé à deux endroits, pour les deux profils distincts de l'arbitrage 3 : `SessionRouterScreen.tsx` (jamais inscrit à la séance, arrive via `#session/<code>`) et `TableAssignmentCard.tsx`/`AllocatingScreen.tsx` (déjà inscrit — voté ou rejoint pendant l'allocation — mais sans affectation, cul-de-sac du chantier 62). | ✅ Mergé le 2026-09-21. `tsc --noEmit`, `npm run build` et `npx vitest run` (119 tests) propres. Migration appliquée en base. **Vérifié en base** (transaction jetable `BEGIN…ROLLBACK`, deux fausses identités) : sur deux tables animées vides, la table de plus petit numéro est choisie (tie-break déterministe) ; une fois une table occupée par un premier participant, le retardataire suivant atterrit bien sur l'autre, la moins remplie — `session_members` créé avec un `reclaim_code_hash` non nul, `table_assignments` correct. **Vérifié au navigateur réel** (séance QA jetable créée/purgée en base pour ce test, dev server lancé sur ce worktree, `.env` copié depuis la racine) : depuis `#session/<code>` sur une séance `debating` sans être jamais inscrit, saisie du nom + clic « Assignez-moi une table » → placement immédiat dans `ParticipantView` de la table la moins remplie (`QANAVA`), sans jamais taper de code ; confirmé en base après coup (`session_members`/`table_assignments` corrects, code de rappel généré). **Non rejoué à l'écran** : le second point d'entrée (`TableAssignmentCard`/`AllocatingScreen`, membre déjà inscrit sans affectation) — partage exactement le même composant et la même RPC que le chemin vérifié ci-dessus, non rejoué séparément faute de scénario `debating`+`assignment=null` facile à mettre en scène sans une seconde identité de navigateur distincte (auth anonyme persistée par origine, un second onglet partage la même identité). Détail complet dans `A_VERIFIER.md` § Chantier 111. | `supabase/migrations/20260921_chantier111_assign_least_filled_table.sql`, `src/lib/voting.ts`, `src/screens/SessionRouterScreen.tsx`, `src/components/voting/TableAssignmentCard.tsx`, `src/screens/AllocatingScreen.tsx` |
| 116 | « Changer mon nom » : aussi faire réapparaître son code de rappel | Demande de Jules (2026-09-21) : « en plus de pouvoir changer de nom, on peut faire réapparaître son code pour ne pas l'oublier. » **Changement de périmètre découvert en cours de route, avant d'écrire une ligne de SQL** : la consigne suppose le code relisible, mais depuis le chantier 93 (20260918_chantier93_identite_participant.sql) `session_members.reclaim_code` (texte clair) a été remplacé par `reclaim_code_hash` (bcrypt) — vérifié en base (`information_schema.columns`), écart que ni CLAUDE.md ni `docs/reference-modele-donnees.md` ne reflétaient encore (les deux décrivent encore l'état pré-93). Un code bcrypt est irrécupérable par construction : seule option, en émettre un nouveau. Nouvelle RPC `regenerate_reclaim_code_self(session_id)` (signature neuve, calquée sur `regenerate_reclaim_code_admin`/`_moderator` déjà existantes — même corps, ciblée sur `auth.uid()` au lieu d'un `member_id`/pseudo fourni par un tiers). Bouton « Changer mon nom » renommé « Changer mon nom / code » aux deux endroits où il existe (`ParticipantToolsButton.tsx`, `VoteScreen.tsx`/`VoteToolsPanel`) — les deux ouvrent la même modale `RenamePseudoModal.tsx`, qui gagne un second bloc « Code de rappel oublié ? » sous le formulaire de renommage, avec avertissement explicite que l'ancien code cesse de fonctionner. Même commit : remplacement de l'icône générique « deux personnes » de l'en-tête `EntryScreen.tsx` (menu principal) par le logo Ecclesia fourni par Jules (`src/assets/ecclesia-logo.webp`) — pur changement visuel, non demandé comme un chantier numéroté mais fait dans la même session. | ✅ Fait le 2026-09-21, sur ce worktree. `tsc --noEmit` et `npm run build` propres. Migration appliquée en base. **Vérifié en base** : `crypt(ancien_code, reclaim_code_hash)` devient `false` après régénération, `crypt(nouveau_code, ...)` devient `true`. **Vérifié au navigateur réel** (séance QA jetable créée/purgée en base pour ce test, `.env` copié depuis un autre worktree) : inscription en `pre_voting`, ouverture d'Outils → « Changer mon nom / code », clic « Faire réapparaître mon code » → nouveau code affiché à l'écran, cohérent avec la vérification en base. Logo vérifié visuellement dans `EntryScreen` (menu principal) en même session. | `supabase/migrations/20260921_chantier116_regenerate_reclaim_code_self.sql`, `src/lib/voting.ts`, `src/components/voting/RenamePseudoModal.tsx`, `src/components/ParticipantToolsButton.tsx`, `src/screens/VoteScreen.tsx`, `src/screens/EntryScreen.tsx`, `src/assets/ecclesia-logo.webp` |
| 113 | Fusionner les deux boutons « modérateur » du panneau Outils | Demande de Jules (2026-09-21) : « Me déclarer modérateur » (chantier 73) et « Je suis le modérateur de cette table » (chantier 110), tous deux dans le panneau Outils de `ParticipantToolsButton.tsx`, avaient la même icône et des libellés quasi identiques sans qu'aucun n'explique en quoi il diffère de l'autre — un participant ne pouvait pas deviner lequel choisir, ni que le second est plus risqué (il évince un titulaire déjà en exercice). Ce ne sont pas des doublons : le premier (`claim_moderator_status`) pose le statut modérateur pour toute la séance sans jamais déloger personne (sauf s'il convertit sur-le-champ une table `leaderless` où l'appelant est déjà assis, chantier 64/107) ; le second (`reclaim_table_as_moderator`) reprend l'autorité physique sur la table où l'appelant est déjà assis, y compris en délogeant un titulaire actif — pensé pour le cas du jeton anonyme renouvelé en séance. Front pur, aucune RPC touchée : les deux entrées de menu sont fusionnées en une seule (« Modérateur »), qui ouvre une modale unique (`ModeratorActionModal.tsx`, nouveau) expliquant les deux actions avant de choisir, chacune gardant sa condition d'affichage d'origine (`table.session_id` pour la première, `!isModerator` pour la seconde). `ReclaimTableModeratorModal.tsx` supprimé (plus utilisé nulle part). `ModeratorClaimModal.tsx` conservé : `VoteScreen.tsx` (`VoteToolsPanel`, phase de vote, avant qu'aucune table de débat n'existe) l'utilise indépendamment — pas de doublon à ce stade-là, hors périmètre. Le bouton 3 (case « Je suis modérateur de cette table » dans `JoinTableForm.tsx`/`TableAssignmentCard.tsx`, chantier 68, pensé pour rejoindre une table libre par son code) est resté tel quel — Jules a confirmé qu'il n'y avait rien à y changer. | ✅ Mergé sur `main` le 2026-09-21 (fast-forward depuis `claude/moderator-buttons-duplicate-edfa9e`). `tsc --noEmit` propre. **Vérifié au navigateur** sur la séance de test partagée `C94A01` (nouveau participant simple, placé via « Assignez-moi une table ») : le panneau Outils n'affiche plus qu'un seul bouton « Modérateur » ; la modale présente bien les deux options avec leurs explications ; cliquer une option ouvre le bon formulaire, « Retour » revient au choix, « Annuler » ferme. RPC volontairement non soumises (séance partagée réelle, déjà animée par quelqu'un — pas de prise de risque sur ses données). **Reste à vérifier humainement, sur une séance jetable** : soumission réelle des deux actions avec le vrai Code Ecclesia, et les deux cas non testés faute de table autonome disponible (bouton limité à la seule option « reprendre » sur une table sans séance ; bouton entièrement masqué si déjà modérateur sur une table sans séance). Détail complet dans `A_VERIFIER.md` § Chantier 113. | `src/components/ParticipantToolsButton.tsx`, `src/components/voting/ModeratorActionModal.tsx`, `src/components/voting/ReclaimTableModeratorModal.tsx` (supprimé) |
| 114 | Onboarding : clarifier que « passif » n'est pas un engagement définitif | Demande de Jules (2026-09-21) : « il faut vraiment bien marqué que passif, c'est qu'on ne prévoit pas de parler, même si on peut toujours faire le choix inverse au cours du débat. Pour que les participants comprennent bien cela. » Changement de texte pur, aucune logique touchée : la question « Comment comptes-tu participer ? » (`OnboardingForm.tsx`, `QuestionStyle`) gagne une phrase d'introduction (« Ce n'est pas définitif : tu pourras toujours prendre la parole en cours de débat, même si tu choisis « Plutôt écouter ». ») et un sous-libellé sous chaque bouton (« Je ne prévois pas de parler » / « Je compte prendre la parole »), pour que le choix se lise comme une intention de départ et non un engagement bloquant. Aucun champ de données touché — `participationStyle` reste `'listener' | 'active'`, la sémantique et le stockage (`entry_responses.participation_style`, chantier 91) sont inchangés. | ✅ Mergé sur `main` le 2026-09-21 (fast-forward `a0dd004..3bb95fc`). `tsc --noEmit` propre. **Non vérifié au navigateur** : ce worktree n'a pas de `.env` Supabase (`supabaseUrl is required`), et l'écran d'onboarding n'est atteignable qu'après un parcours de vote complet (inscription + phase `voting`/`pre_voting`) — hors portée d'une vérification rapide pour un changement de texte pur. Consigné dans `A_VERIFIER.md` § Chantier 114. | `src/components/voting/OnboardingForm.tsx` |
| 117 | Vue Groupes : afficher un modérateur « physique » invisible du superadmin | Constat de Jules (2026-09-21), retrouvé en base (MCP Supabase) sur la vraie séance « Réunion apprentissage modération 21/09 » : un participant assis comme modérateur (`tables.created_by` = son `user_id`, `leaderless = false`, `ModeratorView` normal chez lui) n'apparaissait jamais dans l'onglet Groupes du superadmin, reload ou pas — aucune latence Realtime en cause. Cause : ce modérateur a rejoint la table directement par Code Ecclesia (`claim_table_as_moderator`) sans jamais s'inscrire à la séance, donc aucune ligne `session_members` ; `list_table_assignments_admin` (chantier 50/106) ne lit que `table_assignments` × `session_members`. C'est le « défaut A » déjà documenté dans `20260906_chantier72_1_reprise_moderation.sql`, jamais corrigé côté affichage (seulement côté RPC de libération/reprise). Correctif : `list_table_assignments_admin` gagne une branche `UNION ALL` émettant une ligne synthétique (`member_id = NULL`, `is_moderator = true`) pour tout modérateur physique sans aucune ligne `session_members` — aucune autorité SQL touchée. Côté front, `member_id` devient `string \| null` ; le bouton « Retirer » sur cette ligne appelle `handleReleaseTableModeration` (au niveau table) plutôt que `handleRemoveTableModerator` (qui exige un `member_id`) ; les tableaux d'entrée de l'algorithme (`groupDiagnostics`, `brokenGroupClusters`) filtrent ce `member_id` null pour ne pas fausser les seuils de taille de table. | ✅ Mergé sur `main` le 2026-09-21 (merge de `claude/moderator-display-superadmin-bd561f`, `78cf873`). `tsc --noEmit` et `npm run build` propres. Migration appliquée en base et **vérifiée en base** (requête directe reproduisant le corps de la fonction) : la séance réelle de Jules renvoie bien la ligne synthétique attendue. **Non vérifié au navigateur** : l'onglet Groupes exige le mot de passe superadmin, non détenu par cette session — à confirmer par Jules directement sur l'application déployée. Détail et recette de vérification humaine dans `A_VERIFIER.md` § Chantier 117. | `supabase/migrations/20260921_chantier117_list_table_assignments_admin_physical_moderator.sql`, `src/lib/sessions.ts`, `src/screens/SuperadminScreen.tsx` |
| 118 | Modérateur « physique » retiré devient un session_member normal, flagué modérateur | Suite du chantier 117 (correctif d'affichage) et du chantier 119 (créait déjà, via `sync_table_assignment`, la ligne `session_members`/`table_assignments` + code de rappel dès `claim_table_as_moderator`, sans jamais poser `is_moderator = true`). Retour de Jules après test du 117 : « le but, c'est que quand ils ne tiennent plus la modération d'une table, ils soient comme des participants normaux, avec des codes, des votes, etc, mais juste flagué modérateur ». `claim_table_as_moderator` flague désormais `is_moderator = true` + pose `active_moderator_member_id` sur la ligne créée par `sync_table_assignment` (« modérateur en exercice » Bloc C complet, chantier 106). `release_table_moderation` change de philosophie sur demande explicite de Jules : ne retire plus `session_members.is_moderator` (avant : `false`) — démet seulement `active_moderator_member_id` (perd l'écran, garde le drapeau — « modérateur en surplus »), et rattrape à la volée (pseudo + code de rappel neuf + `table_assignments`) les modérateurs physiques encore orphelins de `session_members` (lignes créées avant le chantier 119). `table_has_moderator` (chantier 68) avait la même lacune que `is_table_moderator` avant le chantier 106 : sa branche (b) ne vérifiait pas `active_moderator_member_id` — corrigée, sinon un modérateur resté flagué après retrait aurait bloqué toute reprise de sa table. Le hack d'affichage du chantier 117 (`UNION ALL`) n'est pas retiré : reste nécessaire pour les modérateurs physiques réclamés avant le chantier 119, tant qu'ils n'ont pas encore été libérés une fois. | ✅ Fait le 2026-09-21, branche `claude/chantier-118-ff6067`. `tsc --noEmit`, `npm run build` et `npx vitest run` (119 tests) propres. Migration appliquée en base. **Vérifié en base** par trois transactions jetables (`BEGIN...ROLLBACK`) : claim pose bien `is_moderator=true`/`active_moderator_member_id` ; release les démet correctement (garde le flag, vide l'exercice, table reprenable par un tiers) ; le rattrapage d'un orphelin pré-119 (aucune ligne `session_members`) crée bien la ligne flaguée + le code de rappel + `table_assignments`. **Non vérifié au navigateur** (pas de mot de passe superadmin ni de vrai Code Ecclesia pour cette session) — recette complète dans `A_VERIFIER.md` § Chantier 118. | `supabase/migrations/20260921_chantier118_physical_moderator_becomes_member.sql`, `src/lib/voting.ts` |
| 119 | Angles morts du code de rappel (inscriptions sans code) — ⚠️ **recoupait le chantier 118 (fait ci-dessus le même jour)** : ce chantier-ci implémente, pour `claim_table_as_moderator`, exactement l'« Option B » que le 118 complète (inscription `session_members`/`table_assignments` + code dès la prise de table, plutôt qu'à la sortie) | Jules a constaté à l'écran qu'un débatteur rejoignant une séance en `debating` par un simple code de table n'avait jamais reçu son code de rappel, et a demandé un audit complet plutôt qu'un correctif isolé. Audit exhaustif en base (`select proname from pg_proc ... where prosrc ilike '%insert into session_members%'` — les 6 fonctions qui créent une ligne `session_members`, jamais les fichiers de migration) : trois n'avaient jamais généré de `reclaim_code`. **1.** `sync_table_assignment` (appelée par `join_table`/`switch_table`/`create_table` — donc tout « rejoindre une table par son code ») créait la ligne `session_members` avec `reclaim_code_hash` NULL. **2.** `claim_table_as_moderator` (chantier 68, rattrapage modérateur sur une table sans modérateur) n'appelait même pas `sync_table_assignment` : ni ligne `session_members`/`table_assignments`, ni code — même cause racine que le chantier 117 ci-dessus (constatée indépendamment par les deux sessions le même jour), mais traitée ici à la source (inscription réelle) plutôt qu'en aval (ligne synthétique à l'affichage) ; les deux corrections sont complémentaires, aucun conflit SQL (fonctions distinctes). **3.** `add_offline_participant` (outil modérateur « ajouter une personne sans téléphone ») créait la ligne avec une identité fictive (`gen_random_uuid()`) et sans code — cette personne ne pouvait donc **jamais** réclamer son profil, même plus tard depuis son propre appareil. Fix : `sync_table_assignment` génère désormais un code à la création (comme `assign_least_filled_table`, chantier 111) et change de type de retour (`void` → `jsonb`) pour le remonter ; `join_table`/`switch_table`/`create_table` le fusionnent dans leur résultat sous `new_reclaim_code` (absent/`null` si le membre existait déjà — cas de tout changement de table en cours de séance) ; `claim_table_as_moderator` appelle désormais `sync_table_assignment`, corrigeant à la fois l'absence de code et l'absence d'inscription ; `add_offline_participant` génère un code retourné au modérateur, à transmettre oralement/par écrit (la personne pourra ensuite réclamer son profil via `confirm_attendance`, pseudo + code, depuis son propre appareil). Front : `JoinTableForm.tsx` affiche `ReclaimCodeDisplay` (composant déjà utilisé par `VoteScreen`, désormais avec un libellé de bouton contextualisable) avant de continuer, quand `new_reclaim_code` est présent ; `ModeratorToolsButton.tsx` affiche de la même façon le code retourné par `add_offline_participant`. **Piège rencontré en appliquant la migration** : le simple `DROP FUNCTION`+`CREATE` de `sync_table_assignment` a réactivé les privilèges par défaut de Supabase (regrant explicite `EXECUTE` à `anon`/`authenticated` sur toute fonction recréée), qu'un `REVOKE ... FROM PUBLIC` seul ne retire pas (grants explicites, pas hérités) — repéré en relisant `pg_proc.proacl` après application, corrigé en nommant `anon, authenticated` explicitement dans le `REVOKE`. | ✅ Mergé sur `main` le 2026-09-21. `tsc --noEmit`, `npm run build` et `npx vitest run` (119 tests) propres. Migration appliquée en base, ACL vérifiée par `pg_proc.proacl` après coup (identique à `leave_other_session_tables`). **Vérifié en base** (transaction jetable `BEGIN…ROLLBACK`, quatre identités fictives) : `join_table`/`claim_table_as_moderator`/`add_offline_participant` retournent chacun `new_reclaim_code` et créent `session_members` avec un hash non nul ; `switch_table` par un membre déjà inscrit ne retourne **pas** ce champ (aucune régression sur le cas nominal). **Vérifié au navigateur réel** (séance QA jetable créée/purgée en base pour ce test, dev server lancé sur ce worktree, `.env` déjà présent) : depuis `#table/<code>` (lien d'ami), saisie du nom → écran « Note ton code de rappel » → code affiché → clic « Rejoindre la table → » → arrivée dans `ParticipantView` ; code affiché confirmé identique au hash stocké en base. **Non rejoué à l'écran** : les chemins `claim_table_as_moderator` et `add_offline_participant` (couverts par la vérification en base, scénarios plus longs à mettre en scène pour un test navigateur — vrai Code Ecclesia et modérateur physique déjà assis respectivement). **Hors périmètre, découvert en cours de route** : `App.tsx` (restauration après renouvellement du jeton anonyme) semble pouvoir désynchroniser durablement `session_members.user_id` de l'identité réelle de l'appareil, indépendamment du code de rappel — non confirmé, non traité ici, chantier 120 proposé pour l'investiguer. Détail complet dans `A_VERIFIER.md` § Chantier 119. | `supabase/migrations/20260921_chantier119_reclaim_code_angles_morts.sql`, `src/lib/supabase.ts`, `src/components/JoinTableForm.tsx`, `src/components/voting/ReclaimCodeDisplay.tsx`, `src/components/ModeratorToolsButton.tsx` |
| 120 | Désynchronisation de `session_members.user_id` au renouvellement du jeton anonyme | Découvert en aparté au chantier 119, **confirmé par repro en base** (transaction jetable) : un membre `session_members` existant, appelé via `sync_table_assignment` sous un `auth.uid()` renouvelé mais le **même pseudo**, ne récupérait **aucune** ligne pour la nouvelle identité — `INSERT` échouant en silence sur `UNIQUE(session_id, pseudo)` (avalé par le `EXCEPTION WHEN OTHERS` du chantier 111), l'ancienne ligne restant orpheline sous l'ancien `user_id`. **Jules a tranché (option B, 2026-09-21)** : « on redemande le code, car le modo peut lui redonner aussi son code au cas où, et le superadmin aussi ». Correctif : `sync_table_assignment` renvoie désormais `{"reconnect_required": true}` au lieu de tenter l'`INSERT` silencieusement voué à l'échec (ne crée/modifie plus rien dans ce cas) ; `join_table` remonte en plus `session_id` (nécessaire côté client) ; `App.tsx` bascule vers un nouvel écran (`src/components/ReconnectPrompt.tsx`, pseudo + code de rappel) au lieu d'entrer silencieusement dans `TableView` ; à la confirmation (`confirmAttendance`, RPC du chantier 93), rappelle `join_table` qui réussit alors normalement. Migration en `CREATE OR REPLACE` sans `DROP` (ACL vérifiée inchangée après coup). | ✅ Mergé sur `main` le 2026-09-21. `tsc --noEmit`, `npm run build` et `npx vitest run` (107 tests, un seul échec préexistant sans rapport — `.env` manquant côté vitest) propres. **Vérifié en base** (transaction jetable, table temporaire de log) : jeton renouvelé → `reconnect_required: true`, rien ne bouge ; mauvais code → erreur, rien ne bouge ; bon code → `user_id` réassigné proprement, `sync_table_assignment` rejoué ensuite réussit normalement, aucun doublon. **Vérifié au navigateur réel** (séance/table/membre QA créés et purgés en base, dev server sur ce worktree) : cas nominal (même identité) inchangé, jeton renouvelé simulé (suppression du seul token d'auth, `ecclesia_table` intact) → écran « Reconnexion nécessaire » affiché, mauvais code refusé avec message clair, bon code → retour direct dans `TableView`, `session_members.user_id`/`participants.user_id` alignés en base après coup. Détail complet dans `A_VERIFIER.md` § Chantier 120. | `supabase/migrations/20260921_chantier120_reconnexion_explicite_token_renouvele.sql`, `src/App.tsx`, `src/components/ReconnectPrompt.tsx`, `src/lib/supabase.ts` |
| 121 | Petites corrections UI : œil mot de passe, popup assertion, actifs par table | Trois retours de Jules, `docs/chantiers-a-faire.md` § 121. **(1)** Composant partagé `PasswordInput.tsx` (bouton œil affichage/masquage), appliqué aux 8 champs mot de passe de l'app (1 seul, dans `SuperadminScreen.tsx`, l'avait déjà) : `JoinTableForm.tsx`, `TestScreen.tsx`, `ModeratorActionModal.tsx`, `ModeratorClaimModal.tsx`, `ModeratorDeclareField.tsx`, `TableAssignmentCard.tsx` (×2). **(2)** Le popup "Proposer une assertion" (`VoteScreen.tsx`) réapparaissait à chaque reload dès 10 votes dépassés car son seuil (`nextNudgeAt`) repartait toujours de 10 au montage, sans persistance. Fix : un `useRef` empêche toute réinitialisation après le premier appel de `loadVoteData` (point d'entrée unique de tout chargement de vote), qui calcule désormais le seuil de départ à partir des votes déjà posés au lieu de le fixer à 10. Le bouton "✏️ Proposer" déjà présent en permanence dans l'en-tête sert de moyen manuel (aucun nouveau bouton nécessaire). **(3)** Onglet Groupes du superadmin (`SuperadminScreen.tsx`) : badge "N actif(s)" ajouté à côté du total de membres par table, en réutilisant `memberProfiles` (`is_active`, déjà chargé pour le badge lettré par membre) — aucune nouvelle donnée. | ✅ Mergé. `tsc --noEmit` propre. **Vérifié au navigateur réel** pour le point (1) uniquement (toggle œil testé sur le mot de passe superadmin et le Code Ecclesia de `JoinTableForm`) — les 6 autres champs mot de passe, le nudge assertion (2) et le badge actifs (3) n'ont **pas** été rejoués à l'écran (nécessitent respectivement une séance de vote à 10+ votes avec reload, et une allocation calculée). Détail dans `A_VERIFIER.md` § Chantier 121. | `src/components/PasswordInput.tsx`, `src/screens/VoteScreen.tsx`, `src/screens/SuperadminScreen.tsx` |
| 122 | Liens docs par phase + binôme visible à l'écran d'allocation | `docs/chantiers-a-faire.md` § 122. **(1)** Les liens fixes « Biais cognitifs » / « Arguments fallacieux » (déjà présents dans le panneau Outils, `ParticipantToolsButton.tsx`) sont désormais répétés sous "📄 Profites-en pour lire la documentation" sur les écrans pré-vote/vote et sur l'écran d'allocation — composant `DocNudge` (auparavant privé à `VoteScreen.tsx`) extrait en `src/components/voting/DocNudge.tsx` et réutilisé par `AllocatingScreen.tsx`, plutôt que dupliqué. **(2)** Binôme visible à l'écran d'allocation (`AllocatingScreen.tsx`, « Ton groupe ») : nouvelle carte « Ton binôme » au-dessus du nudge documentaire, lecture seule (aucune possibilité de modifier son choix depuis cet écran, comme demandé), réutilisant `getMyPairings()` (RPC `get_my_pairings`, chantier 92) et le composant d'affichage déjà existant `PairingResultsList` (`PairingModal.tsx`) — pas de nouveau composant d'affichage créé. ⚠️ **Précision de routage découverte en testant** : contrairement à ce que suggère son nom, `AllocatingScreen.tsx` n'est monté pour le participant qu'au passage en phase `debating` (`VoteScreen.tsx` reste sur l'écran de vote, avec la bannière ambre, tant que la séance est en phase `allocating` — voir le commentaire « on reste sur le vote avec bannière », ligne ~225). C'est donc bien le bon fichier (c'est l'écran où le participant voit effectivement « Ton groupe » et son binôme), mais le nom de la phase DB (`allocating`) et celui du composant ne coïncident pas avec le moment réel de son affichage — à garder en tête si un futur chantier touche à ce routage. | ✅ Mergé. `tsc --noEmit`, `npm run build`, `npx vitest run` (119 tests) propres. **Vérifié au navigateur réel** (séance QA jetable créée/purgée en base sur ce worktree, `.env` copié depuis la racine) : écran de vote avec 1 assertion votée → les 5 liens (Fiche information, Résumé, Sources collaboratives, Biais cognitifs, Arguments fallacieux) s'affichent sous le nudge documentaire ; passage en `debating` avec `table_assignments` posé + un binôme réciproque (`member_pairings` croisé) → carte « Ton groupe » (table 1) + carte « Ton binôme » (« 🔗 QA Binome122 : vous vous êtes cités tous les deux, vous serez ensemble ») + les mêmes 5 liens documentaires, tous corrects. Le cas non réciproque (« ⏳ en attente ») et le cas séance sans aucun binôme déclaré (carte absente) n'ont pas été rejoués à l'écran — couverts par `PairingResultsList`, déjà utilisé ailleurs sans modification de sa logique. | `src/components/voting/DocNudge.tsx`, `src/screens/VoteScreen.tsx`, `src/screens/AllocatingScreen.tsx` |
| 123 | Diagnostic commun : binôme séparé de son modérateur + promotion modérateur par DnD | **Diagnostic demandé par Jules avant tout correctif.** Racine commune trouvée : « qui anime quoi » est décrit par **quatre faits dénormalisés** (`session_members.is_moderator`, `tables.active_moderator_member_id`, `tables.leaderless`, la ligne `table_assignments`), et chaque chemin qui déplace ou désigne quelqu'un n'en met à jour qu'une partie. **(Bug 2, DnD → « modérateur en surplus »)** ⚠️ **Cause principale trouvée seulement à la vérification navigateur** : le `DndContext` de l'onglet Groupes n'avait **aucun `collisionDetection`**, donc dnd-kit appliquait `rectIntersection`, qui classe les cibles par **aire d'intersection** — `add-moderator-<n>` (la petite boîte ambre) est imbriquée dans `group-<n>` (toute la carte), le parent gagnait **toujours**, et la zone « désigner un modérateur » était **inatteignable au glisser-déposer** : le drop partait en `move_member_to_group`, qui ne promeut personne. Sur quelqu'un déjà flagué `is_moderator`, un simple déplacement vers une table sans animateur produit exactement l'affichage « modérateur en surplus » rapporté. Corrigé par `pointerWithin` (même stratégie que le DnD de `ModeratorView`, cf. CLAUDE.md § DnD), avec `add-moderator-*` forcé en tête des collisions. Deux causes SQL **cumulatives** existaient en plus, confirmées et corrigées elles aussi : `assign_moderator_to_table` posait `active_moderator_member_id = COALESCE(active_moderator_member_id, …)` : une valeur **périmée** n'était jamais remplacée. Et c'était bien une valeur périmée, car **aucune** des fonctions qui déplacent un siège (`move_member_to_group` — le DnD lui-même —, `sync_table_assignment` via `switch_table`, et `assign_moderator_to_table`) ne nettoyait `active_moderator_member_id` de la table **quittée** — **confirmé en production** : 2 tables portaient une valeur périmée au moment du diagnostic. Deuxième chemin vers le même symptôme : `SELECT DISTINCT table_id … LIMIT 1` sur `table_assignments` renvoie NULL sur une table **vide** (créée à la main, chantier 95), ce qui sautait **en silence** tout le bloc d'animation. Troisième : `table_assignments.table_id` est **nullable** (vérifié en base) et la requête n'avait ni filtre `IS NOT NULL` ni `ORDER BY` — le DISTINCT pouvait attraper le NULL, de façon non déterministe (**erreur analogue trouvée dans `move_member_to_group`**, seule autre occurrence sur les 60+ fonctions en base). **(Bug 1, binôme séparé)** `src/lib/allocation.ts` plaçait les modérateurs animants par **simple ordre d'index**, hors du solveur (`animatingIds.forEach((mid, idx) => …)`) : un modérateur qui anime n'entre jamais dans `assign[]`, donc `clusterOfId` ne le concerne pas et son binôme est placé sans lui, **structurellement** — un modérateur en *surplus*, lui, repasse par le pool (`seatProfile`) et son appairage était déjà respecté, ce qui explique que le recalcul suivant « le remettait ». Aggravant : `countBrokenClusters` lit `member_ids`, qui n'inclut pas les modérateurs → la grappe cassée n'était comptée **nulle part**. **Correctifs.** SQL : helpers `has_effective_moderator` (l'animateur enregistré est-il encore flagué ET assis ici ?) et `release_moderator_on_leave` (libérer la table quittée), appelés par les trois chemins ; `assign_moderator_to_table` résout la table via `tables` et non `table_assignments`, lève une erreur explicite au lieu d'un no-op silencieux, et **écrase** un animateur non effectif ; nouvelle RPC `set_table_leaderless` (chemin de sortie symétrique, demandé par Jules). TS : l'**ordre** d'affectation des modérateurs aux tables animées est choisi pour maximiser les grappes tenues (glouton stable, aucun `Math.random()` — invariant §6), puis une **réparation par échange à effectif constant** tire le binôme vers la table de son modérateur (option (a) tranchée par Jules ; la règle 1 prime sur l'hétérogénéité, chantier 92) ; `countBrokenClusters` inclut désormais les modérateurs. UI : bouton « En faire le principal » sur le badge de modérateur en surplus, et bouton « Refaire une table sans animateur ». **Déjà conforme, rien à faire** : un modérateur en surplus n'obtient **pas** l'écran modérateur (chantier 106 — `TableContext` et `is_table_moderator` comparent tous deux à `active_moderator_member_id`). | 🟡 Livré, **non mergé**. **Vérifié au navigateur réel** (séance QA jetable créée en base puis supprimée, mot de passe superadmin saisi par Jules lui-même dans le panneau, jamais manipulé par la session) : DnD vers la zone modérateur → désignation en **principal** (échouait avant le correctif de collision) ; « En faire le principal » sur un surplus → promotion + sortie de `leaderless`, et le bouton disparaît dès qu'un principal existe ; « Refaire une table sans animateur » → retour du badge, drapeau `is_moderator` conservé ; aller-retour sortie→reprise concluant ; animateur **fantôme** d'une table quittée nettoyé automatiquement. Également **vérifié en base** sur les 6 scénarios RPC (transaction jetable, `check_superadmin_password` neutralisée dans la seule transaction puis restaurée — empreinte `pg_get_functiondef` revérifiée identique après coup, aucune ligne QA laissée). Migration **appliquée en base** (comparaison préalable à `pg_get_functiondef` pour les 3 fonctions réécrites, conformément à la garde de CLAUDE.md). `tsc --noEmit`, `npm run build` et `npx vitest run` propres (**79 tests**, dont 3 nouveaux sur le binôme d'un modérateur animant — **vérifiés « mordants »** : 2 des 3 échouent sur le code d'avant le correctif). **Parcours binôme également joué à l'écran** (20 membres, 2 modérateurs, 3 binômes réciproques dont 2 avec un modérateur ; « Calculer » puis « Appliquer » depuis `AllocationPanel`) : bandeau « 2/2 conformes · 🔗 3/3 grappes réunies », chaque binôme de modérateur assis à la table qu'anime son modérateur. Séances QA supprimées ; les 2 animateurs fantômes de la « Séance test de bout en bout » ont été remis à `NULL` sur demande de Jules (0 fantôme restant, toutes séances confondues). Reste non joué : la non-régression de l'allocation sur une population réaliste — voir `A_VERIFIER.md` § Chantier 123. | `supabase/migrations/20260922_chantier123_moderation_effective.sql`, `src/lib/allocation.ts`, `src/screens/SuperadminScreen.tsx`, `src/lib/voting.ts` |
| 124 | Verrouiller la proposition d'assertions (superadmin only) | Demande directe de Jules : pouvoir interdire à tout le monde, sauf le superadmin, de proposer de nouvelles assertions, via un bouton. Même patron que les chantiers 46 (`results_public`) et 71 (`onboarding_enabled`) : nouvelle colonne `sessions.assertions_locked` (défaut `false`) + RPC `set_session_assertions_locked` protégée par mot de passe superadmin. `submit_assertion` (réécrit à l'identique de sa définition vivante en base, comparée via `pg_get_functiondef` avant réécriture — pas au fichier de migration d'origine) refuse désormais toute proposition si `assertions_locked = true`. Le superadmin n'ayant lui-même aucun chemin d'appel vers `submit_assertion` (il n'est membre d'aucune séance), « sauf superadmin » se traduit simplement par : lui seul peut activer/désactiver le verrou. Bouton toggle ajouté dans `SuperadminScreen.tsx` (`SessionCard`), même forme visuelle que les deux toggles existants (pastille rouge quand verrouillé). `SubmitAssertionModal.tsx` affiche un message dédié et masque le formulaire côté client quand `session.assertions_locked` est vrai — la vraie protection reste côté SQL. | ✅ Mergé sur `main` le 2026-09-22. Migration appliquée en base (colonne + les deux fonctions, comparaison `pg_get_functiondef` faite avant réécriture de `submit_assertion`, vérifiée identique au fichier de migration d'origine hormis le nouveau verrou). `tsc --noEmit` et `npm run build` propres. **Vérifié en base et au navigateur réel** (séance QA jetable créée puis supprimée) : verrou actif → `submit_assertion` refuse (HTTP 400, message exact) et le modal participant affiche « Propositions désactivées » après rechargement complet de la page ; verrou levé → comportement normal restauré (HTTP 200, assertion créée) ; mauvais mot de passe superadmin rejeté par `set_session_assertions_locked`. Seul le bouton toggle du panneau superadmin lui-même n'a pas été rejoué à l'écran (pas de mot de passe superadmin en session headless) — voir `A_VERIFIER.md` § Chantier 124. | `supabase/migrations/20260922_chantier124_verrouiller_assertions.sql`, `src/lib/types.ts`, `src/lib/sessions.ts`, `src/screens/SuperadminScreen.tsx`, `src/components/voting/SubmitAssertionModal.tsx` |
| 125 | Accordéon « J'ai déjà un code de rappel » sur les écrans d'entrée | Demande directe de Jules (hors file d'attente) : sur `VotingEntryForm` (phases `voting`/`allocating`, `VoteScreen.tsx`), le champ code de rappel était toujours visible à côté du pseudo, comme un second champ ambigu (« pseudo OU code »). Masqué derrière une case à cocher « J'ai déjà un code de rappel », qui se coche automatiquement (+ focus sur le champ) si le pseudo saisi est déjà pris — `PSEUDO_TAKEN_MESSAGE` inchangé. `PseudoForm.tsx` (phase `pre_voting`) gagne un lien symétrique « J'ai déjà un code de rappel → » sous le formulaire, qui bascule directement vers l'écran de reconquête pseudo+code déjà existant (`showReclaim`), sans attendre l'échec — ce chemin de reconquête existait déjà depuis le chantier 93/B3, seul l'accès proactif est nouveau. Aucun changement pour `AttendanceConfirmScreen` (mode reclaim) ni `ReconnectPrompt` : déjà des écrans dédiés atteints par clic explicite, pas d'ambiguïté pseudo/code à lever. | ✅ Mergé sur `main` le 2026-09-22. `tsc --noEmit` propre, aucun changement SQL. **Vérifié au navigateur réel** (deux séances QA jetables créées/purgées via le MCP Supabase, `session_members` insérées directement) : `VotingEntryForm` — champ code masqué par défaut, apparaît à la coche, redevient masqué après un vrai rechargement (`location.reload()`) ; pseudo déjà pris → case cochée automatiquement, focus confirmé (`document.activeElement`), reconnexion réussie avec le bon code. `PseudoForm` — lien visible dès l'arrivée, bascule vers l'écran de reconquête, reconnexion réussie avec pseudo+code corrects sans jamais passer par un refus. | `src/screens/VoteScreen.tsx`, `src/components/voting/PseudoForm.tsx` |
| 126 | Diagnostic et nettoyage du GitHub | Consigne de Jules du 25/09 : « beaucoup de documents sont apparemment en doubles, certains sont pt obsolète, etc. », scope libre sur tout le dépôt, droit de supprimer directement les doublons évidents sans attendre de validation. Constats : (1) trois plans/spec `docs/superpowers/` (trajectoires EWMA par scoring de bloc, déjà implémentées dans `analyze_debate.py`) avaient été ajoutés par erreur à la racine `docs/superpowers/` au lieu de `transcription-debat/docs/superpowers/`, seul emplacement documenté par `reference-transcription.md` — déplacés, contenu inchangé. (2) `vite.config.js`/`vite.config.d.ts` étaient committés à la racine, doublons compilés strictement identiques à `vite.config.ts` (probablement une sortie `tsc` jamais nettoyée) — supprimés, `vite.config.ts` intact. (3) 52 branches distantes `origin/*` entièrement fusionnées dans `main` (aucun contenu perdu, les commits restent accessibles via `main`) et sans worktree local actif dessus — supprimées sur GitHub après confirmation explicite de Jules. **Laissé en l'état, jugement à trancher plus tard** : `AUDIT.md`/`PROJECT_REVIEW_BRIEF.md` (snapshots explicitement datés du 17/07/2026, 65 migrations à l'époque contre 141 aujourd'hui) et `PARCOURS_SEANCE1.md`/`PARCOURS_VERIFICATION.md` (parcours de test manuel antérieurs au Browser pane automatisé) — non référencés par l'index vivant de `CLAUDE.md` mais pas des doublons stricts. `scripts/cleanup-worktrees.sh` existait déjà (jamais exécuté) pour le nettoyage local des ~65 worktrees obsolètes (~7,5 Go) ; doit être lancé depuis la racine du dépôt sur `main`, pas depuis un worktree — délégué à une session dédiée. | ✅ Nettoyage GitHub effectué et poussé sur `claude/chantier-126-a39daf` le 2026-09-25 (confirmation explicite de Jules pour les suppressions de branches et de `vite.config.js`/`.d.ts`). Purement documentaire/hygiène de dépôt, aucun changement fonctionnel — pas de recette `A_VERIFIER.md` nécessaire. | `docs/superpowers/*` (déplacés), `vite.config.js`, `vite.config.d.ts`, branches distantes `origin/*` |
| 127 | Bug : reset vers `allocating` laissait des participants bloqués sur leur ancienne table | Consigne de Jules : « Quand on est rentré dans une table avec débat, et que le superadmin remets allocation, certaines personnes, même en reloadant, restent sur leurs tables, plutôt que de revoir la vue vote. » Cause : `App.tsx` (fonction `init()`) restaure une table depuis `localStorage` (`tableStore`) dès que la ligne `participants` existe encore et que son `user_id` correspond à l'`auth.uid()` courant, **sans jamais revérifier la phase de la séance** — un reset superadmin `debating → allocating` ne supprime ni la table ni la ligne `participants`. `VoteScreen`/`AllocatingScreen` gèrent pourtant déjà correctement les retours en arrière de phase, mais seulement une fois montés : ce chemin de restauration directe les court-circuite entièrement. Correctif : avant de restaurer en phase `table`, si la table a un `session_id`, relire la séance (`getSessionById`) et ne restaurer directement que si elle est encore en `debating` ; sinon vider `tableStore` et rediriger vers `#vote/<join_code de la SÉANCE>` (piège rencontré en testant : `tableStore.joinCode` est celui de la **table**, pas de la séance — la première version du correctif s'y était trompée). Table sans `session_id` (hors Bloc C) non concernée. Développé en parallèle du chantier 129 (même fonction `init()` d'`App.tsx`, fusion sans conflit réel — les deux gardes sont indépendantes et complémentaires). | ✅ Livré. `tsc --noEmit`, `npm test` (122 passants) et `npm run build` propres. **Vérifié au navigateur réel, deux fois** (avant et après la fusion avec le chantier 129, séances QA jetables créées puis supprimées directement en base via le MCP Supabase, `localStorage.ecclesia_table` posé à la main, aucun mot de passe superadmin saisi) : en `debating`, reload → `TableView` (comportement de base inchangé) ; séance repassée en `allocating` en base, reload → `VoteScreen` avec bannière d'allocation, plus d'accès à l'ancienne table. **Reste ouvert, non demandé par la consigne** : un participant déjà affiché dans `TableView` au moment précis du reset (sans reload) n'est pas notifié en direct — `TableContext` ne s'abonne à aucun changement Realtime sur `sessions.phase`. | `src/App.tsx` |
| 128 | Bug : modérateur affiché en double sur une même table (vue superadmin) | Consigne de Jules : « il y avait deux fois la vision "Maxence Reinaudo" comme modérateur sur la table, deux carrés côte à côte. » **Diagnostic confirmé en base** (pas une hypothèse — deux occurrences réelles trouvées et utilisées pour vérifier le correctif) : un modérateur déjà « en exercice » Bloc C (`session_members` + `tables.active_moderator_member_id`) réclame ensuite la MÊME table par Code Ecclesia sous une **nouvelle identité anonyme** (`auth.uid()` renouvelé — nouvel appareil, `localStorage` vidé), en retapant le même pseudo. `active_moderator_member_id = COALESCE(…)` (chantier 106/118) ne bascule jamais sur cette nouvelle identité, et `list_table_assignments_admin` (branche `UNION ALL`, chantier 117) l'affiche comme un second modérateur, car son `NOT EXISTS` ne testait que `user_id`, pas le pseudo. Correctif : le `NOT EXISTS` de la branche `UNION ALL` exclut désormais aussi un pseudo déjà porté par une ligne `session_members` de la séance (`UNIQUE(session_id, pseudo)` garantit l'unicité) — purement un correctif d'affichage, aucune fonction d'autorité touchée. Risque résiduel assumé et documenté : un participant non inscrit qui partage par coïncidence le pseudo d'un membre `session_members` distinct serait aussi masqué — jugé plus rare et moins gênant qu'un doublon visible de la même personne. | ✅ Migration appliquée en base (`pg_get_functiondef` comparé avant réécriture, corps identique au chantier 117). **Vérifié directement en base** sur les deux occurrences réelles (séances `76de0462…` « Maxence reinaudo » et `5fcdcb3a…` « Antoine ») : une seule ligne par table désormais, avec le `member_id` réel ; non-régression vérifiée sur le cas d'origine du 117 (modérateur physique sans aucune ligne `session_members`, ni par `user_id` ni par pseudo). **Non vérifié au navigateur** (mot de passe superadmin non disponible en session headless) — voir `A_VERIFIER.md` § Chantier 128. | `supabase/migrations/20260925_chantier128_dedupe_physical_moderator_by_pseudo.sql` |
| 130 | Historique des tables accessible au superadmin après clôture | Consigne de Jules du 25/09 : « lorsqu'une session est clôturée, j'aimerai toujours, en tant que superadmin, avoir accès à l'historique des tables […], sans rendre publique à nouveau la séance. » **Aucune nouvelle RPC écrite** : en préparant la migration prévue (`get_session_table_history_admin`), découverte que `get_table_participants` et `get_table_speaking_turns_admin` (SECURITY DEFINER, mot de passe superadmin, aucune restriction de phase) existent déjà en base et sont déjà appelées par `SuperadminScreen.tsx` pour l'export CSV « Historique » (`handleExportHistory`, onglet Préparation) — mais **ni documentées dans `docs/reference-fonctions-sql.md`, ni exposées comme vue consultable dans l'app** (CSV uniquement, à télécharger et ouvrir ailleurs). Écrire une nouvelle RPC aurait dupliqué une capacité déjà livrée ; la migration rédigée a été appliquée sur dev puis annulée (`DROP FUNCTION`) dès la découverte. Correctif retenu, purement frontend : nouvelle section accordéon « Historique des tables » dans l'onglet Tables du superadmin, visible **uniquement en phase `closed`** (le point d'entrée UI est ce qui limite l'accès à après-clôture — les RPC elles-mêmes n'ont pas de garde de phase, comme `list_table_assignments_admin`) ; réutilise `attachedTables` (déjà chargé par `load()`) et charge à la demande, table par table dépliée, `getTableParticipants`/`getTableSpeakingTurnsAdmin` (nouveau composant `TableHistoryRow` : participants triés par temps de parole total, puis déroulé chronologique des tours avec source et durée). `get_public_results` n'est pas touché, reste le seul chemin d'accès public (gated `phase='closed'`). | ✅ Fait le 2026-09-25. `tsc --noEmit` propre, aucun changement SQL livré. **Vérifié en base** (données de test jetables insérées/purgées sur le projet Supabase **dev**, premier chantier à utiliser le nouvel environnement dev/prod) : la requête sous-jacente de `get_table_participants` (rejouée sans le mot de passe, rôle postgres) produit exactement la forme attendue par `TableHistoryRow` (tri par `total_ms` décroissant, agrégation tours/durée correcte sur 2 participants/3 tours de test). **Non vérifié au clic dans le navigateur** : mot de passe superadmin non disponible en session headless (même limite que les chantiers 118/119/121/128) — impossible de dérouler le clic « Historique des tables » → dépliage d'une table en conditions réelles. À rejouer par Jules ou une session disposant du mot de passe : séance close réelle, onglet 🪑 Tables → accordéon « Historique des tables » → déplier une table → vérifier participants/tours affichés. Voir `A_VERIFIER.md` § Chantier 130. | `src/screens/SuperadminScreen.tsx` |
| 129 | Bug : identité inter-séances — lien QR d'une nouvelle séance ramenait sur l'ancienne | Consigne de Jules du 25/09 : « des personnes ont participé à une séance précédente, et maintenant, quand je leur donne le QR code d'une nouvelle séance, ils reviennent sur le questionnaire de la séance précédente ». **Cause trouvée, hors des clés `localStorage` scopées par `session_id` suspectées initialement** (`hasQuestionnaireResponse` et les clés de `VoteScreen.tsx`/`SessionRouterScreen.tsx` sont toutes correctement filtrées par séance — pas le bug). La vraie cause est `tableStore` (`src/lib/storage.ts`, clé `ecclesia_table`) : une clé **globale à l'appareil, jamais scopée par séance**, qui retient la dernière table de débat rejointe, toutes séances confondues. `App.tsx.init()` la restaurait **sans jamais regarder le hash de l'URL courante** — un lien `#session/<nouveau_code>` fraîchement scanné était donc systématiquement ignoré dès que l'appareil avait déjà une table stockée : `phase` passait directement à `{type:'table', tableId: ANCIEN}`, et les gardes `phase.type !== 'table'` de toutes les routes hash (`#session/`, `#vote/`, `#table/`) empêchaient alors `SessionRouterScreen`/`VoteScreen` de s'afficher pour la nouvelle séance — le participant retombait dans l'ancienne `TableView` (et son éventuel questionnaire forcé resté ouvert). Fix : `init()` n'effectue plus la restauration automatique quand le hash au chargement correspond à une route de navigation explicite (`#session/`, `#vote/`, `#table/`, `#collab/`, `#results/`, `#superadmin` — liste `KNOWN_HASH_PREFIXES`, déjà utilisée par la détection de hash inconnu du chantier 88, désormais partagée) ; un lien explicite l'emporte toujours. La reprise en cours de débat (reload à hash vide, cas nominal après `handleTableJoined`) est inchangée : `tableStore` reste restauré normalement dans ce cas. | ✅ Fait le 2026-09-25, branche `claude/chantier-129-e55feb`. `tsc --noEmit` propre, aucun changement SQL. **Vérifié au navigateur réel** (deux séances QA jetables créées/purgées via le MCP Supabase — `sessions`/`tables`/`participants` insérés puis supprimés directement, mêmes `user_id` que l'auth anonyme du navigateur de test) : `ecclesia_table` pointé sur la table d'une séance A `closed` → navigation vers `#session/<code_B>` d'une séance B `pre_voting` fraîche → écran d'identification de la séance B affiché correctement (« QA129 Nouvelle séance », formulaire pseudo), pas de retour vers l'ancienne table. Rechargement à hash vide ensuite → restauration normale dans l'ancienne `TableView` (« QA129 Ancienne séance »), confirmant que le cas nominal n'est pas régressé. Séances/table/participant QA supprimés après coup (0 ligne restante). | `src/App.tsx` |
| 134 | Trois types de séance : séance complète / débat simple / sondage (usage interne) | Consigne de Jules du 25/09, arbitrée avec lui le 26/09 (note de conception et arbitrages : [`docs/chantier-134-conception.md`](./chantier-134-conception.md)). **Pas de nouvelle machine à états** : `sessions.session_type` (`full`/`debate`/`poll`, fixé à la création) et chaque type emprunte un sous-ensemble ordonné des phases existantes — débat simple `draft → debating → closed`, sondage `draft → pre_voting → closed`. Garde serveur (`session_type_allows_phase` dans `set_session_phase`) + miroir front (`phaseSequenceFor`, `lib/phaseLabels.ts`). **Débat simple** : `create_session` crée la table unique ; nouvelle RPC `join_simple_debate` (inscription + place à table + prise de modération avec Code Ecclesia, atomique) et écran `DebateEntryForm` (nom, « Je suis le modérateur », « J'ai déjà un code de rappel ») ; questionnaire de fin à la clôture puis « Séance terminée ». Déplacement de la modération : outils existants (superadmin onglet Tables, Outils → chantier 110). **Sondage** : `pre_voting` sans annonce « jour J », sans déclaration modérateur ni questionnaire post-débat ; bouton « Voir les résultats et les camps en direct » (`ResultsMapScreen` avec `onBack`, décompte des votes toujours visible) ; `get_results_map` ouvert en `pre_voting` pour un sondage ; `results_public = true` à la création → consultable par tous à la clôture. **Superadmin** : choix du type dans la modale, barre de phases et onglets filtrés par type, badge dans la liste. **Règle transverse ajoutée à `CLAUDE.md`** (§ Types de séance) : toute évolution des séances complètes se questionne pour les deux autres types. **Constat hors périmètre, non corrigé** : `get_results_map` n'ouvre la carte qu'en `closed` — dans une séance complète en `post_voting`, `ResultsMapScreen` affiche donc « carte pas encore disponible », contrairement à ce que décrit le chantier 89. | 🟡 Livré, **pas mergé** — migrations appliquées sur **dev uniquement**. Vérifié en base (transactions jetables) et au navigateur réel sur dev pour tout le parcours participant des deux nouveaux types ; parties superadmin et Code Ecclesia réel à vérifier par Jules (`A_VERIFIER.md` § Chantier 134). | `supabase/migrations/20260926_chantier134a_modes_de_seance.sql`, `supabase/migrations/20260926_chantier134b_sondage.sql`, `src/lib/types.ts`, `src/lib/phaseLabels.ts`, `src/lib/sessions.ts`, `src/lib/voting.ts`, `src/components/PhaseIndicator.tsx`, `src/components/DebateEntryForm.tsx`, `src/components/voting/PseudoForm.tsx`, `src/components/voting/RenamePseudoModal.tsx`, `src/components/voting/SessionQuestionnaireForm.tsx`, `src/screens/SuperadminScreen.tsx`, `src/screens/SessionRouterScreen.tsx`, `src/screens/VoteScreen.tsx`, `src/screens/ResultsMapScreen.tsx`, `src/screens/ParticipantView.tsx`, `CLAUDE.md` |
| 131 | Bouton "sujet suivant" + tag de sujet à la prise de parole | Consigne de Jules (`docs/chantiers-a-faire.md`) : un bouton par participant pour signaler son accord à passer au sujet suivant, avec compteur visible du modérateur ; et un tag de sujet optionnel saisi en demandant la parole, affiché à côté du prénom dans la file. **Deux ajouts indépendants, aucun nouveau canal Realtime** : `participants.wants_next_topic` (boolean) et `queue_entries.topic_tag` (text, ≤60 car.) sont déjà répliqués par le canal privé `table:<id>` existant (abonnement `event:'*'` sur ces deux tables, chantier 59) — ajouter une colonne suffit, `can_join_realtime_topic` n'a rien à changer. `participants` n'ayant **aucune policy UPDATE** (seulement INSERT/SELECT), le toggle passe par une nouvelle RPC self-service `set_next_topic_vote(p_participant_id, p_value)` (vérifie `auth.uid()` = propriétaire de la ligne, même patron que `add_to_queue`) ; `reset_next_topic_votes(p_table_id)` (modérateur only, `is_table_moderator`) remet tout le monde à `false`. **Choix tranché ici, faute de notion de « sujet courant » dans le modèle de données** : la remise à zéro du compteur est **manuelle** (bouton « Réinitialiser » dans le nouveau composant `NextTopicPanel.tsx`, vue modérateur), pas automatique — il n'existe rien à « passer » côté serveur qui puisse déclencher un reset tout seul. Le tag de sujet est ajouté comme 5ᵉ paramètre optionnel de `add_to_queue` (`p_topic_tag`, trim + `NULLIF('')`) : le nombre d'arguments changeant, la fonction existante a été `DROP FUNCTION IF EXISTS` puis recréée avec regrant explicite (`anon, authenticated`), pour éviter la surcharge ambiguë déjà rencontrée deux fois sur ce projet (cf. CLAUDE.md § Règle SQL). Restreint côté UI au bouton "Demander la parole" (file longue) uniquement — pas à "Coupe file" — en cohérence avec la formulation de Jules (« bouton prendre la parole ») et le nom de la file (« File d'attente »). | 🟡 Livré, **pas mergé** — migration appliquée sur **dev uniquement** (`mnjqrlrrzrycuconlfqb`), reste à appliquer sur prod au merge vers `main`. `tsc --noEmit` et `npm run build` propres. **Vérifié en base sur dev** (transaction jetable `BEGIN…ROLLBACK`, `auth.uid()` simulé via `set_config('request.jwt.claims', …)`) : tag trimmé et stocké correctement, tag blanc → `NULL`, contrainte de longueur (61 car.) refusée, self-toggle accepté, usurpation d'un autre participant refusée, reset par un non-modérateur refusé, reset par le modérateur effectif ramène bien `wants_next_topic` à `false` pour tout le monde. **Non vérifié au navigateur** : deux lacunes d'environnement **découvertes en préparant ce test, sans rapport avec ce chantier**, bloquent toute session anonyme réelle sur le projet Supabase **dev** — (1) le fournisseur d'authentification anonyme y est **désactivé** (`anonymous_provider_disabled`, contrairement à prod) ; (2) **aucune table `public` n'a le moindre GRANT pour `anon`/`authenticated`** (0 ligne dans `information_schema.role_table_grants` pour ces deux rôles, contre 274 sur prod) — la copie du schéma du 25/09 a reproduit RLS et droits d'exécution des fonctions (chantiers 102/103) mais pas les GRANT au niveau table, donc *aucun* accès `.from(...)` normal ne fonctionne sur dev pour l'instant, tous chantiers confondus. Signalé séparément (voir `A_VERIFIER.md`) plutôt que corrigé ici : périmètre bien plus large que ce chantier et risque de mal recopier la matrice de droits de prod sans validation. | `supabase/migrations/20260926_chantier131_sujet_suivant_et_tag.sql`, `src/lib/types.ts`, `src/context/TableContext.tsx`, `src/screens/ParticipantView.tsx`, `src/screens/ModeratorView.tsx`, `src/components/NextTopicPanel.tsx`, `src/components/QueuePanel.tsx`, `src/components/ReadOnlyQueuePanel.tsx` |

---

## Notes de méthode et cas particuliers

- **Chantiers 43 et 44** : développés et mergés sur la même branche (`chantier-43-outils-modo-transcription`), avec des commits distincts. Ni l'un ni l'autre n'a « absorbé » l'autre — vérifié dans `git log` : deux commits séparés (`11bf27a` pour le 43, `5235948` pour le 44), un seul merge commit sur `main`.
- **Chantiers 55, 56, 75** : recherchés sur **toutes** les branches du dépôt (`git log --all --grep`), pas seulement `main`. Aucune trace de commit dédié pour aucun des trois — confirmé, pas juste absent de `main`.
- **Chantier 58** est un cas intermédiaire réel, pas une erreur de classement : une partie de son contenu (les 4 RPC de lecture) est en base de production alors que la branche qui les contient n'est pas mergée sur `main`, et l'autre partie (le `REVOKE`) n'est appliquée nulle part. Les trois états (mergé / en base seulement / nulle part) coexistent pour un même chantier — voir la ligne du tableau pour le détail exact.
- **Chantier 74** est le seul cas où le code n'existe même pas sur une branche `origin` portant son nom — seulement dans un worktree local. À récupérer directement depuis ce dossier (`git fetch <chemin du worktree> chantier-74-phases-et-hors-ligne`) plutôt que depuis `origin`, tant que personne n'aura réussi à le pousser.
- Les numéros **1 à 33** existent (`chantier-1-navigation` à `chantier-33-gestion-moderateurs-tables`, tous mergés) mais sont hors du périmètre demandé pour ce document.

---

## Annexe A — Chantiers 1 à 25 : tâches lettrées

> **Repris tel quel de `PROJECT_STATUS.md`, supprimé au chantier 78 (2026-09-07).** Contenu **déplacé, pas résumé**.
>
> Pourquoi le garder : le corps du tableau principal ci-dessus couvre les chantiers 34 à 77 et exclut délibérément la numérotation lettrée antérieure. Or `CLAUDE.md`, `A_VERIFIER.md` et de nombreux en-têtes de migration désignent encore des tâches par leur lettre (« chantier 18 / F24 », « D7 », « H26 », « chantier 19 / G3 »…). Ces tables sont la **seule** correspondance entre ces 83 identifiants et les chantiers numérotés 1 à 25. Sans elles, ces renvois deviennent illisibles.
>
> ⚠️ Les statuts ci-dessous datent de l'été 2026 et n'ont pas été revérifiés : les lire comme un historique, pas comme un état courant. L'état courant est le tableau principal de ce document et `docs/registre-merges-en-attente.md`.

## Chantier 1 — Navigation partout
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| C5 | Bouton « Quitter » dans toutes les phases | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D3 | Messages de reload en phase d'attente | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D5 | Message d'intro sur le fonctionnement de l'app | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D9 | Infos sur la phase « allocating » | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |

## Chantier 2 — Questionnaire & identité avant allocation
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| D18 | Question modérateur oui/non | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D4 | Renommer « pseudo » → « nom prénom » | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D7 | Préremplir le nom/pseudo | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |

## Chantier 3 — Débat sans admin
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| D1 | Lecture rapide des règles à l'entrée de table | Backlog | | Chantier 1 |
| D2 | Désignation d'un admin en cours de débat | Fait — migration `designate_moderator` appliquée en base (rapporté 22/07, à vérifier fonctionnellement — voir A_VERIFIER.md) | Claude | Chantier 1 |
| F11 | Lien table sans admin ↔ séance d'origine | Bug trouvé et corrigé (chantier 14) — voir section dédiée ci-dessous | Claude | Chantier 3 |

## Chantier 4 — Rejoindre en cours de séance
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| D14 | Rejoindre le débat en retard, quelle que soit la phase | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D8 | Rejoindre un ami via code distribué | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |

## Chantier 5 — Algo d'allocation & modérateurs
> ⚠️ **La livraison de juillet (B1/B2/E4) est remplacée par le chantier 19** (`docs/chantier-5-allocation-v2-spec.md`). `run_clustering_v3` et `get_moderator_responses` sont supprimées ; `run_clustering_v1`/`v2` sont conservées le temps de valider l'algo v2 en prod.

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| B1 | Refonte algo d'allocation + questionnaire | **Remplacé par le chantier 19** (`run_clustering_v3` supprimée) | Claude | Chantier 2 |
| B2 | Assignation des modérateurs | **Remplacé par le chantier 19** (règle 5 + `session_members.is_moderator`) | Claude | Chantier 2 |
| E4 | Vue superadmin : retour des réponses modérateur | **Annulé** — `moderator_pref` supprimée, panneau retiré (remplacé par le tableau de bord d'allocation) | Claude | Chantier 2 |

## Chantier 19 — Algorithme d'allocation v2 (Vague 3)
> Spec normative : `docs/chantier-5-allocation-v2-spec.md` · amendements : `docs/VAGUE3-amendements-allocation.md`

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| G1 | Algorithme v2 — 5 règles en ordre lexicographique | Fait — `src/lib/allocation.ts` + 41 tests vitest (`npm test`). **À vérifier sur données réelles** — voir A_VERIFIER.md | Claude | — |
| G2 | RPC `create_tables_batch` (N tables vides en lot) | Fait — migration `20260725_2` **appliquée et vérifiée en base** | Claude | — |
| G3 | Onboarding 6 → 3 questions | Fait — migration `20260725_1` **appliquée** (3 colonnes supprimées ; `ecclesia_experience` → booléen, conversion vérifiée : 18 true / 6 false sur 24 lignes) | Claude | — |
| G4 | Signal « modérateur pour cette séance » | Fait — `session_members.is_moderator` **en base** + `set_member_moderator` / `claim_moderator_status` vérifiées. UI minimale (onglet Participants). **Flow UI complet = chantier 21** | Claude | — |
| G5 | Dépréciation `run_clustering_v3` / `get_moderator_responses` / panneau E4 | Fait — migration `20260725_3` **appliquée** (absence des 2 fonctions confirmée via `pg_proc`) | Claude | G1 |

## Chantier 20 — Tableaux de bord allocation (Vague 3)
> Dépend du chantier 19. Amendements : `docs/VAGUE3-amendements-allocation.md` (section « Vue modérateur — conscience idéologique » / « Vue superadmin — représentation des tables »).

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| G6 | Vue superadmin — composition par camp/actifs/enregistrable visibles directement sur chaque carte + état "à jour" (Realtime) | Fait — `npx tsc -b`/`npm run build` OK. **À vérifier fonctionnellement** — voir A_VERIFIER.md | Claude | Chantier 19 |
| G7 | Vue modérateur — composition idéologique de sa table + assertions représentatives/clivantes/consensuelles de sa table | Fait — migration `20260727_1_chantier20_table_opinion_summary.sql` **non appliquée** (MCP Supabase indisponible cette session). **À vérifier fonctionnellement** — voir A_VERIFIER.md | Claude | Chantier 19 |

## Chantier 21 — Flow d'entrée modérateur & refonte menu participant (Vague 3)
> Amendements : `docs/VAGUE3-amendements-allocation.md` § « Processus modérateurs » et § « Fusion rejoindre / reprendre »

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| G8 | Onglet « Modérateur » (remplace « Voter ») | Fait — `EntryScreen.tsx`, appelle `claimModeratorStatus`. **À vérifier en conditions réelles** (mot de passe superadmin requis) — voir A_VERIFIER.md | Claude | G4 |
| G9 | Fallback superadmin marquage modérateur | Déjà fait au chantier 19 (`MembersPanel`, onglet Participants) — vérifié suffisant, non modifié | Claude | G4 |
| G10 | Fusion onglets Rejoindre/Reprendre + « Je suis modérateur » | Fait — `EntryScreen.tsx`, onglet unique avec révélation inline du mot de passe | Claude | — |
| G11 | Renommer l'onglet fusionné | Fait — « Rejoindre ou reprendre une table » | Claude | G10 |

⚠️ Correction associée (hors périmètre G8-G11 mais indispensable) : `AllocatingScreen.handleJoin` codait `isModerator` en dur à `false` — corrigé pour lire `member.is_moderator`, sans quoi un modérateur assigné par l'allocation v2 (chantier 19) rejoignait sa table comme participant ordinaire.

## Chantier 22 — Petits ajustements UX liés (Vague 3)
> Amendements : `docs/VAGUE3-amendements-allocation.md` § « Phase pre_voting — message d'annonce », § « Table leaderless — message de clarification », § « Suppression des notions de durée par phase »

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| G12 | Pop-up unique pre_voting (remplace la bannière inline) | Fait — `PreVotingAnnounceModal` dans `VoteScreen.tsx`, mémorisée en localStorage. **À vérifier fonctionnellement** (nécessite une séance en `pre_voting`) — voir A_VERIFIER.md | Claude | — |
| G13 | Message de clarification « devenir animateur » (table leaderless) | Fait — texte de confirmation enrichi dans `ParticipantView.tsx` | Claude | — |
| G14 | Suppression des timers de phase | Fait — `vote_timer_minutes`/`vote_threshold_percent` retirés du type `Session`, de `update_session_config`, et de toute l'UI superadmin (VotingStatsPanel, alertes timer/seuil, formulaire de création). Migration `20260727_2_chantier22_remove_vote_timers.sql` **non appliquée** (MCP Supabase indisponible) | Claude | — |

⚠️ Développé en worktree dédié (`chantier-22-ajustements-ux` → `C:\Users\jules\projet\Ecclesia-chantier-22`) après avoir détecté que le dossier de travail partagé contenait déjà le travail non commité de la session concurrente du chantier 11 (QR codes déplacés vers Outils/Outils Modo) — collision résolue par `git stash`/restauration sur `main` sans perte, puis isolation dans un worktree. Voir A_VERIFIER.md pour le détail.

## Chantier 24 — Flow d'entrée modérateur : entrée & repérage (suite du chantier 21)
> Retours de test de Jules sur le chantier 21.

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| H3 | Case à cocher « je suis modérateur » avant le bouton Rejoindre | Fait — `EntryScreen.tsx`, vérifié interactivement | Claude | Chantier 21 |
| H4 | `claim_moderator_status` crée le profil à la volée si absent | Fait — migration `20260727_4_chantier24_claim_moderator_create_profile.sql` **non appliquée** (MCP Supabase indisponible). Échec propre côté client confirmé tant que la migration n'est pas là (aucune donnée créée) | Claude | Chantier 19 (G4) |
| H5 | Texte de l'écran « Débat en cours » | Fait — `VoteScreen.tsx` + `SessionRouterScreen.tsx`. **À vérifier fonctionnellement** (nécessite une séance en `debating`) — voir A_VERIFIER.md | Claude | — |
| H6 | Mise en avant du message « Fais un screen » | Fait — `ReclaimCodeDisplay` (`VoteScreen.tsx`), bandeau dédié plus haut/plus grand. **À vérifier fonctionnellement** (nécessite une séance en `pre_voting`) — voir A_VERIFIER.md | Claude | — |
| H11 | Badge « Vous êtes modérateur » pendant vote/prévote | Fait — `VoteScreen.tsx`, badge + modale explicative. **À vérifier fonctionnellement** (nécessite un membre `is_moderator=true`, donc H4 appliquée) — voir A_VERIFIER.md | Claude | H4 |
| H23 | Modale d'accueil modérateur en phase debating | Fait — `ModeratorView.tsx`, une fois par table (localStorage). **À vérifier fonctionnellement** (nécessite une séance en `debating`) — voir A_VERIFIER.md | Claude | — |

⚠️ Développé en worktree dédié (`chantier-24-flow-moderateur-entree` → `C:\Users\jules\projet\Ecclesia-chantier-24`, port 5186 ajouté à `.claude/launch.json`), en parallèle des chantiers 25 (algo d'allocation) et 28 (nommage des camps) — zones de code disjointes, aucun chevauchement constaté. Voir A_VERIFIER.md pour le détail et le parcours manuel restant.

## Chantier 6 — Analyse des camps (Gemini)
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| A1 | Bug de nommage des camps | Fait & live — symptôme corrigé en prod (fallback frontend). Amélioration Edge (labels neutres) **redéployée** (`gemini-proxy` v9, rapporté 22/07 — à valider empiriquement en k=3+ camps, cf. A_VERIFIER.md) | Claude | — |
| E3 | Nommage Gemini systématique après analyse | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D10 | Assertions consensuelles inter-groupes | Fait — lisibilité (calcul inter-camps préexistant) ; à vérifier — voir A_VERIFIER.md | Claude | A1/E3 |
| C6 | Tracking impact énergétique des appels LLM | **Retiré au chantier 17 (F19)** — l'estimation d'énergie était indicative et sans base fiable ; remplacée par l'affichage du modèle Gemini réellement utilisé | Claude | — |

## Chantier 7 — Fusion des assertions
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| B4 | Fusion des assertions ne marche pas | Fait — migration `update_assertion_content` appliquée + Edge `gemini-proxy` redéployée avec prompt durci (rapporté 22/07, à vérifier fonctionnellement — voir A_VERIFIER.md). **Suite : chantier 18** | Claude | — |

## Chantier 18 — Fusion : prescription vs jugement + annulation
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| F23 | Prescription ≠ jugement dans le prompt de fusion | Fait — règle de typage PRESCRIPTION/JUGEMENT/CONSTAT ajoutée à `buildMergePrompt`, Edge `gemini-proxy` redéployée (**v12**). Vérifié par 1 appel Gemini réel sur le jeu PUBFUS retrouvé en base — voir A_VERIFIER.md | Claude | Chantier 7 (B4) |
| F24 | Annuler une fusion déjà acceptée | Fait — migration `20260728_chantier18_merge_undo` **appliquée** (table `assertion_merges` + `apply_assertion_merge`/`revert_assertion_merge`/`list_assertion_merges`). Aller-retour vérifié en SQL ; rendu visuel du panneau à vérifier avec le mot de passe superadmin — voir A_VERIFIER.md | Claude | Chantier 7 (B4) |

> **Suite prévue (hors chantier)** : session de calibrage dédiée où Jules tranchera cas par cas quelles assertions doivent ou non fusionner, pour affiner le prompt à partir d'exemples réels validés.

## Chantier 8 — Bugs techniques divers
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| A2 | Bugs DND phase débat | Backlog | | — |
| A3 | Vérifier sauvegarde des notes | Backlog | | — |
| A4+D17 | Fin de séance / forçage questionnaire | Backlog | | — |
| C7 | Bug affichage prevote | Backlog | | — |
| B3 | Instabilité user ID / collisions pseudo | Fait (à vérifier — migration non appliquée, voir A_VERIFIER.md) | Claude | — |

## Chantier 9 — Superadmin : gestion des données
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| E1 | Suppression groupée assertions poubelle | Fait — migration `delete_assertions_admin` appliquée en base (rapporté 22/07, à vérifier fonctionnellement — voir A_VERIFIER.md) | Claude | — |
| E2 | Masquer qui a soumis quelle assertion | Fait — migration `hide_assertion_author` appliquée en base (rapporté 22/07, à vérifier fonctionnellement — voir A_VERIFIER.md) | Claude | — |

## Chantier 10 — Petites tâches transverses
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| C1 | Ping automatique Supabase | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| C2 | Identité visuelle / branding | Backlog | | Charte graphique (Jules) |
| C3 | Affichage documents + backend de stockage | **Remplacé par le chantier 13** — décision d'infra tranchée (site externe séparé) | Claude | — |
| C4 | Distinction vote pass/neutre + doc technique | Fait — à vérifier (voir A_VERIFIER.md) | Claude | Jules (doc pol.is) |
| D6 | Mention non-conservation des audios | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D11 | Assertions visibles pendant le débat | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D12 | Mention anonymat des votes | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D13 | Ordre aléatoire des assertions | ⚠️ Correction chantier 12 (F8) — le shuffle initial était bien là, mais les assertions approuvées après coup (cas normal en séance réelle) étaient ajoutées en fin de liste, dans le même ordre pour tout le monde | Claude | — |
| D15 | QR code lien table (modérateurs) | Fait (à vérifier — voir A_VERIFIER.md) | Claude | — |
| D16 | Pouvoir changer son vote | Fait — harmonisé chantier 12 (F9), voir A_VERIFIER.md | Claude | — |

## Chantier 11 — Petits fixes UX
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| F1 | Placeholder générique "Alice Dupont" → confusion | Fait & vérifié en production | Claude | — |
| F2 | Afficher le nom des séances en cours | Déjà correct — vérifié par lecture de code, aucun changement | Claude | — |
| F3 | Tooltip vote affiché une seule fois | Fait — vérifier après un premier passage sur une séance de vote (voir A_VERIFIER.md) | Claude | — |
| F4 | Affordance case "tout sélectionner" superadmin | Fait — checkbox réelle avec état indeterminate (à vérifier — voir A_VERIFIER.md) | Claude | — |
| F5 | Bug : champ "mon nom" se vide sur l'écran de code de rappel | Fait & vérifié en production (bug reproduit puis fix confirmé) | Claude | — |
| F6 | Supprimer "inviter un ami", garder QR code via Outils | Fait — bundle déployé vérifié (à vérifier interactivement — voir A_VERIFIER.md) | Claude | — |
| F7 | Déplacer QR code modérateur dans "Outils Modo" | Fait — nouveau menu créé (à vérifier interactivement — voir A_VERIFIER.md) | Claude | — |

## Chantier 13 — Fiches d'info : 404 persistant
> Décision C3 (2026-07-23, Jules) : les fiches d'info sont hébergées sur un site externe séparé (en construction par l'équipe), pas dans Ecclesia. `doc_info_url`/`doc_summary_url` sont de simples liens externes, sans backend de stockage interne.

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| F10 | 404 sur les fiches d'info d'une séance | Bug corrigé — voir détail ci-dessous et A_VERIFIER.md | Claude | C3 |

**Cause racine identifiée** : trois copies quasi-identiques d'une logique de réécriture d'URL (`normalizeUrl`/`normalizeDocUrl` dans `DocumentationButton.tsx`, `ParticipantToolsButton.tsx`, `VoteScreen.tsx`, `SuperadminScreen.tsx`) réécrivaient `doc_info_url`/`doc_summary_url` vers `https://ecclesia-cs.github.io<BASE_URL>docs/<fichier>` en supposant un stockage interne (`public/docs/*.html` commité dans ce repo). La copie de `VoteScreen.tsx` (panneau "Outils" en phase de vote) avait un `BASE_DOCS` codé en dur **sans** le segment `/Ecclesia-Animation-Moderateur/` (`https://ecclesia-cs.github.io/docs/` au lieu de `https://ecclesia-cs.github.io/Ecclesia-Animation-Moderateur/docs/`) — un 404 systématique sur GitHub Pages dès qu'un participant cliquait "Fiche information"/"Résumé" pendant le vote. Confirmé avec les données réelles en base : la séance de test `🧪 Test général — parcours chantiers 1-4 / 8-10` (phase `voting`, live) a `doc_info_url = "http://localhost:5173/Ecclesia-Animation-Moderateur/docs/fiche-info-test-general.html"` — un artefact de test en local, jamais nettoyé, que seule cette logique de réécriture pouvait transformer en URL de prod (et le faisait mal dans `VoteScreen.tsx`).

**Correctif appliqué** (conforme à la décision C3 — plus de stockage interne) : suppression complète des 4 copies de réécriture d'URL. `doc_info_url`/`doc_summary_url` sont maintenant utilisés tels quels (passthrough), sans transformation. Le champ superadmin (`DocFileField`) est un simple `<input type="url">` en champ libre (au lieu du champ "docs/<fichier>" qui forçait la convention interne). `doc_collab_url` (document collaboratif, fonctionnalité distincte à base de `#collab/<code>`) n'est pas concerné et reste inchangé.

**Reste à faire par Jules** (hors portée code, nécessite le mot de passe superadmin) : mettre à jour `doc_info_url`/`doc_summary_url` de la séance de test `GENER1` (actuellement une URL localhost, cassée pour tout le monde) — soit vider le champ, soit y coller la vraie URL du site externe une fois disponible. Les fichiers `public/docs/*.html` (fiches de test committées à l'ancienne convention) sont maintenant orphelins — plus référencés par aucun code, à supprimer si Jules confirme qu'ils ne servent plus.

## Chantier 14 — Lien séance ↔ table sans admin (suite du chantier 3)
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| F11 | Table créée sans administrateur non rattachée à sa séance d'origine | Bug trouvé et corrigé — voir détail ci-dessous et A_VERIFIER.md | Claude | Chantier 3 |

**Investigation (demandée explicitement avant toute correction)** : les deux seuls parcours de création de table accessibles depuis l'UI — `EntryScreen.tsx` (onglet "Créer", case "Table sans modérateur") et le bouton superadmin "+ Sans admin" (`SuperadminScreen.tsx`, dans le détail d'une séance) — passent tous les deux `session_id` à la RPC (`create_table`/`admin_create_table` respectivement) et **liaient déjà correctement la table à sa séance au niveau du code applicatif**. Vérifié empiriquement (pas seulement lu) : table créée via le vrai formulaire `EntryScreen` contre la séance de test réelle `GENER1` (phase `voting`), puis relue via l'API REST avec le token de la session anonyme du navigateur — `session_id` bien renseigné, table visible avec le bon badge "Sans animateur".

**Cause racine réelle trouvée** (pas un bug d'UI, un trou de sécurité RPC) : la RPC `create_table` avait un garde-fou `IF p_session_id IS NULL THEN RAISE EXCEPTION 'session_required'` ajouté le 2026-05-26 (migration `20260527000003_fix_tables_mandatory_session.sql`). Ce garde-fou a été **silencieusement perdu** le 2026-06-01 quand `20260618_leaderless_tables.sql` a refait un `DROP FUNCTION`/`CREATE OR REPLACE` de `create_table` pour ajouter le paramètre `p_leaderless`, sans reporter la vérification. Depuis, seul le frontend (`EntryScreen.tsx`, champ `<select required>` + `disabled` tant qu'aucune séance n'est choisie) empêche la création d'une table orpheline — en violation du principe déjà établi ailleurs dans ce projet (CLAUDE.md : « comparer les codes côté client — uniquement via `crypt()` en `SECURITY DEFINER` »), ici appliqué à l'intégrité référentielle plutôt qu'à un secret.

**Reproduction empirique** : appel RPC direct (hors UI, via `fetch` avec le token de la session anonyme active dans le navigateur — donc exactement le même chemin serveur que la fonctionnalité "table sans admin" en production) avec `p_pseudo`, `p_creation_code: ''`, `p_session_id: null`, `p_leaderless: true` → HTTP 200, table réellement créée avec `session_id: null`. Confirme que n'importe quel bug frontend futur, régression de l'UI, ou appel externe à la RPC (elle est `SECURITY DEFINER` et joignable par tout client authentifié anonyme) peut aujourd'hui produire l'exact symptôme F11 : une table "sans admin" orpheline, invisible dans `list_session_tables` d'une séance et visible uniquement dans "Tables disponibles à rattacher" du superadmin (rattachement manuel).

**Correctif appliqué** : migration `supabase/migrations/20260727_3_chantier14_create_table_session_required.sql` — restaure le garde-fou `session_required` dans `create_table`, sans autre changement de comportement (le check du code Ecclesia reste sauté pour `p_leaderless=true`, comme depuis `20260619_leaderless_no_code.sql`). **`admin_create_table` n'est volontairement pas touchée** : la création de tables sans séance y est une fonctionnalité intentionnelle et réellement utilisée (section superadmin "Tables disponibles à rattacher" → `attach_table_to_session` en différé) ; seule la voie participant (`create_table`, accessible sans mot de passe superadmin dès qu'une case "sans modérateur" existe) devait être verrouillée côté serveur.

**Migration appliquée et vérifiée par Jules** le 27/07/2026 (via son propre accès Supabase MCP) : appel direct à `create_table` avec `p_session_id: null` confirmé en échec avec l'erreur `session_required`. Aucun changement frontend nécessaire (le chemin UI testé était déjà correct) — `npx tsc -b` et `npm run build` passent sans erreur (aucune modification TypeScript dans ce chantier).

**Données de test** : une table de test a été créée via le vrai formulaire `EntryScreen` (leaderless, rattachée à `GENER1`) et une seconde directement via RPC (orpheline, pour la reproduction) — **toutes deux supprimées par moi-même** avant la fin de la session (`DELETE` REST avec le token du créateur, autorisé par la policy RLS existante sur `tables`). Aucune donnée résiduelle laissée en base.

## Chantier 12 — Ordre des assertions & changement de vote
| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| F8 | Ordre aléatoire des assertions ne fonctionne pas en pratique | Bug réel trouvé et corrigé — `src/screens/VoteScreen.tsx`, insertion à position aléatoire des assertions nouvellement approuvées (Realtime + polling), au lieu d'un append en fin de liste identique pour tout le monde. **Reste à vérifier en conditions réelles multi-participants** — voir A_VERIFIER.md | Claude | D13 |
| F9 | Harmoniser les deux points d'entrée "changer son vote" | Fait — la modale "Voir toutes les assertions" affiche maintenant "icône + Changer" comme le bouton "Voter" et comme la liste "Tes votes". Aucune raison légitime trouvée pour l'ancienne asymétrie (reliquat du fragment WIP D16) | Claude | D16 |

## Chantier 16 — Résultats / clôture

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| F14 | Bouton "voir les résultats" indisponible en phase `closed`, redevient accessible après avoir quitté | Bug réel trouvé et corrigé — voir détail ci-dessous et A_VERIFIER.md | Claude | — |

**Investigation** : l'hypothèse de départ (séance de test sans clustering/analyse faite → indisponibilité "normale") a été écartée après lecture de `ResultsMapScreen.tsx`/`PublicResultsScreen.tsx` : les deux gèrent déjà proprement l'absence d'analyse PCA avec un message "pas encore disponible, revenez plus tard" — ce n'est jamais le bouton lui-même qui aurait dû être affecté par ça.

**Cause racine identifiée** : le bouton "Voir les résultats"/"Voir vos résultats" (overlay "Séance terminée", affiché quand `session?.phase === 'closed'` dans `ParticipantView.tsx`/`ModeratorView.tsx`) était un simple `<a href="#session/${session.join_code}">`, rendu **depuis l'intérieur de `TableView`**, donc pendant que `App.tsx` a `phase.type === 'table'`. Or le guard de routage de `App.tsx` pour la route `#session/` est : `hash.startsWith('#session/') && phase.type !== 'table'` — il exclut explicitement ce cas (par construction, pour laisser la priorité à `TableView` quand on vient de rejoindre une table en retard, cf. commentaire "Guard: si l'utilisateur vient de rejoindre une table en retard"). Résultat : le clic sur le lien change bien `window.location.hash`, mais `App.tsx` ne réévalue jamais la route vers `SessionRouterScreen` puisque `phase.type` reste `'table'` — le bouton semble ne rien faire. En cliquant ensuite sur "← Retour au menu" (`leaveTable()`, qui déclenche `onTableEnd` → `phase: 'entry'`), le hash déjà positionné sur `#session/<code>` devient exploitable au re-render suivant, et la navigation aboutit enfin — d'où le symptôme exact rapporté : "indisponible... redevient accessible après avoir quitté puis retenté".

**Correctif appliqué** : dans `ParticipantView.tsx` et `ModeratorView.tsx`, le `<a href>` est remplacé par un `<button onClick>` qui positionne `window.location.hash` **et** appelle `leaveTable()` dans le même handler, au lieu de compter sur une navigation `<a>` pure que le guard de `App.tsx` bloque tant qu'on est encore rattaché à la table. Aucune autre occurrence du même motif (`href={\`#session/…`, `#vote/…`, `#table/…`\`}` depuis un écran rendu en `phase.type === 'table'`) trouvée ailleurs dans le code.

**Vérifié** : `npx tsc -b` et `npm run build` (worktree dédié) passent sans erreur. Mécanisme confirmé en direct dans le Browser pane (`chantier-16-dev`, port 5181, contre la vraie base Supabase) : création d'une table sans admin rattachée à la séance de test `GENER1` (phase `voting`) → `ParticipantView` s'affiche sans erreur console. Puis reproduction du bug exact du guard de routage : `window.location.hash = '#session/16E27A'` (séance de test déjà `closed`, "Retraite") pendant que la table est encore ouverte → aucune navigation (bug confirmé, page reste sur `TableView`) ; clic sur "Quitter" juste après → `PublicResultsScreen` de la séance "Retraite" s'affiche immédiatement (résolution confirmée, hash déjà positionné + `phase.type` qui change). Ce test valide le mécanisme exact utilisé par le correctif (positionner le hash + quitter la table), sans avoir eu besoin du mot de passe superadmin. **Non vérifié** : le clic réel sur le nouveau bouton "Voir les résultats"/"Voir vos résultats" affiché par l'overlay "Séance terminée" lui-même — nécessite une séance passée en phase `closed` *pendant* qu'un participant est sur `ParticipantView`/`ModeratorView` (mot de passe superadmin requis, non saisi) — voir A_VERIFIER.md.

**Effet de bord assumé** : une table sans admin réelle ("TestF14routing", code `5A3B51`) a été créée et rattachée à la séance de test `GENER1` (phase `voting`) pour ce test — table vide, sans impact sur le déroulé de la séance de test, à nettoyer par Jules si souhaité (superadmin → onglet Tables).

## Chantier 17 — Analyse des camps : E4 repensé + reporting tokens (suite du chantier 6)

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| F15 | Vue superadmin sur `staff_interest` (recrutement modérateurs futurs) | Fait — nouvelle section "🙋 Recrutement modérateurs", onglet Analyse | Claude | Chantier 6 |
| F18 | Renommer "Rapport" → "Rapport token" | Fait | Claude | — |
| F19 | Retirer l'estimation d'énergie, afficher le modèle Gemini utilisé | Fait — nécessite le redéploiement de l'Edge Function pour que le modèle s'affiche (sinon `—`) | Claude | — |
| F20 | Ajouter `thoughts_tokens` au rapport | Fait — nécessite aussi le redéploiement de l'Edge Function | Claude | — |
| F21 | Sous-comptage de tokens sur les tentatives de nommage rejetées | Bug réel trouvé et corrigé — voir détail ci-dessous | Claude | — |
| F22 | Détailler le rapport avec les 3 champs bruts Gemini | Fait | Claude | — |

**F15** : confirmé au préalable que `staff_interest` (table `questionnaire_responses`) est un champ texte libre (nom + contact si intéressé), rempli à la question "Est-ce que tu voudrais staffer chez Ecclesia ?" du questionnaire de fin de séance — **sans lien** avec `session_members.is_moderator` (modérateur pour la séance en cours, critère dur de l'algo d'allocation v2, chantier 19). Nouvelle section dans l'onglet Analyse du superadmin, réutilisant les réponses déjà chargées pour le reste de l'onglet (aucune requête supplémentaire), filtrées sur `staff_interest` non nul.

**F21 (bug corrigé)** : dans `nameSingleGroup` (`src/lib/gemini.ts`), le rejet d'un nom générique par la regex anti-"Groupe N" levait `throw new Error('generic_name')` avant que `tokens_used` ne soit retourné — ces tokens (réellement consommés côté API Gemini) n'étaient donc jamais comptabilisés dans le rapport. Corrigé avec une classe `GenericNameError` qui transporte l'objet `usage` complet de la tentative rejetée ; `groupNaming.ts` l'intercepte et additionne ces tokens à l'agrégat avant de retenter (jusqu'à 2 essais), puis de basculer sur le fallback descriptif si besoin. `recordAiUsage` se déclenche désormais dès que des tokens ont été consommés, même si les 2 tentatives échouent.

**Vérifié** : `npx tsc -b`, `npm run build`, `npm test` (41 tests allocation, non-régression) — worktree dédié `chantier-17-analyse-camps-tokens`, port 5182. Écran d'accueil et connexion superadmin vérifiés sans erreur console dans le Browser pane. Bundle de production grep'é pour confirmer la présence des nouvelles chaînes. **1 appel Gemini réel effectué** (authentification anonyme + appel direct de l'Edge Function déployée, données fictives) pour confirmer qu'un appel `name_single_group` consomme bien des tokens non nuls (720 au total sur cet essai) — valide la prémisse du bug F21 sans avoir pu forcer la reproduction exacte du rejet générique (aléatoire côté Gemini). **Edge Function `gemini-proxy` modifiée (F19/F20/F22 : `thoughtsTokenCount`, `model` dans la réponse) mais non redéployée** — aucun outil MCP Supabase disponible cette session. Aucune migration SQL nécessaire (`staff_interest` existe déjà). Voir A_VERIFIER.md pour le parcours manuel restant.

## Chantier 25 — Allocation : modérateurs en surplus/déficit, persistance, dimensionnement

Retours du test manuel de Jules sur l'algorithme d'allocation v2 (chantier 19), séance `VERIF7`.

| ID | Résumé | Statut | Contributeur | Dépend de |
|---|---|---|---|---|
| H12 | Trop de modérateurs : documenter le comportement | Fait — comportement décrit ci-dessous ; **pas de changement de spec** (à arbitrer avec Jules) | Claude | Chantier 19 |
| H13 | Relancer le calcul en changeant le nb de modérateurs ne change rien | Investigué — **conforme au §4**, pas un bug. Cause identifiée et désormais expliquée dans l'UI | Claude | Chantier 19 |
| H14 | La proposition de tables est perdue au changement d'onglet / reload | Bug réel corrigé — persistance `sessionStorage` | Claude | Chantier 19 |
| H15 | Dimensionnement des tables selon le nb d'enregistreurs | Investigué — **conforme au §5 (règle 2)**, pas un bug de l'algo ; l'objectif effectif est maintenant affiché | Claude | Chantier 19 |
| H16 | Sélection des modérateurs réellement présents | Fait — liste à cocher (inline, pas modale : écart assumé, voir A_VERIFIER.md) | Claude | H12 |
| H17 | Un modérateur en surplus n'est affecté à aucune table | **Bug réel corrigé** — placé comme participant ordinaire | Claude | Chantier 19 |
| H18 | Table fantôme apparaissant plus tard dans le cycle de séance | **Bug réel corrigé** — migration `20260727_4`, **non appliquée** | Claude | Chantier 19 |

**Méthode** : chaque symptôme a été reproduit en exécutant `runAllocation` sur des populations synthétiques *avant* toute correction. C'est ce qui a permis de séparer les trois bugs réels (H14/H17/H18) des deux comportements conformes à la spec mais non expliqués (H13/H15) — et d'éviter de « corriger » un arbitrage lexicographique voulu par la spec.

**H17 (bug réel)** : `runAllocation` ignorait purement les modérateurs d'indice ≥ `shape.moderatedCount`. Avec 4 modérateurs et 3 tables, le 4e n'apparaissait ni dans `moderator_member_ids`, ni dans `member_ids` — donc dans aucune ligne de `table_assignments`. Il est désormais assis dans la table la moins remplie, avec ses attributs réels d'onboarding (nouvelle entrée `moderatorProfiles`). **Le placement est fait après la recherche de forme, pas avant** : une première version qui réinjectait ces personnes dans la population en amont s'est révélée circulaire (asseoir quelqu'un change le nombre de tables, donc le surplus) et régressive — sur 30 participants / 4 modérateurs elle transformait `[10, 10, 10]` en 6 tables.

**H13 (pas un bug)** : `moderatedCount = min(tableCount, capacity)` sature dès que toutes les tables sont animées, et `shapePreference` préfère moins de tables à qualité égale — c'est exactement ce que demande le §4 (« préférer un nombre de tables ≤ nombre de modérateurs »). Contre-épreuve : avec 0 modérateur inscrit, faire varier « Modérateurs à ajouter » de 0 à 3 change bien le résultat à chaque incrément. Le mécanisme fonctionne, il était saturé.

**H15 (pas un bug de l'algo)** : sur 25 participants / 4 modérateurs, `recorderCount` ≤ 2 → `[10, 10, 5]` ; `= 3` → **`[10, 5, 5, 5]`**, la forme exacte décrite par Jules ; `≥ 4` → 5 tables de 5. La valeur en vigueur lors de son test était donc ≥ 3, pas 1 — avec 1 enregistreur l'algorithme produit bien la répartition qu'il dit préférer. La règle 2 étant prioritaire sur la politique de dimensionnement, c'est l'ordre lexicographique de la spec qui joue ; l'objectif effectif est désormais affiché sous le champ et expliqué par un avertissement.

**H18 (bug réel)** : `apply_allocation` réutilise les tables rattachées puis crée les manquantes, sans jamais traiter les excédentaires. Une allocation à 3 tables après un essai à 4 laissait la 4e avec son `session_id` — invisible dans l'onglet Groupes (construit depuis `table_assignments`) mais toujours retournée par `list_session_tables`. La migration détache les excédentaires **vides** (`session_id = NULL`, la table retourne dans le pool réutilisable) et signale celles où quelqu'un a déjà rejoint sans y toucher.

**Vérifié** : `npx tsc -b`, `npm run build`, `npm test` → **49/49** (les 41 tests d'allocation existants passent inchangés + 8 nouveaux). Worktree dédié `chantier-25-allocation-moderateurs`, port 5184 — app chargée, `#superadmin` rendu, **zéro erreur console**. Le module `allocation.ts` réellement servi par Vite a été exercé dans la page : sur 25 participants + 4 modérateurs, 29 personnes placées (contre 28 avant le correctif). **Non vérifié** : tout le parcours UI de `AllocationPanel` (mot de passe superadmin requis, non détenu) et la migration H18 (MCP Supabase indisponible). Voir A_VERIFIER.md.

---

## Annexe B — Reste à faire (éventuel)

> Repris de `PROJECT_STATUS.md` (items encore au statut `Backlog` à sa suppression) et de la section « Reste à faire (éventuel) » de `CLAUDE.md`, allégée au chantier 78. Aucun de ces points n'est engagé ; aucun n'est daté.

**Items encore ouverts dans `PROJECT_STATUS.md` au moment de sa suppression** (identifiants lettrés, cf. annexe A) :

| ID | Résumé | Rattaché à |
|---|---|---|
| D1 | Lecture rapide des règles à l'entrée de table | Chantier 3 |
| A2 | Bugs DND phase débat | Chantier 8 |
| A3 | Vérifier sauvegarde des notes | Chantier 8 |
| A4+D17 | Fin de séance / forçage questionnaire | Chantier 8 |
| C7 | Bug affichage prevote | Chantier 8 |
| C2 | Identité visuelle / branding | Chantier 10 — dépend d'une charte graphique (Jules) |

**Idées non rattachées à un chantier** (ex-section « Reste à faire » de `CLAUDE.md`) :

- ~~Toast notifications~~ — **fait (chantier 88a, 2026-09-08)**, `src/context/ToastContext.tsx`
- ~~Page 404 / table expirée élégante~~ — **fait (chantier 88a, 2026-09-08)**, `src/screens/NotFoundScreen.tsx`
- ~~Persistance de la pause après rechargement (localStorage)~~ — **déjà fait**, antérieurement à cette annexe : `ModeratorView.tsx` la persiste déjà par clé de table, avec revalidation à la reprise. Constaté au chantier 88b (2026-09-08) en préparant ce chantier, aucun code écrit.
- Tests manuels complets sur mobile (iOS Safari, Android Chrome) — toujours ouvert, nécessite le jeton de serveur de dev
- ~~Génération de QR code dans l'UI superadmin (actuellement : site externe)~~ — **fait (chantier 88b, 2026-09-08)**, bouton QR code dans `ShareLinkBanner` (`SuperadminScreen.tsx`)

Ci-dessous, les items D1/A2/A3/A4+D17/C7/C2 du tableau plus haut, mis à jour au chantier 88 : **D1** (règles à l'entrée de table, `DebateRulesModal.tsx`), **A2** (bug DnD, corrigé au chantier 8) et **A3** (sauvegarde des notes, `NotesModal.tsx`) sont désormais **vérifiés au navigateur** (chantier 88c, `A_VERIFIER.md`). **A4+D17** (fin de séance / forçage questionnaire) a été traité autrement par les chantiers 39 et 89 (phase `post_voting`).

**C7 reste ouvert — toujours au statut Backlog, sans repro.** Touche `VoteScreen.tsx`. Le gel du parcours de vote qui le bloquait (`docs/registre-merges-en-attente.md` §1) a expiré le 10/09 et Jules a explicitement donné le feu vert pour le dépasser (chantier 88, 2026-09-08) ; ce n'est donc **plus** ce qui bloque. Ce qui bloque : aucune description concrète du bug n'a jamais été trouvée nulle part dans le dépôt — l'entrée d'origine (`PROJECT_STATUS.md`, chantier 8, juillet 2026) ne contient qu'un titre, jamais de repro écrite, et `VoteScreen.tsx` a été réécrit en profondeur depuis (chantiers 71, 73, 74, plus un fix d'affichage mobile du header le 09/09 — commit `c0a2191`, sans rapport connu avec C7). Jules a choisi de laisser tomber plutôt que de fournir une repro (chantier 88, 2026-09-15). **Pour le reprendre un jour** : il faut d'abord une description ou une repro du bug, avant toute investigation de code.

**C2** (identité visuelle) reste bloqué sur une charte graphique à fournir par Jules.

Deux entrées de cette liste étaient déjà barrées comme faites, et sont conservées ici pour la trace de leur résolution — chacune ayant été traitée **autrement** que ce que l'entrée annonçait :

- ~~Phase `questionnaire` : connecter `SessionRouterScreen` + flow questionnaire participant~~ — **fait (chantier 39)**, autrement : la phase `questionnaire` a été supprimée plutôt que connectée ; `SessionRouterScreen` route désormais vers `SessionQuestionnaireForm` en phase `closed` tant que `questionnaire_responses` ne contient pas de réponse pour le membre.
- ~~Exposer les assertions clivantes (`repness`) depuis `AnalysisPanel` via callback pour les passer à `nameSingleGroup` comme `divisive_assertions`~~ — **fait (chantier 28 / H9)** autrement : `groupNaming.discriminatingAssertions()` recalcule côté client, par camp, les 3 assertions où il s'écarte le plus du reste (proxy de `repness`), sans dépendre d'un callback depuis `AnalysisPanel`.
