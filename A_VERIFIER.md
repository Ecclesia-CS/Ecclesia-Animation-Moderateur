# À vérifier

Liste des points nécessitant une validation humaine, générés lors des sessions Claude Code.
Ne pas supprimer une entrée sans validation explicite de Jules — se contenter de la déplacer en section "Validé" une fois confirmée.

## En attente

- [ ] **2026-07-21** — Chantier 2 (D18/D4/D7) — `src/screens/EntryScreen.tsx`, `src/components/voting/PseudoForm.tsx`, `src/screens/VoteScreen.tsx` (`VotingEntryForm`, `AttendanceConfirmScreen`), `src/components/voting/OnboardingForm.tsx`, `src/lib/storage.ts`

  **Contexte** : trois demandes du doc de réflexion de Jules — D18 (question modérateur strictement oui/non dans le questionnaire d'entrée), D4 (renommer "pseudo" → "nom prénom" partout où l'identité est saisie, avec une consigne "retiens bien ce que tu inscris"), D7 (préremplir ce champ la fois suivante, tout en restant modifiable).

  **⚠️ Point important découvert pendant ce chantier** : l'onboarding (`OnboardingForm`, questionnaire de 6 questions dont celle du modérateur — D18) avait été **temporairement désactivé le 2026-06-04** pour un débat ponctuel (voir l'ancienne section "Changements temporaires" de CLAUDE.md, maintenant retirée). Comme D18 porte justement sur ce questionnaire, je l'ai **réactivé** en restaurant `handlePseudoSuccess`/`handleConfirmAttendanceSuccess` dans `VoteScreen.tsx` à leur comportement d'origine (`setStep('onboarding')` au lieu d'aller direct au vote). Concrètement : en phase `voting` (vote présentiel), tout nouveau participant (ou participant qui confirme sa présence sans avoir déjà répondu) verra maintenant le questionnaire de 6 questions avant de voter. **Décision prise sans confirmation de Jules faute de pouvoir demander en temps réel (session asynchrone)** — à valider : si ce n'est pas souhaité, il suffit de re-commenter ces deux fonctions vers `loadVoteData` direct, en gardant le changement de libellé "Oui/Non" dans `QuestionModerator` pour le chantier 5 (B1) qui refera le questionnaire.

  **Ce qui a changé** :
  - `OnboardingForm.tsx` → question modérateur : les boutons disent maintenant "Oui" / "Non" (au lieu de "Oui" / "Pas nécessaire"). La donnée stockée (`moderator_pref: boolean`) ne change pas.
  - Tous les champs de saisie d'identité (rejoindre/reprendre/créer une table dans `EntryScreen`, formulaire pré-vote `PseudoForm`, formulaire vote présentiel `VotingEntryForm`, reclaim `AttendanceConfirmScreen`) affichent désormais "Nom Prénom" et un texte d'aide type "Retiens bien ce que tu inscris ici". Le champ DB reste `pseudo` (pas de migration — changement d'UI seulement).
  - Nouveau `lastNameStore` dans `src/lib/storage.ts` (clé localStorage `ecclesia_last_name`) : mémorise le dernier nom/prénom saisi avec succès et préremplit (mais reste modifiable) tous les formulaires listés ci-dessus.

  **Déjà vérifié par moi (Browser pane + anon key en lecture, sans rien créer en base)** : les 4 onglets d'`EntryScreen` (Voter/Rejoindre/Reprendre/Créer) affichent bien "Nom Prénom" / "Votre nom Prénom" / "Nom Prénom (animateur)" avec le texte d'aide. `npx tsc -b` compile sans erreur. J'ai aussi vérifié via l'API REST (anon key) qu'aucune séance n'est actuellement active en base (toutes `closed`) — donc rien d'observable plus loin que l'écran d'accueil sans données de test.

  **(1) Parcours manuel à suivre** (une fois les données de test ci-dessous en place) :
  1. Aller sur `#vote/<join_code>` de la séance de test, onglet "Mon nom" (phase `voting`) → vérifier le label **"Nom Prénom"**, le placeholder "Ex : Marie Dupont" et le texte "Retiens bien ce que tu inscris ici...". Entrer un nom, valider.
  2. **Le questionnaire d'entrée doit apparaître** (6 questions) avant le vote — vérifier que l'écran "Modération" affiche bien seulement deux boutons **"Oui" / "Non"** (plus "Pas nécessaire"). Répondre "Non" et vérifier que ça n'empêche pas de continuer, et que `entry_responses.moderator_pref = false` est bien enregistré en base.
  3. Revenir sur `#vote/<join_code>` (même séance ou une autre) depuis le **même navigateur** → le champ nom doit être **prérempli** avec le nom saisi à l'étape 1 (D7), tout en restant modifiable.
  4. Tester le parcours "Reprendre mes votes" (onglet "Mon code de rappel" / écran de confirmation présentielle après un pré-vote) : label "Nom Prénom pré-vote", bannière "Retrouve ton profil pré-vote avec ton nom et prénom ou ton code de rappel", préremplissage du champ nom également attendu là.
  5. Vérifier aussi les formulaires de `EntryScreen` (rejoindre/reprendre/créer une table hors séance) avec des données existantes : label "Nom Prénom", préremplissage.

  **(2) Données de test nécessaires** (à créer par la conversation de vérification dédiée — je n'ai rien créé moi-même) :
  - Une séance (`sessions`) en phase `voting`, avec `join_code` connu.
  - Idéalement un `session_member` déjà inscrit en phase `pre_voting` au préalable (pour tester le reclaim "Reprendre mes votes" à l'étape 4) — sinon un simple nouveau membre en phase `voting` suffit pour les étapes 1-3.
  - Au moins une `table` créée hors séance (ou une séance en phase `draft`/`voting`/`debating` listée) pour l'étape 5 (`EntryScreen` rejoindre/reprendre/créer).
  - Aucune donnée de vote (`assertions`/`assertion_votes`) n'est nécessaire spécifiquement pour D18/D4/D7 — seuls `session_members` et `entry_responses` sont concernés.

- [ ] **2026-07-21** — Chantier 1 (C5/D3/D5/D9) — `src/components/QuitLink.tsx` (nouveau), `src/screens/VoteScreen.tsx`, `src/screens/AllocatingScreen.tsx`, `src/components/voting/TableAssignmentCard.tsx`

  **Contexte** : quatre demandes du doc de réflexion de Jules, toutes côté participant.
  - **C5** : pouvoir quitter et revenir au menu (`EntryScreen`) depuis n'importe quelle phase, pas seulement pendant le débat.
  - **D3** : des messages incitant à recharger la page pendant les phases d'attente asynchrones.
  - **D5** : un bref aperçu du déroulé de la séance (vote → répartition → débat → questionnaire) affiché à la connexion.
  - **D9** : expliquer ce qui se passe pendant la phase "allocating" (formation des groupes).

  **Ce qui a changé** :
  - Nouveau composant `QuitLink` (bouton flottant "← Menu" en haut à gauche, `window.location.hash = ''`) ajouté à tous les écrans de `VoteScreen.tsx` qui n'avaient encore aucun moyen de sortir : `waiting` (attente en phase `draft`), `pseudo` (`PseudoForm`/`VotingEntryForm`), `reclaim_code`, `confirm_attendance`, `onboarding`, `ended`, `questionnaire`. L'étape `vote` a un bouton "Quitter" explicite dans le header (même convention que `ModeratorView`/`ParticipantView`, qui avaient déjà ce bouton). `AllocatingScreen` a aussi `QuitLink` dans son header. **Ces écrans n'ont pas encore de table rejointe** (pas de `TableProvider`), donc "Quitter" ne fait qu'effacer le hash — aucun appel RPC, aucune perte de données. Le comportement existant en phase `debating` (`leaveTable()` via `TableContext`, ne supprime pas le participant) n'a pas été touché.
  - Nouvelle modale `AppIntroModal` dans `VoteScreen.tsx` : affichée une seule fois par séance (clé localStorage `ecclesia_app_intro_<session.id>`), déclenchée dès que la séance est chargée (avant même l'inscription). Résume les 4 phases ; mentionne la durée du vote si `session.vote_timer_minutes` est configuré (sinon message générique).
  - `TableAssignmentCard.tsx` : le message "Formation des groupes en cours…" (phase `allocating` sans assignation encore) est enrichi d'une explication (répartition en groupes aux avis variés à partir des votes) + un lien "Recharge la page". Idem pour l'état "ta table n'est pas encore créée" (phase `debating` sans `join_code`).
  - `VoteScreen.tsx` : bannière de la phase `allocating` (déjà existante pendant qu'on peut encore voter) et écran `waiting` (phase `draft`) ont chacun reçu un lien "Recharge la page" en plus du texte existant.

  **Déjà vérifié par moi** : `npx tsc -b` et `npm run build` passent sans erreur. Testé dans le Browser pane sur `http://localhost:5173` : lien invalide (`#vote/ZZZZZZ`) affiche toujours correctement "Séance introuvable" (non modifié), aucune erreur console sur l'écran d'accueil. **Je n'ai pas pu piloter le parcours participant complet** : aucune séance n'est active dans la base à l'heure où j'écris ceci, et je n'ai ni accès MCP Supabase dans cette session (aucun outil Supabase n'apparaît dans mon inventaire d'outils, malgré ce qu'indique CLAUDE.md — probablement à reconnecter côté configuration Claude Code) ni la possibilité de saisir moi-même le mot de passe superadmin (règle de sécurité stricte, non contournable même avec autorisation explicite). Je n'ai donc créé aucune donnée de test.

  **(1) Parcours manuel à suivre** (une fois la séance de test ci-dessous en place) :
  1. Depuis le superadmin, passer la séance en phase `pre_voting` (ou `voting`). Ouvrir `#vote/<join_code>` dans un nouvel onglet/navigation privée → **avant même de saisir un nom**, la modale "Comment se déroule la séance ?" doit apparaître (4 étapes : vote/répartition/débat/questionnaire), avec la mention de la durée si un timer de vote est configuré pour cette séance. La fermer.
  2. Recharger la page (`#vote/<join_code>`) → la modale **ne doit plus réapparaître** (elle est mémorisée par séance).
  3. Sur cet écran d'inscription (avant d'avoir un nom), vérifier la présence du bouton flottant **"← Menu"** en haut à gauche → cliquer dessus → doit revenir à `EntryScreen` (accueil), sans erreur console.
  4. Repasser la séance en phase `draft` (ou utiliser une séance encore en `draft`) et rejoindre `#vote/<join_code>` avec un nom déjà inscrit → écran "En attente de l'organisateur" : vérifier le lien **"Recharge la page"** sous le spinner, et le bouton "← Menu".
  5. Passer la séance en `voting`/`pre_voting`, voter sur au moins une assertion → dans le header de l'écran de vote, vérifier la présence des 3 boutons **Quitter / Outils / ✏️ Proposer**. Cliquer "Quitter" → retour à l'accueil.
  6. Passer la séance en phase `allocating` **sans avoir lancé le clustering** (`run_clustering_v1`/`v2`) → recharger `#vote/<join_code>` en tant que participant déjà voté : la bannière ambre en haut de l'écran de vote doit expliquer que les groupes sont en formation, avec un lien "recharge la page".
  7. Lancer le clustering (superadmin) puis passer en phase `debating` sans avoir rattaché de table physique au groupe du participant testé → sur `AllocatingScreen`, la carte doit afficher "Formation des groupes en cours…" (si assignation pas encore reçue) ou "Ta table n'est pas encore créée" (si assignation reçue mais pas de `join_code`), chacune avec le lien "Recharge la page". Vérifier aussi le bouton "← Menu" en haut à gauche du header `AllocatingScreen`.
  8. Rattacher une table physique au groupe → le code de table doit apparaître (Realtime ou après le reload suggéré par le message).

  **(2) Données de test nécessaires** (aucune créée par moi — MCP Supabase indisponible + je ne saisis pas le mot de passe superadmin) :
  - Une séance de test pilotable à travers les phases `draft → pre_voting/voting → allocating → debating`, avec `vote_timer_minutes` renseigné sur au moins un cas (pour vérifier le texte dynamique de l'intro D5) et non renseigné sur un autre cas (texte générique).
  - Au moins 2-3 `assertions` approuvées pour pouvoir voter et atteindre la bannière `allocating` à l'étape 6.
  - Au moins un `session_member` inscrit, dont on peut suivre le parcours complet (idéalement avec des votes en attente au moment du passage en `allocating`).

- [ ] **2026-07-21** — Chantier 9 (E1/E2) — `supabase/migrations/20260721_delete_assertions_admin.sql` (nouveau), `supabase/migrations/20260721_hide_assertion_author.sql` (nouveau), `src/lib/voting.ts`, `src/screens/SuperadminScreen.tsx`, `src/screens/VoteScreen.tsx`, `src/components/AnalysisPanel.tsx`, `src/components/voting/LLMModerationPanel.tsx`

  **Contexte** : deux demandes du doc de réflexion de Jules, superadmin uniquement.
  - **E1** : pouvoir supprimer définitivement des assertions de la corbeille (rejetées), en groupe, et pouvoir annuler un import CSV.
  - **E2** : enlever la possibilité pour le superadmin de voir qui a soumis quelle assertion.

  **⚠️ MCP Supabase confirmé indisponible dans cette session** (vérifié à plusieurs reprises pendant ce chantier, à la demande de Jules : aucun outil `mcp__supabase__*` chargeable via ToolSearch, `claude mcp list` → "No MCP servers configured", aucun `.mcp.json` dans le repo, aucun token d'accès Supabase dans `.env`/`.env.local` — seule la clé anon publique y est présente, insuffisante pour `claude mcp add`). Ce n'est pas un problème d'autorisation mais une vraie absence de configuration dans cette session précise ; Jules la corrigera lui-même via un terminal interactif. **Conséquence : je n'ai ni appliqué les 2 migrations ci-dessous, ni créé de données de test, ni pu me connecter en superadmin** (je ne saisis pas de mot de passe applicatif à la place de l'utilisateur, même avec autorisation — cf. règles de sécurité). Tant que les migrations ne sont pas appliquées, la corbeille et le masquage restent du code non déployé.

  **Ce qui a changé** :
  - Nouvelle RPC `delete_assertions_admin(password, session_id, assertion_ids[])` (SECURITY DEFINER) : supprime définitivement les assertions listées (cascade sur `assertion_votes` via la FK existante). Pas de restriction de statut côté SQL — la suppression est possible quel que soit le statut, mais l'UI ne l'expose que pour les assertions rejetées et pour annuler un import CSV.
  - `AssertionsPanel` (onglet "Rejetées") : case à cocher par assertion, "Tout sélectionner/désélectionner", bouton "🗑 Supprimer la sélection (N)", bouton "🗑 Supprimer" par ligne — chacun ouvre une confirmation (`ConfirmModal`, action irréversible).
  - Import CSV : après import, un bouton "↩ Annuler l'import (N)" apparaît et supprime (avec confirmation) exactement les assertions créées par ce lot, quel que soit leur statut ensuite (pending/approved/rejected).
  - `list_assertions_admin` (RPC) ne renvoie plus `member_pseudo` ni `member_id` — seulement `id, session_id, content, status, created_at`. Le pseudo de l'auteur n'apparaît plus nulle part dans `AssertionsPanel`/`AssertionRow` côté superadmin. Type TS renommé `AssertionWithPseudo` → `AssertionAdmin` (plus de champ pseudo/member_id).
  - `VoteScreen.tsx` (`loadVoteData` + polling 10s) : les deux lectures directes de la table `assertions` (`select('*')`) ont été restreintes à `id, session_id, content, status, created_at`, sans `member_id`. Changement de code pur, sans dépendance DB, qui réduit ce qui transite par REST vers le navigateur de chaque participant — mais ne couvre pas le canal Realtime (voir point ci-dessous).
  - **Analyse approfondie de la fuite d'identité, et pourquoi je ne l'ai pas corrigée en profondeur** : `list_assertions_admin` n'était pas le seul chemin où l'auteur d'une assertion est identifiable. Deux policies RLS existantes (versionnées dans les migrations du repo, donc lisibles sans MCP) l'exposent plus largement :
    - `assertions` : policy `assertions_select_approved` = `USING (status = 'approved')` — n'importe quel client authentifié (pas seulement le superadmin) peut lire `member_id` sur les assertions approuvées.
    - `session_members` : policy `session_members_select` = `USING (true)` — n'importe qui peut lire la table entière, donc la correspondance `id ↔ pseudo`.
    En combinant les deux (console navigateur, ou simplement en observant le canal Realtime `vote:<session_id>` que `VoteScreen` utilise déjà — celui-ci reçoit la ligne complète du WAL, `member_id` inclus, indépendamment des colonnes demandées par un `select()`), n'importe qui peut retrouver qui a soumis quoi. **J'ai vérifié avant d'agir que je ne pouvais pas simplement restreindre `session_members_select` à `user_id = auth.uid()`** : `SuperadminScreen.tsx` (`loadGroups`, l.1274-1306) fait une jointure PostgREST directe `table_assignments.select('..., session_members!member_id(pseudo)')` pour afficher les pseudos dans la vue de gestion des groupes (répartition par table, drag & drop entre groupes) — une fonctionnalité superadmin réelle et déjà utilisée, qui casserait immédiatement si cette policy était resserrée sans être d'abord remplacée par une RPC dédiée. Je n'ai donc **pas touché aux policies RLS** : le risque de régression (sur du code que je ne peux pas tester sans accès DB) dépassait le gain, pour un fix qui de toute façon ne fermerait pas le canal Realtime. Fermer complètement ce chemin demande une vraie décision d'architecture (RPC de remplacement pour `loadGroups`, puis policy resserrée, puis potentiellement retirer `member_id` du flux répliqué/Realtime) — hors périmètre de ce chantier tel que formulé, à traiter comme un chantier à part si Jules le souhaite.

  **Déjà vérifié par moi** : `npx tsc -b` et `npm run build` passent sans erreur (aucune régression de typage après le renommage `AssertionWithPseudo` → `AssertionAdmin` ni après la restriction des colonnes dans `VoteScreen.tsx`). Testé dans le Browser pane : l'écran de connexion superadmin (`#superadmin`) s'affiche sans erreur console avant et après ces changements. **Je n'ai pas pu ouvrir une séance ni ses assertions** : pas d'accès MCP Supabase, et je ne saisis pas le mot de passe superadmin à la place de Jules.

  **(1) Parcours manuel à suivre** (une fois les migrations appliquées et les données de test ci-dessous en place) :
  1. Se connecter en `#superadmin`, ouvrir la séance de test, onglet assertions → onglet **"Rejetées"** : vérifier qu'aucun pseudo/nom n'apparaît plus sous chaque assertion (seulement la date), y compris pour les onglets "En attente" et "Approuvées".
  2. Dans "Rejetées", cocher 2-3 assertions → un bouton **"🗑 Supprimer la sélection (N)"** doit apparaître → cliquer → confirmation → les assertions doivent disparaître de la liste et ne plus être comptées dans le badge "Rejetées".
  3. Sur une assertion rejetée isolée, cliquer le bouton **"🗑 Supprimer"** de la ligne → confirmation → suppression individuelle.
  4. Cliquer **"Tout sélectionner"** puis **"Tout désélectionner"** → vérifier le libellé du bouton change et la sélection se vide.
  5. Ouvrir "+ Ajouter des assertions (animateur)" → **Importer CSV** un petit fichier de 3-4 lignes → une fois l'import terminé, un bouton **"↩ Annuler l'import (N)"** doit apparaître à côté de "Importer CSV" → cliquer → confirmation → les N assertions importées doivent disparaître (quel que soit l'onglet où elles étaient tombées : pending si modération fermée, approved si `moderation_policy = 'open'`).
  6. Vérifier qu'après un rechargement de page (F5), le bouton "Annuler l'import" a disparu (il n'est pas censé survivre au rechargement — c'est un état local, pas persistant).
  7. **Ce point-là nécessite un accès DB direct (SQL editor Supabase ou MCP), pas juste l'UI** : pour confirmer l'ampleur réelle de la fuite décrite ci-dessus, ouvrir la console du navigateur sur `#vote/<join_code>` d'une séance de test en tant que simple participant (pas superadmin) et exécuter `await window.supabase.from('session_members').select('*').eq('session_id', '<id>')` puis `await window.supabase.from('assertions').select('*').eq('session_id', '<id>').eq('status','approved')` — si les deux renvoient des lignes avec `pseudo`/`member_id` exploitables, la fuite est confirmée telle que décrite. (Le client Supabase n'est peut-être pas exposé sur `window` selon le build — sinon, reproduire avec les devtools réseau en observant les requêtes REST émises par l'app.)

  **(2) Données de test nécessaires** (à créer par la conversation de vérification dédiée — je n'ai rien créé moi-même) :
  - Une séance en phase `voting` (ou toute phase où l'onglet Assertions est visible), avec `moderation_policy = 'closed'` pour voir les 3 onglets (En attente/Approuvées/Rejetées).
  - Au moins 4-5 assertions au statut `rejected` (soumises par 2-3 `session_members` différents, pour vérifier que le pseudo n'apparaît nulle part une fois qu'on sait qui a soumis quoi en interne).
  - Au moins une assertion `pending` et une `approved` pour vérifier que ces onglets n'affichent pas non plus de pseudo.
  - Pour le point (7) ci-dessus : au moins une assertion `approved` et son `session_member` associé connu, pour confirmer par requête directe que la corrélation reste possible.


- [ ] **2026-07-21** — Chantier 4 (D14/D8) — `src/components/JoinTableForm.tsx` (nouveau), `src/components/InviteFriendButton.tsx` (nouveau), `src/screens/JoinTableScreen.tsx` (nouveau), `src/App.tsx`, `src/screens/SessionRouterScreen.tsx`, `src/screens/VoteScreen.tsx`, `src/screens/ParticipantView.tsx`, `src/screens/ModeratorView.tsx`

  **Contexte** : deux demandes du plan de chantiers (`PROJECT_STATUS.md`, chantier 4 "Rejoindre en cours de séance"). Aucune description détaillée trouvée dans le repo au-delà du résumé d'une ligne (`ecclesia_plan_chantiers.md` référencé par `CLAUDE.md` n'existe pas dans ce dépôt) — interprétation ci-dessous construite en explorant le code existant, à valider par Jules.
  - **D14 — "Rejoindre le débat en retard, quelle que soit la phase"** : gap identifié dans le code — un participant qui n'a jamais voté et arrive alors que la séance est déjà en phase `debating` tombait sur une impasse : `SessionRouterScreen` (`#session/<code>`) affichait juste "Rejoins ta table avec le code affiché en salle" sans aucun moyen de saisir ce code, et `VoteScreen` (`#vote/<code>` direct) affichait "Phase de vote terminée... attends les instructions" — un message trompeur pour quelqu'un qui n'a jamais participé. Dans les deux cas, aucun formulaire de rattrapage n'existait ; il fallait deviner qu'il fallait retourner à l'accueil puis utiliser l'onglet "Rejoindre".
  - **D8 — "Rejoindre un ami via code distribué"** : le code d'une table (`table.join_code`) n'était partageable que verbalement/manuellement (l'onglet "Rejoindre" d'`EntryScreen` existait déjà et fonctionne). Il n'y avait aucun lien direct cliquable (contrairement à `#session/`, `#vote/`, `#collab/` déjà utilisés pour le partage) ni aucun bouton pour qu'un participant déjà en débat partage sa table à un ami.

  **Ce qui a changé** :
  - Nouveau composant partagé `JoinTableForm` (code de table + nom prénom, préempli via `lastNameStore`, appelle `join_table` RPC — même mécanique que l'onglet "Rejoindre" d'`EntryScreen`) — réutilisé dans les 3 écrans ci-dessous pour éviter de dupliquer la logique RPC.
  - Nouvelle route `#table/<join_code>` → `JoinTableScreen` (D8) : écran dédié, code verrouillé/préempli depuis l'URL, juste le nom à saisir. Suit le même schéma que `#vote/`/`#session/` dans `App.tsx` (callback `onTableJoined`, guard `phase.type !== 'table'`).
  - Nouveau bouton `InviteFriendButton` ("Inviter un ami") ajouté dans le header de `ParticipantView` et `ModeratorView`, à côté du code de table déjà affiché : copie `<origin>/<path>#table/<join_code>` dans le presse-papier (`navigator.clipboard.writeText`), confirmation inline "Copié !" pendant 2s (pas de système de toast existant dans le projet).
  - `SessionRouterScreen` : l'état `debating_no_member` (arrivée tardive, jamais inscrit, séance en `debating`) affiche maintenant `JoinTableForm` inline au lieu du message impasse. `onTableJoined` remonté depuis `App.tsx` (nouveau prop optionnel).
  - `VoteScreen` : le step `ended` (même cas, mais atteint via un lien `#vote/<code>` direct) distingue maintenant `session?.phase === 'debating'` (→ `JoinTableForm` inline) du cas générique `questionnaire`/`closed` (message existant conservé, mais qui affiche maintenant `errorMsg` réel au lieu d'un texte toujours identique — petite correction adjacente : le message "Le vote est terminé, tu ne peux plus rejoindre cette séance" était calculé mais jamais affiché).

  **Déjà vérifié par moi** (Browser pane, `npm run dev` dans un *git worktree séparé* sur le port 5174 — la session concurrente chantier-10 tournait déjà sur le port 5173 dans le répertoire partagé, isolation stricte respectée) :
  - `npx tsc -b` et `npm run build` passent sans erreur.
  - `#table/ABC123` → `JoinTableScreen` affiche bien le code verrouillé "ABC123" et le champ nom. Soumission avec un code inexistant → RPC `join_table` répond correctement, erreur "Session introuvable" affichée proprement (testé contre la vraie base Supabase de dev, aucune donnée créée — juste une tentative de jointure avec un code invalide, rejetée côté serveur).
  - `#session/ZZZZZZ` (code inexistant) → toujours "Séance introuvable" correctement affiché (non-régression sur mon refactor de `SessionRouterScreen`).
  - Aucune erreur console sur les deux parcours ci-dessus.
  - **Non vérifiable dans cette session** : aucune séance n'est active en base (`sessions` vide sur les phases pre_voting/voting/allocating/debating au moment du test, vérifié par requête REST anon en lecture seule) → impossible de dérouler le parcours complet avec de vraies données : succès de jointure via `#table/<code>` valide, rendu de `debating_no_member` sur `SessionRouterScreen`, rendu du step `ended`+`debating` sur `VoteScreen`, et affichage/clic du bouton "Inviter un ami" dans `ParticipantView`/`ModeratorView` (nécessite une table de débat active). Pas d'accès MCP Supabase dans cette session (revérifié : aucun outil `mcp__supabase__*` disponible), et je ne saisis pas le mot de passe superadmin à la place de Jules (règle de sécurité, non contournable).

  **(1) Parcours manuel à suivre** (une fois les données de test ci-dessous en place) :
  1. Superadmin : passer une séance en phase `debating` avec au moins une table physique rattachée à un groupe (clustering déjà lancé, `join_code` de table connu).
  2. **D8** : depuis un onglet participant déjà dans le débat (`ParticipantView` ou `ModeratorView`), cliquer "Inviter un ami" dans le header → vérifier que le bouton passe à "Copié !" pendant ~2s, coller le presse-papier ailleurs pour vérifier le contenu (`.../#table/<code_de_la_table>`).
  3. Ouvrir ce lien copié dans un nouvel onglet/navigation privée → `JoinTableScreen` doit afficher le code verrouillé et un champ nom → entrer un nom → "Rejoindre la table" → doit atterrir directement sur `ParticipantView` de cette table, sans passer par le vote.
  4. **D14** : avec un compte/navigateur qui n'a jamais rejoint la séance, ouvrir `#session/<code_de_la_séance>` (lien type QR code) pendant que la séance est en `debating` → au lieu du message impasse, un formulaire "Débat en cours" avec code de table + nom doit apparaître → entrer le code de table (affiché en salle/sur `ModeratorView`) + un nom → doit rejoindre la table directement.
  5. Même test que 4 mais via un lien `#vote/<code_de_la_séance>` direct (au lieu de `#session/`) → même comportement attendu sur l'écran "Débat en cours" (step `ended` de `VoteScreen`).
  6. Vérifier la non-régression : `#session/<code>` pendant que la séance est en `questionnaire` ou `closed` doit toujours afficher le comportement existant (questionnaire / résultats), pas le formulaire de rattrapage (qui est spécifique à `debating`).

  **(2) Données de test nécessaires** (aucune créée par moi) :
  - Une séance en phase `debating`, avec au moins une table physique créée et rattachée à un groupe (pour avoir un `join_code` de table valide à distribuer).
  - Au moins un participant déjà dans cette table (pour tester le bouton "Inviter un ami" depuis `ParticipantView`/`ModeratorView`).
  - Un navigateur/compte "neuf" (jamais inscrit à cette séance) pour jouer le rôle du retardataire (D14) et de l'ami invité (D8).

  **Note de résolution de merge (chantier 4)** : en fusionnant avec `main`, j'ai découvert que le chantier 10 (D15 — QR code table) générait un lien `#join/<code>` qui n'était en réalité géré que par un `useEffect` local dans `EntryScreen.tsx` (préremplit l'onglet Rejoindre) — ce lien ne fonctionnait pas si l'utilisateur qui scanne a déjà une autre table stockée en `localStorage` (l'app resterait sur son ancienne table sans jamais lire le hash), et surtout faisait doublon avec ma route `#table/<code>` (plus robuste : route dédiée au niveau `App.tsx`, cohérente avec `#session/`/`#vote/`/`#collab/`, déjà testée bout-en-bout dans cette session). J'ai donc fait pointer `QrCodeButton` (`ModeratorView.tsx`) vers `#table/<code>` au lieu de `#join/<code>`, et supprimé le `useEffect` `#join/` devenu mort dans `EntryScreen.tsx`. Décision prise seul (session asynchrone) — à valider : si Jules préférait garder les deux schémas ou l'inverse, c'est réversible.

- [ ] **2026-07-21** — Chantier 3 (D1/D2) — `src/lib/debateRules.ts` (nouveau), `src/components/DebateRulesModal.tsx` (nouveau), `src/screens/ParticipantView.tsx`, `src/context/TableContext.tsx`, `supabase/migrations/20260721_designate_moderator.sql` (nouveau)

  **Contexte** : deux demandes du plan "Débat sans admin" (`PROJECT_STATUS.md`), débloquées par le merge de Chantier 1 (navigation). Le plan détaillé (`ecclesia_plan_chantiers.md` cité par `CLAUDE.md`) n'existe pas dans le dépôt — seul le résumé d'une ligne dans `PROJECT_STATUS.md` était disponible pour chaque item. Décisions prises sans pouvoir demander confirmation à Jules en temps réel (session asynchrone headless) :
  - **D1 — "Lecture rapide des règles à l'entrée de table"** : Jules a tranché explicitement en amont de cette session — confirmation individuelle "j'ai lu" par participant, **sans attente synchronisée des autres**. Le texte réel des règles n'a pas encore été fourni par Jules ("je te le donnerai plus tard") : j'ai mis un texte **placeholder** clairement marqué, à remplacer.
  - **D2 — "Désignation d'un admin en cours de débat"** : aucune description détaillée disponible au-delà du titre. **Mon interprétation** (documentée ici faute de mieux) : ce chantier s'appelle "Débat sans admin" et concerne les tables `leaderless` (fonctionnalité existante — table sans modérateur, gérée par claim_floor/file d'attente). J'ai interprété "désignation d'un admin en cours de débat" comme la possibilité, pour n'importe quel participant d'une table `leaderless`, de s'auto-désigner comme animateur à tout moment pendant le débat (ex. si l'auto-gestion par file ne suffit plus) — une "soupape de sécurité" qui transforme la table en table normale avec modérateur. **Si ce n'est pas ce que Jules avait en tête (ex. il s'agissait peut-être d'un vote collectif pour élire l'admin, ou d'une désignation par le superadmin), ce chantier est à refaire.**

  **Ce qui a changé** :
  - `DEBATE_RULES_TEXT` dans `src/lib/debateRules.ts` — **placeholder marqué `// TODO: texte définitif à fournir par Jules`**, à remplacer par le texte réel dès qu'il sera fourni. Contenu actuel : 4 lignes génériques (respect du temps de parole, écoute, courtoisie, usage des files).
  - `DebateRulesModal.tsx` : nouvelle modale, affichée une fois par table côté `ParticipantView` (clé localStorage `debate_rules_read_<tableId>`), **avant** la modale "Bienvenue" existante. Un seul bouton "J'ai lu →" — aucune synchronisation avec les autres participants, aucun état stocké en base (mécanique 100% locale au navigateur, cohérent avec le pattern déjà utilisé pour `debate_welcome_<tableId>`).
  - **`ModeratorView` n'a volontairement pas reçu cette modale** (le composant "Bienvenue" existant est déjà participant-only ; le modérateur, souvent créateur de la table, est supposé connaître les règles). À reconsidérer si Jules voulait un affichage universel.
  - Nouvelle RPC `designate_moderator(p_table_id)` (SECURITY DEFINER, migration `20260721_designate_moderator.sql`) : vérifie que l'appelant est participant de la table et que la table est encore `leaderless` (verrou `FOR UPDATE`, la première tentative gagne), puis passe `leaderless = false` et `created_by = auth.uid()`. Pas de vérification du code Ecclesia — c'est une promotion en cours de débat, pas une création de table.
  - `TableContext.tsx` : nouvelle action `designateModerator()`, met à jour `isModerator` localement (bascule immédiate `ParticipantView` → `ModeratorView` pour le client qui s'auto-désigne) ; les autres clients reçoivent la mise à jour via le mécanisme Realtime existant sur `tables` (déjà conçu pour gérer les changements de `created_by`, réutilisé tel quel, aucune modif nécessaire là).
  - `ParticipantView.tsx` : bouton "🎙️ Devenir animateur" dans le header, visible uniquement si `table.leaderless === true`. Ouvre une `ConfirmModal` avant d'appeler `designateModerator()`. En cas de succès, persiste `isModerator: true` dans `tableStore` (localStorage) pour survivre à un rechargement de page.

  **⚠️ Migration SQL non appliquée** : comme pour le Chantier 9, **aucun outil MCP Supabase n'était disponible dans cette session** (vérifié via `ToolSearch`, aucun résultat). Je n'ai donc pas pu appliquer `20260721_designate_moderator.sql` ni créer de données de test. Tant que la migration n'est pas appliquée, le bouton "Devenir animateur" échouera avec une erreur RPC (fonction inexistante).

  **Déjà vérifié par moi** : `npx tsc -b` et `npm run build` passent sans erreur. Testé dans le Browser pane sur `http://localhost:5173` (réutilisation d'un serveur déjà lancé sur le port 5173 par une autre session — même répertoire de travail partagé) : l'écran d'accueil s'affiche sans erreur console, non affecté par ces changements (aucune séance active en base pour aller plus loin). **Je n'ai pas pu piloter le parcours d'une table `leaderless` réelle** : pas d'accès MCP Supabase pour appliquer la migration ni créer une table de test, et je ne saisis pas le mot de passe superadmin à la place de Jules (règle de sécurité stricte).

  **(1) Parcours manuel à suivre** (une fois la migration appliquée et les données de test ci-dessous en place) :
  1. Appliquer la migration `supabase/migrations/20260721_designate_moderator.sql`.
  2. Depuis le superadmin, créer une table sans animateur (bouton "+ Sans admin") ou cocher "Table sans animateur" depuis `EntryScreen`. Rejoindre cette table avec 2 participants (2 navigateurs/onglets privés différents).
  3. Sur chaque participant, à l'entrée de la table : vérifier que la modale **"Règles du débat"** (texte placeholder) apparaît **avant** la modale "Bienvenue dans le débat", avec un seul bouton "J'ai lu →". Fermer les deux modales.
  4. Recharger la page → aucune des deux modales ne doit réapparaître (mémorisées par table).
  5. Vérifier la présence du bouton **"🎙️ Devenir animateur"** dans le header, uniquement pour cette table `leaderless` (comparer avec une table classique créée avec un animateur : le bouton ne doit pas apparaître).
  6. Sur le participant A, cliquer "Devenir animateur" → confirmation → après validation, le participant A doit basculer instantanément vers la vue **modérateur** (`ModeratorView`, panneau de contrôle complet), sans rechargement.
  7. Sur le participant B (autre onglet), sans action de sa part : dans les ~5-10s (Realtime ou polling), vérifier qu'il voit disparaître le bouton "Devenir animateur" et que le bandeau "Groupe auto-géré" n'a plus lieu d'être (la table n'est plus leaderless — B reste en `ParticipantView` classique, géré par A).
  8. Recharger la page du participant A → il doit rester modérateur après reload (persistance `tableStore`/localStorage).
  9. Côté superadmin (liste des tables de la séance) : vérifier que le badge "Sans animateur" a disparu pour cette table et que le pseudo du participant A apparaît comme animateur.
  10. Cas d'erreur à tester : ouvrir un 3ème onglet participant C sur la même table *avant* l'étape 6, et essayer de cliquer "Devenir animateur" presque simultanément avec A (race condition) → un seul des deux doit réussir, l'autre doit voir un message d'erreur ("Cette table a déjà un animateur"), pas de double-promotion.

  **(2) Données de test nécessaires** (à créer par la conversation de vérification dédiée — je n'ai rien créé moi-même) :
  - Une table `leaderless = true` (créée via superadmin "+ Sans admin" ou `EntryScreen` en cochant "Table sans animateur"), avec au moins 2-3 participants pour tester le comportement multi-client.
  - Idéalement rattachée à une séance en phase `debating` pour un test en conditions réelles, mais une table `leaderless` autonome (sans séance) suffit pour tester D1/D2 isolément.

- [ ] **2026-07-21** — Chantier 5 (B1/B2/E4) — `supabase/migrations/20260721_moderator_responses.sql`, `supabase/migrations/20260721_clustering_v3.sql`, `src/lib/types.ts`, `src/lib/voting.ts`, `src/screens/SuperadminScreen.tsx`, `docs/chantier-5-allocation-design.md`

  **⚠️ Contrainte forte de la session** : **l'accès MCP Supabase était indisponible** — je n'ai **pas pu appliquer les migrations sur la base distante** ni tester les nouvelles RPC de bout en bout dans le navigateur (le dev server pointe sur la même base distante). De plus, tout le code livré vit dans la **vue superadmin authentifiée** (mot de passe que je n'ai pas). **Vérification humaine obligatoire après application des migrations.** Voir aussi `docs/chantier-5-allocation-design.md` pour les choix de design.

  **⚠️ Décisions prises seul (session headless — à valider par Jules)** :
  - Interprétation des intitulés (le détail `ecclesia_plan_chantiers.md` est absent du dépôt) : **E4** = afficher au superadmin l'agrégat des réponses à la question modérateur (`moderator_pref`, la question D18 de chantier-2) ; **B2** = outiller le superadmin pour aligner tables modérées / tables « sans animateur » sur la demande (couche informationnelle, pas de nouvel algorithme de partition) ; **B1** = `run_clustering_v3` opt-in qui équilibre en plus le `participation_style`.
  - **Réconciliation chantier-2** : je n'ai **pas** touché au questionnaire ni ajouté de 7ᵉ question. B2/E4 réutilisent le signal existant `moderator_pref`. La question D18 mesure la *préférence à avoir un modérateur*, pas la *volonté d'être modérateur* — si Jules voulait ce second signal, c'est une évolution à spécifier.
  - **Point produit non tranché** : faut-il que l'algorithme *partitionne* les membres selon `moderator_pref` (au prix de l'hétérogénéité d'opinion) ? Je ne l'ai pas fait unilatéralement — `run_clustering_v3` garde l'hétérogénéité comme objectif primaire.

  **Ce que j'ai vérifié moi-même** : `npm run build` (tsc strict + vite) passe sans erreur ; le dev server tourne ; l'écran d'accueil et la **porte de login** du superadmin s'affichent sans erreur console ; aucune régression sur le chargement de l'app. Je **n'ai pas pu** vérifier le rendu réel des panneaux E4/B2/B1 (auth-gated + RPC absentes de la base tant que les migrations ne sont pas appliquées). Dégradation gracieuse prévue : si les RPC n'existent pas, le panneau E4 est simplement masqué et le toggle B1 remonte une erreur claire — l'app ne casse pas.

  **(1) Parcours manuel à suivre** (une fois les migrations appliquées + données de test en place) :
  1. Appliquer `20260721_moderator_responses.sql` et `20260721_clustering_v3.sql` (via `supabase db push` ou MCP).
  2. **E4** — superadmin → séance en phase `voting` avec plusieurs membres présentiels ayant complété l'onboarding : onglet **En direct** → accordéon **« Réponses modérateur »** (badge `X/Y veulent un modérateur`). Vérifier les 3 compteurs (veulent / sans préférence / non répondu) et la barre de pourcentage cohérents avec les réponses réelles.
  3. **B1** — lancer le clustering : la modale doit afficher la case **« Allocation avancée »** *uniquement si l'analyse des camps est faite* (`status='done'`). Cocher, confirmer → l'allocation doit réussir (appelle `run_clustering_v3`). Vérifier que les tables ne sont pas toutes 100% « écoute » ou 100% « actif » (répartition du `participation_style`), tout en gardant le mélange de camps d'opinion. **Comparer** avec une allocation non cochée (v2). Décocher = comportement inchangé (v2/v1).
  4. **B2 / E4 par table** — une fois en phase `allocating`/`debating` : dans « Réponses modérateur » → section **« Par table »**, chaque table montre `want_count/member_count`. Rattacher une table **sans animateur** (`leaderless`) à un groupe où des membres veulent un modérateur → un **⚠️ badge ambre** doit apparaître (dans le panneau E4 *et* sur la carte du groupe, onglet Tables). Rattacher une table avec animateur → le badge redevient neutre (indigo).
  5. **Cas dégradé** : si une migration n'est **pas** appliquée, vérifier que le superadmin ne plante pas (panneau E4 absent ; cocher « Allocation avancée » puis confirmer doit afficher une erreur lisible, pas un crash).

  **(2) Données de test nécessaires** (à créer par la conversation de vérification — je n'ai rien créé) :
  - Une séance en phase `voting` avec **plusieurs membres présentiels** (`attending_in_person=true`) ayant complété l'onboarding, avec un **mélange** de réponses : certains `moderator_pref=true`, d'autres `false` ; certains `participation_style='listener'`, d'autres `'active'`.
  - L'**analyse des camps** lancée (`session_analysis status='done'`) pour pouvoir tester B1 (v3) et la répartition hétérogène.
  - Au moins **2 tables physiques rattachées**, dont une `leaderless` et une avec animateur, pour tester les badges de demande B2.

- [ ] **2026-07-21** — Chantier 10 (C1/D6/D11/D12/D13/D15) — `.github/workflows/supabase-ping.yml` (nouveau), `src/components/voting/OnboardingForm.tsx`, `src/screens/VoteScreen.tsx`, `src/components/AssertionsButton.tsx` (nouveau), `src/components/QrCodeButton.tsx` (nouveau), `src/screens/ModeratorView.tsx`, `src/screens/EntryScreen.tsx`, `package.json` (ajout dépendance `qrcode`)

  **Contexte** : petites tâches transverses indépendantes du doc de réflexion de Jules. C2/C3/C4 et D16 sont explicitement hors scope de cette itération (C2/C3/C4 attendent des inputs de Jules ; D16 a un fragment WIP non fonctionnel isolé sur la branche `chantier-10-revote-fragment-incomplet`, créée par la session de merge à partir de code trouvé non commité — pas repris ici).

  **⚠️ MCP Supabase toujours indisponible dans cette session** malgré le server id communiqué (`bc4cebec-26f6-4a73-88c1-600037f18184`) — vérifié via `ToolSearch` à deux reprises, aucun outil `mcp__supabase__*` chargeable. Je n'ai donc appliqué aucune migration (aucune n'était de toute façon nécessaire pour ces items — tous sont du changement pur frontend/CI) et n'ai créé aucune donnée de test.

  **⚠️ Répertoire de travail partagé entre sessions concurrentes** : pendant ce chantier, le port 5173 était occupé par le serveur de dev d'une autre session (vraisemblablement le worktree `travail-parallele-a`, visible dans `git worktree list`) — confirmé par des hot-reloads `ParticipantView.tsx` que je n'avais pas déclenchés, et par le fait que mon édition de `EntryScreen.tsx` n'y apparaissait pas. J'ai donc lancé un serveur Vite isolé sur un port libre (5189, via `npx vite --port` sans `--strictPort`, laissé auto-incrémenter) uniquement pour vérifier D15, puis je l'ai arrêté immédiatement après (`taskkill`). Je n'ai pas touché `.claude/launch.json` (fichier commité partagé).

  **Ce qui a changé** :
  - **C1** — nouveau workflow GitHub Actions `.github/workflows/supabase-ping.yml` : `curl` quotidien (06:00 UTC, + déclenchement manuel `workflow_dispatch`) vers `${VITE_SUPABASE_URL}/rest/v1/` avec la clé anon existante (secrets déjà configurés pour `deploy.yml`, aucun nouveau secret nécessaire). Objectif : éviter la mise en pause auto du projet Supabase gratuit après ~7 jours d'inactivité API. Échoue explicitement (`::error::` + exit 1) si Supabase répond en 5xx, pour être visible dans l'onglet Actions du repo en cas de vrai problème.
  - **D6** — `OnboardingForm.tsx` (`QuestionConsent`, question 1/6 sur la transcription) : ajout d'une phrase sous la question — "Seul le texte transcrit et anonymisé est conservé. L'enregistrement audio n'est utilisé qu'en direct pour produire cette transcription et n'est jamais sauvegardé." Cohérent avec le pipeline `transcription-debat/` (offline, pas de stockage audio décrit dans son propre CLAUDE.md).
  - **D11** — nouveau composant `AssertionsButton.tsx` (pattern identique à `NotesButton.tsx`), ajouté dans le header de `ModeratorView.tsx` entre "Notes" et "Questionnaire" ; ne s'affiche que si `table.session_id` existe (même garde que côté participant). Ouvre `VoteResultsList` (déjà utilisé côté `ParticipantToolsButton`/`ParticipantView`, réutilisé tel quel). Le modérateur peut désormais consulter la liste complète des assertions votées et leurs résultats pendant le débat, comme les participants le pouvaient déjà via le bouton "Outils" → "Résultats du vote".
  - **D12** — `VoteScreen.tsx`, modale "Comment fonctionne le vote ?" (affichée à chaque entrée en phase de vote) : ajout d'un 4ème point "🔒 Ton vote est anonyme — Ni les autres participants ni les organisateurs ne peuvent voir comment tu as voté. Un lien technique existe dans notre base de données pour te permettre de voter, mais il n'est jamais consulté." Formulation reprise du doc de réflexion de Jules (anonymat vis-à-vis des autres et des admins, mention explicite du lien technique en base).
  - **D13** — **déjà implémenté**, aucun changement de code nécessaire. `VoteScreen.tsx` mélange déjà l'ordre des assertions par un shuffle Fisher-Yates (`shuffle()`, ligne ~26), appliqué séparément aux assertions non-votées et votées à chaque `loadVoteData` — donc un ordre différent par participant (le générateur `Math.random()` tourne côté client, par session de vote). Vérifié en lisant `git log`/`git show HEAD` : cette fonction était déjà committée avant le début de ce chantier, pas une régression fantôme. Limite connue, non bloquante : les nouvelles assertions qui arrivent en cours de vote (Realtime/polling 10s) sont ajoutées à la fin de la liste plutôt que mélangées dans les non-votées restantes — acceptable pour la version "aléatoire" à court terme demandée par Jules (l'algo de pertinence est explicitement noté "moyen terme" dans sa demande).
  - **D15** — nouveau composant `QrCodeButton.tsx` (dépendance `qrcode` ajoutée à `package.json`/`package-lock.json`, génération SVG 100% locale, aucun appel réseau vers un service tiers), ajouté dans le header de `ModeratorView.tsx` à côté du code de table, avec pour valeur `${origin}${pathname}#join/<table.join_code>`. Nouveau hash `#join/<code>` géré par un `useEffect` dans `EntryScreen.tsx` : préremplit le champ "Code de session" de l'onglet "Rejoindre" et bascule automatiquement sur cet onglet, puis nettoie le hash (`history.replaceState`, même pattern que les autres écrans). Aucune modification de `App.tsx` nécessaire — `#join/` n'est pas intercepté par les routes existantes (`#collab/`, `#session/`, `#vote/`, `#superadmin`), il retombe naturellement sur `EntryScreen`.

  **Déjà vérifié par moi** : `npx tsc -b` et `npm run build` passent sans erreur. Testé en isolation (serveur Vite dédié port 5189, voir note ci-dessus) : navigation vers `#join/ZZ99YY` → le champ "Code de session" de l'onglet "Rejoindre" est bien prérempli avec `ZZ99YY` et le hash est nettoyé (vérifié via `location.hash` et la valeur des inputs en JS, pas de capture d'écran possible — l'outil `computer`/screenshot a systématiquement timeout dans cet environnement pendant ce chantier, sans rapport avec le code testé). Génération SVG du QR code testée en dehors du navigateur (`node -e` avec le package `qrcode`) : fonctionne, produit un SVG valide. **Je n'ai pas pu tester** : le rendu réel du bouton QR/Assertions dans `ModeratorView` (nécessite une vraie table avec modérateur + séance), ni le texte D6/D12 en contexte (nécessite une séance en phase `voting`/`pre_voting` avec un membre arrivant à l'onboarding/au vote) — aucune donnée de ce type en base et MCP Supabase indisponible pour en créer.

  **(1) Parcours manuel à suivre** (une fois les données de test ci-dessous en place) :
  1. **C1** : vérifier dans l'onglet "Actions" du repo GitHub que le workflow "Supabase keep-alive ping" apparaît et peut être déclenché manuellement (bouton "Run workflow") ; lancer une exécution manuelle et vérifier qu'elle se termine en succès (HTTP 200 loggé).
  2. **D6** : rejoindre une séance en phase `voting` en tant que nouveau participant → à la question 1/6 (consentement transcription), vérifier la présence de la phrase sur la non-conservation de l'audio sous la question.
  3. **D11** : côté modérateur d'une table rattachée à une séance, en phase débat → vérifier la présence du bouton "Assertions" dans le header (à côté de Notes/Questionnaire) → cliquer → la liste des assertions votées avec leurs résultats doit s'afficher. Vérifier qu'il n'apparaît PAS si la table n'est pas rattachée à une séance (`table.session_id` null).
  4. **D12** : en phase de vote, à l'ouverture de la modale "Comment fonctionne le vote ?" (première assertion) → vérifier la présence du 4ème point "🔒 Ton vote est anonyme".
  5. **D13** : ouvrir la même séance de vote depuis 2 navigateurs différents (2 participants) → comparer l'ordre des assertions affichées → doit différer entre les deux.
  6. **D15** : côté modérateur d'une table → vérifier la présence du bouton "QR" dans le header, à côté du code de table → cliquer → un QR code doit s'afficher, avec le lien en clair en dessous. Scanner ce QR code avec un téléphone (ou copier le lien et l'ouvrir dans un nouvel onglet/navigation privée) → **[mis à jour par le chantier 4, voir sa note de résolution de merge ci-dessus]** doit arriver directement sur l'écran "Un ami t'invite à le rejoindre" (`#table/<code>`), avec le code de table déjà verrouillé → il ne reste plus qu'à saisir un nom et cliquer "Rejoindre la table".

  **(2) Données de test nécessaires** (à créer par la conversation de vérification dédiée — je n'ai rien créé moi-même) :
  - Pour D6/D12/D13 : une séance en phase `voting` ou `pre_voting` avec au moins 3-4 assertions approuvées, et la possibilité de s'inscrire comme nouveau participant (pour atteindre l'onboarding et la modale d'intro au vote).
  - Pour D11/D15 : une table rattachée à une séance, avec un animateur (pas `leaderless`), en phase débat (`ModeratorView` accessible) — idéalement avec quelques assertions votées en amont pour que le bouton "Assertions" affiche un contenu non vide.
  - Pour C1 : aucune donnée de test — juste un accès à l'onglet Actions du repo GitHub (permissions repo, pas données applicatives).

- [ ] **2026-07-21** — Chantier 6 (A1/E3/D10/C6) — Analyse des camps / Gemini — `src/lib/aiUsage.ts` (nouveau), `src/lib/groupNaming.ts` (nouveau), `src/components/voting/LLMModerationPanel.tsx`, `src/screens/SuperadminScreen.tsx`, `src/components/AnalysisPanel.tsx`, `src/lib/gemini.ts`, `supabase/functions/gemini-proxy/index.ts`

  **⚠️ MCP Supabase indisponible dans cette session** (vérifié 2× via `ToolSearch`, aucun `mcp__supabase__*` chargeable) → **aucune migration appliquée** (aucune n'était nécessaire — tout est frontend + Edge Function) et **aucune donnée de test créée**. Le plan détaillé `ecclesia_plan_chantiers.md` référencé par `PROJECT_STATUS.md` **n'existe pas dans le repo** — je me suis appuyé sur les libellés une-ligne de `PROJECT_STATUS.md` + la section "Modération IA / Bug connu" de `CLAUDE.md`. Interprétation de chaque item documentée ci-dessous.

  **⚠️⚠️ POINT DE VÉRIFICATION LE PLUS IMPORTANT — l'Edge Function `gemini-proxy` N'A PAS ÉTÉ DÉPLOYÉE** (MCP indispo, pas de déploiement possible depuis cette session). Le fix A1 côté serveur (labels neutres A/B/C) est donc **présent dans le code source mais PAS actif en production** tant que la fonction n'est pas redéployée (via MCP `deploy_edge_function` ou `supabase functions deploy gemini-proxy`). **En attendant, le symptôme A1 est déjà couvert côté frontend** par le fallback descriptif (voir ci-dessous), qui garantit qu'aucun camp ne s'affiche jamais « Groupe N » quoi que retourne Gemini.

  **Ce qui a changé, par item** :

  - **A1 — Bug de nommage « Groupe N »** (2 volets) :
    1. **Frontend, actif immédiatement** — nouveau `src/lib/groupNaming.ts` : quand Gemini échoue ou retourne un identifiant technique (« Groupe 3 », « Camp A », lettre seule — rejet élargi dans `gemini.ts`, regex `/^(groupe|camp)\s*([0-9]+|[a-z])$/i` → retry ×2), le fallback ne met plus `name: "Groupe N"` mais un **nom descriptif dérivé des votes réels du camp** (`deriveFallbackName`) : on choisit l'assertion sur laquelle le camp est le plus tranché (max |agree−disagree|) et on en fait une étiquette « Plutôt pour/contre : « <extrait> » ». Cas sans vote → « Camp peu tranché ». **Ne renvoie jamais « Groupe N »** (validé par script de logique pure, 11/11 assertions métier OK).
    2. **Edge Function, NON déployée** — `gemini-proxy/index.ts` : le prompt `name_single_group` utilise désormais des étiquettes **neutres** « Camp A / Camp B / … » (`serializeGroupsNeutral`) au lieu de « Groupe 1/2/3 » dans le contexte envoyé à Gemini. C'est l'hypothèse non testée n°2 listée dans `CLAUDE.md` §"Bug connu" : sans numéro dans le contexte, le modèle ne peut plus recopier « Groupe N ». La réponse ne contient que `{name, description}` (le `table_number` est réattaché côté frontend) → renommage de contexte **sans risque pour le mapping**. **À valider empiriquement après déploiement** (k=3+ groupes).

  - **E3 — Nommage systématique après analyse** : le nommage ne se déclenchait qu'en phase `allocating`. Désormais, un calcul d'analyse **manuel** ("Analyser les camps") en phase `voting`/`pre_voting` déclenche aussi le nommage (`AnalysisPanel.onAnalysisComplete` → `SuperadminScreen.handleAnalysisNaming`), en dérivant les camps des clusters k-means (`group_id + 1 = table_number`). La logique de nommage est mutualisée dans `runNaming` (cache par empreinte `groupsFingerprint` conservé → ne rappelle Gemini que si la composition des camps a changé). **DÉCISION PRISE (à valider) : NON déclenché sur les ticks d'auto-analyse** (setInterval), pour éviter de spammer Gemini à chaque recomposition de clusters pendant le vote actif — uniquement sur analyse manuelle et en `allocating`. Voir "DÉCISIONS À VALIDER" dans le rapport.

  - **D10 — Assertions consensuelles inter-groupes** : le calcul inter-camps existait **déjà** (`analysis.ts` `computeGroupConsensus` = min des moyennes par camp ; affiché dans `AnalysisPanel` et `ResultsMapScreen`). Item marqué dépendant de A1/E3. **Interprété comme un item de lisibilité** : reformulation du bloc "Assertions consensuelles" de `AnalysisPanel` pour expliciter le sens inter-camps ("assertions que **tous les camps approuvent en moyenne** malgré leurs désaccords", explication du score, meilleur message vide). **Aucun nouveau calcul.** Si Jules attendait davantage (ex : exposer ces assertions aux participants ailleurs, ou les passer à Gemini comme contexte de convergence symétrique à `divisive_assertions`), à préciser — voir "DÉCISIONS À VALIDER".

  - **C6 — Tracking impact énergétique des appels LLM** : nouveau `src/lib/aiUsage.ts` centralise journal + compteurs journaliers + **estimation énergétique**. Bloc "🌱 Énergie estimée" ajouté au rapport de `LLMModerationPanel` (énergie de la séance + du jour, équivalence en charges de smartphone). **Les tokens du nommage des camps, auparavant jetés, sont désormais comptabilisés** (`recordAiUsage` appelé depuis `groupNaming.generateGroupNames`) → le rapport couvre enfin *tous* les appels Gemini (modération + fusion + nommage). **DÉCISION À VALIDER : facteur `WH_PER_TOKEN = 0.0003 Wh/token`** — aucun chiffre officiel pour Gemini 2.5 Flash Lite, valeur indicative isolée dans `aiUsage.ts`, affichée explicitement comme "ordre de grandeur, pas une mesure". Options dans le rapport.

  **Déjà vérifié par moi** : `npx tsc --noEmit` et `npx vite build` passent sans erreur. Logique pure des nouveaux helpers validée hors navigateur (`deriveFallbackName` ne renvoie jamais "Groupe N", choisit la position la plus tranchée, gère no-vote/négatif ; `groupsFingerprint` ordre-indépendant ; math énergie correcte). Superadmin chargé dans le Browser pane (port dédié 5178, 5173 occupé par une autre session) → écran d'auth rendu **sans erreur console**. **Je n'ai PAS pu vérifier en conditions réelles** : le rapport énergie, le nommage + fallback, le déclenchement E3 et le bloc consensus D10 — tous nécessitent une **session superadmin authentifiée + données de vote réelles + analyse `status='done'`**, impossibles à créer sans le mot de passe superadmin ni le backend (MCP indispo). L'Edge Function A1 n'a pu être ni déployée ni testée.

  **(1) Parcours manuel à suivre** (session superadmin authentifiée requise) :
  1. **C6** : ouvrir une séance avec de l'activité IA (modération/fusion déjà effectuées) → panneau "🤖 Modération IA" → bouton "Rapport" → vérifier le bloc "🌱 Énergie estimée" (séance + aujourd'hui + équivalence smartphone). Lancer une modération puis vérifier que les compteurs augmentent. Vérifier qu'après un nommage des camps, une ligne "name_groups — N camp(s) nommé(s)" apparaît dans l'Historique et que les tokens correspondants sont comptés.
  2. **E3** : séance en phase `voting` avec assez de votes → onglet Analyse → "Analyser les camps" → une fois le scatter affiché, vérifier que les camps se **nomment automatiquement** dans la foulée (légende + en-têtes "Assertions clivantes" passent de "Groupe N" à des noms descriptifs), sans passer par `allocating`.
  3. **A1 (fallback frontend)** : après nommage, vérifier qu'**aucun camp** n'affiche « Groupe N » ni « Camp A » — au pire un nom descriptif « Plutôt pour/contre : … » ou « Camp peu tranché ». Tester spécifiquement avec **k=3+ camps** (cas historiquement cassé).
  4. **A1 (Edge, après redéploiement uniquement)** : une fois `gemini-proxy` redéployée, refaire un nommage k=3+ et vérifier que **Gemini lui-même** produit des noms descriptifs pour TOUS les camps (plus de fallback déclenché pour le dernier). Consigner si le bug persiste malgré les labels neutres (cela confirmerait qu'il faut tester `gemini-2.5-flash` non-lite, hypothèse restante du `CLAUDE.md`).
  5. **D10** : onglet Analyse, bloc "Assertions consensuelles" → vérifier le nouveau texte explicatif inter-camps et le message vide clarifié.

  **(2) Données de test nécessaires** (à créer par la conversation de vérification — je n'ai rien créé) :
  - Une séance en phase `voting` avec **≥6 votants présentiels** et **≥5 assertions approuvées** bien votées (seuils min de `analysis.ts` : `MIN_VOTERS=6`, `MIN_ASSERTIONS=5`), avec des **profils de vote contrastés** pour que le k-means dégage **au moins 3 camps distincts** (condition pour reproduire le bug A1 historique). Idéalement des votes qui rendent certaines assertions consensuelles (approuvées par tous) et d'autres clivantes.
  - Le **mot de passe superadmin** pour accéder à l'onglet Analyse et lancer modération/fusion/analyse/nommage.
  - Pour tester le fix Edge A1 : **redéployer `gemini-proxy`** au préalable.

## Validé

<!-- déplacer ici une fois vérifié, au format : - [x] **AAAA-MM-JJ (validé le AAAA-MM-JJ)** — `fichier` — description -->
